apiVersion: apps/v1
kind: Deployment
metadata:
  name: game-session-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: game-session-service
  template:
    metadata:
      labels:
        app: game-session-service
    spec:
      containers:
        - name: game-session-service
          image: game-session-service:latest
          ports:
            - containerPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: game-session-service
spec:
  selector:
    app: game-session-service
  ports:
    - port: 8080
      targetPort: 8080
  type: ClusterIP
