apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  namespace: firemud
  name: firemud-grpc-tls
spec:
  secretName: firemud-grpc-tls
  dnsNames:
    - "firemud.example.com"
  issuerRef:
    name: firemud-ca-issuer
    kind: ClusterIssuer
