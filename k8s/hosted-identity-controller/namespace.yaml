apiVersion: v1
kind: Namespace
metadata:
  # These fixed control-plane labels must be restored when the namespace is
  # deleted and recreated during controller installation or recovery.
  name: firemud-system
  labels:
    app.kubernetes.io/part-of: firemud
    firemud.dev/managed-by: hosted-identity-controller-bootstrap
    firemud.dev/control-plane: "true"
    pod-security.kubernetes.io/enforce: restricted
    pod-security.kubernetes.io/audit: restricted
    pod-security.kubernetes.io/warn: restricted
