apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# This base is limited to shared PrometheusRule resources; profile overlays add profile-specific rules.
resources:
  - prometheus-rules-firemud.yaml
