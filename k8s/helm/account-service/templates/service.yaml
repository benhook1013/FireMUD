apiVersion: v1
kind: Service
metadata:
  name: account-service
spec:
  type: {{ .Values.service.type }}
  selector:
    app: account-service
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8080
