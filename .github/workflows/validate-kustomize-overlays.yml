name: Validate Kustomize Overlays

on:
  pull_request:
    paths:
      - "k8s/**"
      - "design/operations/deployments/production/attestations/**"
      - "design/operations/deployments/production/backup-readiness/**"
      - "dev-tools/ci/validate-kustomize-overlays.sh"
      - "dev-tools/deploy/preflight.sh"
      - ".github/workflows/validate-kustomize-overlays.yml"
  workflow_dispatch:

permissions:
  contents: read
  packages: read

jobs:
  validate-overlays:
    runs-on: ubuntu-latest
    if: ${{ github.actor != 'dependabot[bot]' }}
    steps:
      - name: ⬇️ Checkout Code
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: 🧰 Set up kubectl
        uses: azure/setup-kubectl@v4

      - name: 🐳 Set up Docker
        uses: docker/setup-buildx-action@v3

      - name: 🔐 Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: ⚙️ Set Up Java
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '21'

      - name: ⚙️ Set Up Gradle
        uses: gradle/actions/setup-gradle@v5

      - name: 🏗️ Build service jars for overlay validation
        run: ./gradlew :account-service:bootJar :automation-scripting-service:bootJar :entity-management-service:bootJar :game-design-service:bootJar :game-logic-service:bootJar :game-session-service:bootJar :logging-admin-service:bootJar :social-groups-service:bootJar :spring-cloud-gateway:bootJar :tcp-proxy-service:bootJar :world-management-service:bootJar

      - name: 🏗️ Build PR images for overlay validation
        run: |
          docker build \
            -f docker/base.Dockerfile \
            -t ghcr.io/benhook1013/firemud-base:latest \
            .

          services="account-service automation-scripting-service entity-management-service game-design-service game-logic-service game-session-service logging-admin-service social-groups-service spring-cloud-gateway tcp-proxy-service world-management-service"
          for service in $services; do
            docker build \
              --build-arg BASE_IMAGE=ghcr.io/benhook1013/firemud-base:latest \
              -t ghcr.io/benhook1013/$service:latest \
              -f ./services/$service/Dockerfile \
              .
          done

      - name: ✅ Validate overlays and image availability
        run: bash dev-tools/ci/validate-kustomize-overlays.sh
