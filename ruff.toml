line-length = 120
# Repository tools remain compatible with the supported local Python 3.10 runtime;
# CI validates that same source on a newer interpreter.
target-version = "py310"

[lint]
# Repository tools are invoked through an explicit Python interpreter; executable
# mode is not part of their source contract.
extend-ignore = ["EXE001"]
