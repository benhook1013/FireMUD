{
  "title": "Player Experience",
  "description": "Player-experience calibration views are informational and non-enforcing until a deployment profile promotes a measured objective with minimum-sample and multi-window policy.",
  "schemaVersion": 36,
  "version": 1,
  "timezone": "browser",
  "panels": [
    {
      "type": "graph",
      "title": "Login Success Ratio by Service (15m Calibration)",
      "targets": [
        {
          "expr": "sum by (scope, service) (rate(login_requests_total{outcome=\"success\"}[15m])) / sum by (scope, service) (rate(login_requests_total{outcome=~\"success|server_failure\"}[15m]))",
          "legendFormat": "{{scope}} {{service}}",
          "refId": "A"
        }
      ],
      "id": 1
    },
    {
      "type": "graph",
      "title": "Login Success Ratio by Service (5m Trend)",
      "targets": [
        {
          "expr": "sum by (scope, service) (rate(login_requests_total{outcome=\"success\"}[5m])) / sum by (scope, service) (rate(login_requests_total{outcome=~\"success|server_failure\"}[5m]))",
          "legendFormat": "{{scope}} {{service}}",
          "refId": "A"
        }
      ],
      "id": 6
    },
    {
      "type": "graph",
      "title": "Command Latency (p99)",
      "targets": [
        {
          "expr": "histogram_quantile(0.99, sum by (scope, le, command) (rate(command_end_to_end_latency_ms_bucket[5m])))",
          "legendFormat": "{{scope}} {{command}}",
          "refId": "A"
        }
      ],
      "id": 2
    },
    {
      "type": "graph",
      "title": "Command Latency (p99) by Service",
      "targets": [
        {
          "expr": "histogram_quantile(0.99, sum by (scope, service, le, command) (rate(command_end_to_end_latency_ms_bucket[5m])))",
          "legendFormat": "{{scope}} {{service}} {{command}}",
          "refId": "A"
        }
      ],
      "id": 7
    },
    {
      "type": "graph",
      "title": "Telnet/WebSocket Path Availability (5m Trend)",
      "targets": [
        {
          "expr": "sum by (service, scope, path) (rate(entrypath_connection_attempts_total{outcome=\"success\"}[5m])) / sum by (service, scope, path) (rate(entrypath_connection_attempts_total{outcome=~\"success|server_failure\"}[5m]))",
          "legendFormat": "{{service}} {{scope}} {{path}}",
          "refId": "A"
        }
      ],
      "id": 3
    },
    {
      "type": "graph",
      "title": "Telnet/WebSocket Path Availability (1d Calibration)",
      "targets": [
        {
          "expr": "sum by (service, scope, path) (increase(entrypath_connection_attempts_total{outcome=\"success\"}[1d])) / sum by (service, scope, path) (increase(entrypath_connection_attempts_total{outcome=~\"success|server_failure\"}[1d]))",
          "legendFormat": "{{service}} {{scope}} {{path}}",
          "refId": "A"
        }
      ],
      "id": 8
    },
    {
      "type": "graph",
      "title": "Chat Delivery Latency (p99)",
      "targets": [
        {
          "expr": "histogram_quantile(0.99, sum by (service, scope, completion_boundary, le, channel_type) (rate(chat_delivery_latency_ms_bucket{completion_boundary=\"recipient_dispatch\"}[5m])))",
          "legendFormat": "{{service}} {{scope}} {{completion_boundary}} {{channel_type}}",
          "refId": "A"
        }
      ],
      "id": 4
    }
  ]
}
