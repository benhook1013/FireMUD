# FireMUD System Architecture: Authorization Route Matrix

The machine-readable [authorization route matrix](./system-architecture-authz-route-matrix.yaml) is normative for the route entries it contains. It is not yet a complete route inventory: source-stable OpenAPI/protobuf coverage and its comparison validation are incomplete. This document is the human-readable companion and must not define a competing policy.

Every protected route in a validated inventory must be listed here with:

- route identifier (service + method/path),
- classification (`public`, `account_scoped`, `caller_membership_scoped`, `player_bootstrap_tenant`, `pre_tenant_discovery`, `public_production_onboarding`, `gameplay_admission`, `tenant_regular`, `billing_safe_tenant`, `cross_tenant_support_safe`, `cross_tenant_billing_safe`, `cross_tenant_data_bearing`, `internal_workload`, `pending_deletion_scoped`),
- whether a matching Account issued-token registry record is required,
- required role checks,
- tenant-billing authority-generation applicability,
- caller-bound membership authority-generation applicability where relevant,
- required live authority checks for the route class,
- any response-profile or mutation-contract requirements needed for CI/security enforcement,
- any role-assurance or bounded privileged-elevation contract required before a global role may authorize the route.

**Canonical incomplete-inventory rule:** Until source-stable OpenAPI/protobuf inventory coverage is complete and validated, the YAML is declaration-only and must not generate runtime or default-deny policy for routes absent from the validated inventory. Runtime must reject unclassified protected routes and leave unclassified external routes unreachable or denied; CI and deployment inventory checks must fail validated candidate routes missing from the YAML.

Target state: once source-stable OpenAPI/protobuf inventory coverage is complete and validated, services will enforce these classifications through shared middleware annotations/interceptors. Until that gate passes, a protected route missing from the current matrix is recorded as authorization drift/gap; the canonical incomplete-inventory rule above governs its runtime and CI/deployment handling rather than forwarding or approximating it.

## Implementation Status

- Public-production onboarding remains incomplete: current `PLAY` returns `JOIN_REQUIRED` only when fresh authority confirms a publicly visible target whose public-production join policy allows entry and the caller's membership is absent; a policy denial remains `PUBLIC_PRODUCTION_ADMISSION_DENIED`, and unavailable or invalid authority uses the applicable unavailable/invalid-authority outcome. Non-public missing membership remains `WORLD_ACCESS_DENIED`; current connect-token issuance requires existing membership and never creates it. The complete target still requires explicit `JOIN` before character creation, connect-token issuance, or `PLAY`.
- The current `IssueConnectToken` path requires existing caller-bound membership regardless of realm visibility or realm grant; the remaining drift is that it does not reread membership authority generation after bootstrap/discovery validation. A realm grant never substitutes for membership.
- `privileged_control` is target-only and currently unavailable; route branches that authorize through a global `platformAdmin` or cross-tenant `billingAdmin` role therefore fail closed, while the `support` branch of `cross_tenant_support_safe` does not require `privileged_control`. Operator mutation rows that require an Account-issued authorization reference or this privileged-control window are normative target rows, not current availability claims.
- The `platformAdmin` privileged-control predicate applies to the exact internal-workload `account-service/IssueHumanOperatorAuthorizationReference` issuance route in addition to its declared route-class allowlist. This exact route identity does not add `internal_workload` to the support or billing-safe classification scopes; the separate automation issuance path prohibits privileged control.
- `LinkExternalAccount` is currently routable as a tenant-scoped caller-asserted scaffold with known drift, while the target is an account-subject-bound route with recent reauthentication and verified provider authorization.
- The current Account implementation of `ExportTenantData` remains account-targeted while the canonical target is a tenant-wide, tenant-owned export; this is recorded as known drift while the target route remains a distinct `billing_safe_tenant` exception.
- Logging & Admin feature-flag, admission-pointer/version-upgrade, tick, and moderation mutation paths are implemented or declared but remain externally disabled and nonconformant until each family has a published action schema, shared cross-language `mutationDigest/v1` golden vectors, and the Account-issued authorization-reference issuance/redemption flow. Their rows below describe the target authorization boundary, not current external support. Routes that use global-role branches additionally depend on the target-only `privileged_control` issuance path. Live admission-pointer reads/audit/prepared-upgrade proof reads and `EvaluateModerationPolicy` are separate read contracts and remain available.
- The operator mutation support gate is identity-complete even while the route inventory is incomplete: every exact identity in `operator_mutation_support_gate.applies_to` or `live_exceptions` must either match a YAML route entry or have a nested `coverage_drift` record with a reason. The Game Session HTTP create, stop, and restart mutations are `target_not_currently_routable` under this gate even though current owner-local OpenAPI scaffolds still exist. The owner-local workload-authenticated `refresh-roles` route is not authorization-reference-redeeming and remains outside the gate. The validator rejects unclassified gate identities and stale drift records.
- Public report submission (`POST /reports`) is target-only and not currently routable; the target player-bootstrap route must derive `tenantId` and `reporterAccountId` from the authenticated caller and live membership before it becomes routable.
- Game Session still exposes current owner-local `/sessions*` REST scaffolds. Direct owner edge exposure is denied and the canonical target external ingress for operator create, stop, and restart is Logging & Admin with Account authorization-reference forwarding, but Logging & Admin does not yet have a complete source-stable `/sessions*` route family. The workload-authenticated `refresh-roles` owner route is a separate non-redeeming operation. This remains explicit coverage drift; no external route is invented here.
- The static Gateway route catalog and bounded internal/actuator blockers provide partial edge-exposure enforcement.
- The YAML is normative for declared entries, but the current route inventory is incomplete. CI inventory generation, source-stable OpenAPI/protobuf coverage, YAML completeness comparison, matrix-aware shared middleware, strict token-profile enforcement, and exact proof for remaining broad Gateway route families are not implemented. Missing coverage is a recorded drift/gap; the canonical incomplete-inventory rule above remains the immediate runtime and CI/deployment safeguard.

## Token Profile Vocabulary

The current JWT profile names are `control-ui`, `player-bootstrap`, the one-use `gameplay-connect` handshake token, and receiver-specific private player-delegation profiles. `gameplay-connect` uses audience `gameplay-connect` and is consumed by Gateway rather than accepted as gameplay command authorization. Ordinary JWT registry middleware explicitly excludes `gameplay-connect`; this profile uses its dedicated Account issuance/replay record, signed `replayAdmissionFence`, and Gateway `jti` consume marker. The current private delegation profile is `game-session-account-delegation` with audience `account-service`. A generic backend JWT profile and the `internal` audience are forbidden. A privileged-control window is an authorization condition, not a JWT profile.

### Operator Idempotency and Receiving Boundaries

For a forwarded owner mutation, `controlPlaneRequestId` locates the candidate owner record but is not the complete idempotency proof. The owner compares the full authority-bound idempotency tuple: normalized action family, schema identifier/version, typed scope, target, expected version, mutation, audit reason, every schema-bound presence/absence value, the applicable Account authority tuple, independent membership version, issuance fence, immutable authority evidence, and the exact `mutationDigest` integrity value. `mutationDigest` is the canonical digest of the typed request fields; it is a tuple member and integrity check, not a replacement for the tuple. Any mismatch returns `IDEMPOTENCY_CONFLICT` without mutation, while an exact tuple replays the stored owner result.

The moderation policy-input/audit path is the sole receiving-boundary exception. `/moderation/actions` and `ApplyModerationAction` have no forwarded domain owner: Logging & Admin validates and redeems the Account authorization reference at its own receiving boundary before persistence. They do not require domain-owner redemption or enforcement; Game Session and Social & Groups separately consume the live `EvaluateModerationPolicy` read at their enforcement boundaries.

## Governance (Required)

- **Owner**: Platform Security + Account Service maintainers jointly own this matrix.
- **Machine-readable source**: `design/architecture/system-architecture-authz-route-matrix.yaml` is the normative source for declared entries; this Markdown file is the human-readable companion. It is not a complete route registry until the inventory gate below passes.
- **CI enforcement**:
  - Once source-stable OpenAPI/protobuf inventories cover the governed surfaces and validate in the same run, CI and deployment policy checks independently fail if a protected route is not present in the YAML matrix.
  - Fail if a route uses an unknown classification value.
  - Fail if a route is marked billing- or support-safe but lacks required redaction/authorization tests.
  - Once the source-stable inventory gate above passes, fail if the same-run generated route inventory (OpenAPI/proto) differs from the YAML matrix for auth/session and billing/subscription domains. Before that gate passes, record the difference as drift without treating the incomplete inventory as an enforceable registry.
- **Governance vocabulary**:
  - `route_status` is optional; when present, it must use one of the closed values declared by the YAML `route_status_vocabulary`: `current_openapi_operator_surface` for a route currently present and supported under its declared policy, including non-operator routes, or `target_not_currently_routable` for target-only coverage and present scaffolds whose supported path remains gated. The historical `current_openapi_operator_surface` value name does not limit current coverage to operator routes. An omitted value does not imply current or target routability.
  - `route_status_override` uses the closed `route_status_override_vocabulary`; `gate_wins_for_applies_to` gives `operator_mutation_support_gate.status` precedence for every matching `applies_to` route identity, so each such route must declare the same `route_status`. This precedence does not classify `live_exceptions` or identities recorded only in `coverage_drift`.
  - Authority-generation applicability uses the explicit `*_authority_generation_applies` fields, including `issuer_authority_generation_applies`, `account_authority_generation_applies`, `tenant_billing_authority_generation_applies`, and `membership_authority_generation_applies`. Pending-deletion routes must set issuer, account, tenant-billing, and membership authority-generation applicability explicitly to `false`; the non-registry-backed gameplay-connect exception explicitly sets issuer and account applicability to `false`.
  - Class-level `required_authority` is metadata describing authority types that may be required or supplied conditionally by a route or token. It is not an exact per-entry `required_live_checks` list. Route-level checks and explicit branch validators remain authoritative; public-production onboarding, account-scoped subject branches, and pending-deletion credentials retain their dedicated route-specific exceptions.
- **Default-deny behavior**:
  - The declared-entry `default_action: deny` is normative only for entries in the YAML; the canonical incomplete-inventory rule above governs routes absent from the validated inventory.
  - Runtime classification and CI/deployment inventory registration are separate gates; both follow the canonical incomplete-inventory rule above.
  - No route may default to `tenant_regular`, billing-safe, support-safe, or another executable class.
- **Change control**:
  - `billing_safe_tenant`, `cross_tenant_support_safe`, and `cross_tenant_billing_safe` changes require explicit security review approval.

### Critical Domains And Inventory Gate

The following domains become full-fail in CI only after source-stable OpenAPI/protobuf inventories cover them and the same run validates the comparison:

- Authentication/session admission routes (`LOGIN`/`PLAY` surfaces and equivalents).
- Billing-safe and support-safe routes.
- Subscription mutation and entitlement routes.

Before that gate passes, protected routes missing from the YAML matrix are recorded as drift/gap, including pre-existing routes; the canonical incomplete-inventory rule above applies until the validated candidate inventory is compared.

CI should generate candidate inventories from OpenAPI/proto definitions and compare them against the YAML matrix so protected-route drift is detected automatically.

Critical-domain inventory artifacts (required):

- CI must persist generated candidate inventories for auth/session and billing/support domains (OpenAPI + proto derived) under version control (for example `design/architecture/authz-inventory/*.json`).
- Full-fail assertions and generated default-deny policy in critical domains are valid only when these generated inventories are present, source-stable, and compared against the YAML matrix in the same run.
- Inventory generation must distinguish tenant-scoped and cross-tenant route variants explicitly; mixed-scope APIs must not be represented as a single ambiguous route key.

## Classification Rules

Strict issued-token-registry and applicable authority-generation middleware applies to protected control-plane operations and JWT-presenting bootstrap/admission operations. It does not blanket the dedicated `gameplay-connect` handshake: Gateway uses its one-use replay fence, quarantine cutoff, deny marker, and atomic exact-`jti` consume instead. Non-JWT in-band `LOGIN`, `PLAY`, fresh admission, reconnect, and resume use their exact bound-session, binding-fence, and current-authority contracts. Routine commands within an already-admitted gameplay binding use the validated bound context and bounded reconciliation instead of repeating registry or generation lookups; the binding remains valid only within its existing bounded lease and invalid or conflicting evidence still invalidates it.

| Classification | Required issued-token state | Tenant/Membership authority generations applied? | Notes |
| --- | --- | --- | --- |
| `public` | none | No | No JWT required |
| `account_scoped` | One matching token record for the exact profile declared by the route | No | Account-level control-plane routes with subject binding (`accountId == caller`), plus explicit route-level admin overrides. A `platformAdmin` exception is never inherited; it is present only on an entry that declares `platform_admin_override: platformAdmin_only`. Subject-bound `ExportAccount` and `DeleteAccount` entries declare exactly two mutually exclusive `account_authorization_branches`: `self_service` binds the target to the caller and requires current account authority, while `platformAdmin_override` permits an explicit target account only with the live global role and role-appropriate assurance. |
| `caller_membership_scoped` | One matching token record for the exact profile declared by the route | No tenant authority generation; membership authority generation: Yes | Caller-bound lifecycle operations on the caller's own account-to-tenant membership. Any current membership role may act on itself; the route must perform a live subject-bound membership check and must not accept an arbitrary account target or global-role override |
| `player_bootstrap_tenant` | One matching `player-bootstrap` token record | No tenant authority generation by default; selected-tenant authority is a route-specific exception and must be declared explicitly | Player-bootstrap-authenticated routes targeting a tenant before gameplay socket auth is complete. `IssueConnectToken` requires the current selected-tenant authority generation, membership authority generation, and live membership, entitlement, and admission-pointer checks |
| `pre_tenant_discovery` | In-band gameplay commands may use the already-authenticated Game Session context without presenting a JWT; other routes require the exact token profile they declare | No | Authenticated discovery surfaces that run before a single `tenantId` is selected (for example `WORLDS`). A route using `game_session_authenticated_context` therefore declares `accepted_token_profiles: []`; this does not make the command anonymous. |
| `public_production_onboarding` | No JWT for in-band gameplay commands; otherwise the exact route-declared profile (`player-bootstrap` for Account bootstrap writes) | No tenant authority generation before join; membership authority generation applies after join | **Target:** discovery and explicit open-enrollment join for the default public production realm. Brand-new authenticated accounts may discover it before membership exists, but `JOIN`/`Join & Play` creates the durable Account-owned membership before character creation, connect-token issuance, or `PLAY`. `JOIN_REQUIRED` is valid only when fresh public-production authority confirms target visibility and an allowed join policy; policy denial is `PUBLIC_PRODUCTION_ADMISSION_DENIED`, and unavailable or invalid authority remains the applicable unavailable/invalid-authority outcome. The Gateway WebSocket's one-use `gameplay-connect` profile belongs to `gameplay_admission`, not onboarding. Grant-backed private/playtest realms require existing caller-bound membership plus the current membership authority generation and applicable Account-owned realm grant, skip `JOIN`, and never use this class to create membership. |
| `gameplay_admission` | No JWT for in-band `PLAY` or the trusted TCP Proxy bridge; the first-party WebSocket edge presents the one-use `gameplay-connect` profile | Mode-selected and route-specific | Post-transport gameplay admission. Game Session selects exactly one of `public_production_onboarding`, `returning_membership`, or `grant_backed_private_or_playtest` from current Account-owned membership, public-production target, and realm-grant evidence. Missing, stale, contradictory, or multiply applicable selector evidence denies admission. The selected branch supplies its exact membership, generation, grant, entitlement, routing, and binding checks; membership generation is a common route-level check, while membership existence and conditional grant checks remain branch-specific. A grant never replaces caller-bound membership. |
| `tenant_regular` | One matching token record for the exact profile declared by the route | Tenant authority generation: Yes; membership authority generation: Yes | Gameplay-affecting and regular tenant control-plane operations. Every YAML entry in this class explicitly declares both applicability fields; tenant-role callers require live membership while an allowed global-role branch uses its declared target-tenant binding. |
| `billing_safe_tenant` | One matching token record for the exact profile declared by the route | Tenant authority generation: No; Account-owned caller-bound membership authority generation: Yes | Must remain reachable during `grace`, `suspended`, or `canceled` through the live caller-bound `tenantAdmin` membership authority, but must fail immediately after membership/role revocation |
| `cross_tenant_support_safe` | One matching token record for the exact profile declared by the route | No tenant or membership generation; Account-owned issuer/account authority and live global role still apply | High-level troubleshooting only; the `support` branch uses the current Account-owned global role without `privileged_control`, while a `platformAdmin` branch requires `privileged_control` and currently fails closed |
| `cross_tenant_billing_safe` | One matching token record for the exact profile declared by the route | No tenant or membership generation; Account-owned issuer/account authority and live global role still apply | Billing operations for global roles; the surviving authority is the current Account-owned global `billingAdmin` role and global token scope, plus the required `privileged_control` window for cross-tenant use |
| `cross_tenant_data_bearing` | One matching token record for the exact profile declared by the route | Yes when operation targets tenant-scoped data | Platform-admin-only data-bearing operations |
| `internal_workload` | Route-specific: explicitly `none` or one exact delegated profile | Route-specific | Internal-only RPCs require exact mTLS workload identity and a method caller allowlist, and both constraints must pass. Each entry declares whether it carries delegated subject authority; this class never inherits an end-user token requirement implicitly. |
| `pending_deletion_scoped` | One Account-issued opaque `pending-deletion-access` credential whose hash resolves to `state=ACTIVE` in the separate pending-deletion registry | None; no issuer, account, tenant, or membership generation applies | The validator must bind the credential to the caller account, `deletionWorkflowId`, `deletionWorkflowGeneration`, and route `actionFamily`, then verify expiry and live workflow state. It is limited to deletion status/cancel, full-subject export, and necessary billing-settlement routes; normal JWT/session/gameplay authority is never a fallback. |

`pending_deletion_scoped` is a separate workflow-credential path, not a JWT exception. A declared route in this class must use the exact Account workflow-registry lookup and reject a missing, expired, revoked, terminal, wrong-account, wrong-workflow, wrong-generation, wrong-action-family, or otherwise malformed credential as `AUTH_SESSION_REVOKED`; an unavailable credential registry or deletion-workflow authority is `AUTH_UNAVAILABLE`. Login, bootstrap, connect-token issuance, tenant, purchase, and gameplay routes are outside this class. The dedicated public recovery routes may issue only another `pending-deletion-access` credential and must not upgrade it into normal authority; see the [pending-deletion JWT/access contract](./system-architecture-jwt-and-token-contracts.md#pending-deletion-access-credential).

### Route-Class Branches

The route-class defaults below are branch-specific. `omitted` is intentional: it means that authority is not an input to that branch, not that a caller may use cached or inferred evidence. `required` names the corresponding live generation check; `target_tenant_generation` is the global-role target fence and is not caller membership.

| Classification | Branch | Scope | Role | Issuer/account generation | Tenant generation | Membership generation | `privileged_control` |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `account_scoped` | `platformAdmin` global | `account` | `platformAdmin` | required | omitted | omitted | required |
| `tenant_regular` | tenant role | `tenant` | Route-declared tenant role | required | `tenant_generation` required | required | not required |
| `tenant_regular` | `platformAdmin` global | `tenant` | `platformAdmin` | required | `target_tenant_generation` required | omitted | required |
| `cross_tenant_data_bearing` | `platformAdmin` global | `cross_tenant` | `platformAdmin` | required | `target_tenant_generation` required | omitted | required |
| `billing_safe_tenant` | `tenantAdmin` | `tenant` | `tenantAdmin` | required | omitted | required | not required |
| `cross_tenant_support_safe` | `support` global | `cross_tenant` | `support` | required | omitted | omitted | not required |
| `cross_tenant_support_safe` | `platformAdmin` global | `cross_tenant` | `platformAdmin` | required | omitted | omitted | required |
| `cross_tenant_billing_safe` | `billingAdmin` global | `cross_tenant` | `billingAdmin` | required | omitted | omitted | required |
| `cross_tenant_billing_safe` | `platformAdmin` global | `cross_tenant` | `platformAdmin` | required | omitted | omitted | required |

The `tenant_regular` tenant-role row represents the route-declared tenant roles rather than adding a role to every route; its operator variants are the concrete `tenantAdmin`/`moderator` entries in the YAML. `cross_tenant_data_bearing` is a target class with no currently declared executable route. Exact token profile/registry validation supplies the issuer and account authority for tenant-regular routes; the branch table does not authorize a new route or permit a route to omit its declared role checks.

`tenant_regular` branch selection is deterministic and has no tenant-role fallback after a global-role result. The evaluator first obtains fresh, well-formed current global-role evidence. If `platformAdmin_global` is present, only the explicitly route-allowlisted global branch is eligible; a route without that override, or a failed global-branch predicate, is denied even when a tenant role is also present. Only a conclusive `platformAdmin_global`-absent result permits evaluation of the tenant-role branch. Unavailable, stale, malformed, mismatched, or inconclusive global-role evidence denies the request without evaluating either branch.

### Gameplay Admission Applicability

The gameplay transport and the admission reason are separate dimensions. `gameplay-connect` is the one-use edge credential for the first-party WebSocket path; the trusted TCP Proxy bridge is an mTLS transport that does not consume that credential; and `PLAY` is the in-band Game Session admission decision after transport authentication. None of these transports creates membership or converts a selected UI target into authority.

| Admission mode | `gameplay-connect` | Trusted TCP Proxy bridge | `PLAY` and downstream admission |
| --- | --- | --- | --- |
| `public_production_onboarding` | Not applicable before explicit `JOIN`/`Join & Play`; after Account commits membership, the ordinary connect-token checks apply | May carry the post-join gameplay connection on its trusted mTLS listener; it does not authorize `JOIN` or replace Game Session membership checks | Requires the newly committed caller-bound membership, membership generation, `public_production_admission`, entitlements, pointer, routing, and binding checks; it never creates membership |
| `returning_membership` | Applies to an existing caller-bound member after discovery and current membership-generation validation | May carry the same returning-member admission when the trusted listener is used; transport trust does not replace membership or authority checks | Requires the existing caller-bound membership and current authority/entitlement/routing checks; no onboarding join is performed |
| `grant_backed_private_or_playtest` | Applies only after the applicable private/playtest grant, existing caller-bound membership, and current membership authority generation are validated | May carry the same grant-backed admission when the trusted listener is used; transport trust does not replace the required membership, membership-generation, or grant checks | Requires the applicable grant, existing caller-bound membership, current membership authority generation, current authority/entitlement/routing checks, and final binding checks. The grant never substitutes for membership; no grant-backed admission creates membership or uses the public onboarding `JOIN` path |

Route entries and client flows must state which admission mode they support. A route or transport being reachable does not make all three modes applicable, and a `gameplay-connect` token, proxy identity, or `PLAY` command is not by itself proof of the mode-specific membership or grant predicate.

`pending_deletion_scoped` has no target tenant and is therefore declared under `tenant_generation_policy.no_target_tenant_classifications`, not the closed tenant-target exception allowlist. It has a separate validator and rejection contract: Account validates the opaque `pending-deletion-access` credential against its server-side registry, binds the credential's `accountId` and deletion-workflow identity to the route target, and checks live pending-deletion state before allowing the declared action. Invalid, expired, revoked, binding-mismatched, or wrong-state credential/workflow evidence returns the established `AUTH_SESSION_REVOKED` outcome; unavailable credential registry or deletion-workflow authority returns retryable `AUTH_UNAVAILABLE`; neither outcome discloses account state. Cancellation revokes pending access before restoring normal authority generation; export and necessary billing settlement remain restricted to their declared response/action profiles. These routes never fall back to cached JWT, normal session allowlist, or gameplay context.

Any route that accepts more than one token profile must declare an `accepted_token_profile_audiences` YAML map that binds each accepted profile to its exact audience rather than using an implicit shared audience. When the accepted profiles do not share the same registered type and issuer, the route must additionally declare exact `accepted_token_profile_types` and `accepted_token_profile_issuers` maps; scalar predicates or implicit profile inference are not allowed. Each mapping must cover exactly the accepted profiles and must match the token-profile vocabulary.

The `account_generation` and `current_account_generation` live checks are deliberately distinct. `account_generation` is the captured Account authority generation carried in a private delegation tuple and compared by its receiving owner; `current_account_generation` is a live Account reread used while issuing or validating control-plane authority. They must not be consolidated unless those two evaluation points become the same contract. The `platformAdmin_override` branch of account-scoped subject-bound routes also retains the captured `issuer_generation` and `account_generation` fences; role and assurance checks do not replace those fences.

The `tenant_generation` and `target_tenant_generation` live checks are also deliberately distinct. `tenant_generation` is the caller-bound tenant generation captured by a tenant-scoped token or private delegation and compared for that caller's tenant authority. `target_tenant_generation` is the separately redeemed target-tenant freshness fence used by an allowed global-role operator branch that has no target membership. A route with both authorization branches declares the check required by each branch rather than treating the two names as interchangeable.

Internal-service routes must additionally declare their **service caller policy** in the machine-readable matrix:

- whether the route is callable only by specific service identities,
- whether an end-user issued-token record and scope authorization are still evaluated on behalf of a delegated subject, and
- which token profile/audience the caller must present.

Each `caller_policies` entry is one complete alternative. Alternatives are disjunctive, while the `caller` method identity, `mtls_identity` certificate identity, token state/profile, applicable authority generations, delegated context, and live checks inside one entry are conjunctive. A caller name is not sufficient without its exact certificate identity, and an mTLS identity is not sufficient without the method caller policy. When alternatives use different token states, generation-applicability fields belong inside each caller policy rather than at route level. The YAML `caller_policies_schema` makes these two identity fields and their conjunctive relation explicit without introducing a broader policy framework.

Without these fields, a route classification is incomplete for internal-only APIs.

Critical routes may also require explicit machine-readable fields for:

- `applicability` when multiple entries share one transport route or command; predicates must be explicit and mutually exclusive so classification is deterministic
- `membership_authority_generation_applies`, which may be `true`, `false`, or `conditional_by_operator_role`; the operator conditional form must declare `membership_authority_generation_condition.tenant_role: true` and `membership_authority_generation_condition.platformAdmin_global: false`. Gameplay admission declares its mode-specific membership-generation checks in `admission_mode_selection` rather than using a legacy conditional applicability value.
- `account_authority_generation_applies`, which must be a boolean when declared and is explicitly `false` for `pending_deletion_scoped` routes
- `tenant_billing_authority_generation_applies`
- `required_live_checks`, whose values must come from the closed YAML `required_live_check_vocabulary`, such as `membership`, `conditional_realm_access_grant`, `public_production_admission`, `membership_generation`, `runtime_entitlements`, `admission_pointer`, `connect_token_single_use_consume`, `replay_protection_available`, `replay_admission_fence_match`, and `connect_scope_match`. For conditional operator routes this top-level list contains only checks common to every authorization branch; `operator_authorization_branches` contains the exact mutually exclusive tenant-role and global-platform-admin checks, and the evaluator applies the common checks plus every check in the one matching branch. `ExportAccount` and `DeleteAccount` use the same common-versus-branch rule with `account_authorization_branches`; `current_account_generation` belongs only to `self_service`, while `issuer_generation`, `account_generation`, `current_global_role`, and `role_appropriate_assurance` belong to `platformAdmin_override`. Gameplay `admission_mode_selection` uses the same explicit composition: its common `required_live_checks` are combined with only the selected mode branch, so returning-membership and grant-backed branches do not inherit `public_production_admission` or unconditional membership checks.
- The validator caches structural parsing for repeated `required_fields` reads by source identity and field name. It must parse and cache that result before consumers use it, and cache hits must reuse the validated result without re-emitting structural errors, matching the equivalent live-check cache contract.
- `mutation_contract` for payment-instrument flows must require billing-owner authorization, explicit per-subscription binding, replacement-before-detach, and audited billing-ownership transfer; a shared-instrument acknowledgement is not an authorization substitute. The target-only `PaymentInstrumentWalletCrossTenant` entry must additionally bind `target_account_id`, `affected_subscription_ids`, `new_billing_owner_account_id`, replacement instrument bindings, expected subscription row versions, effective boundary, reason, and `control_plane_request_id`, and require issuer/account generation, current global role, privileged assurance, wallet-to-provider-customer mapping, explicit bindings, safe detachment, and a clear audited ownership-transfer state. When the owning payment-instrument route is not yet routable, the entry must mark this as target-only rather than claiming an implemented path
- `canonical_errors` that CI and contract tests must expect for route-specific security rejections
- `realm_grant_authority` and `realm_grant_version` when grant-gated access depends on Account-owned realm authority
- `canonical_target` when the entry records the converged target contract alongside known implementation drift
- `implementation_status` when the current implementation is known not to satisfy that target

For first-party gameplay, `connection_mode: first_party_web` names the verified public non-proxy `gameplay-connect` path, not a browser-only client class. It covers every currently supported first-party protected-cookie client, including browser/mobile-browser clients and native or other non-browser Account clients using a protected cookie jar. It does not imply support for the target-only public non-browser header carrier. The `/ws/game/**` route declares `operation: websocket_upgrade`, while `POST /ws/game/connect-token/revoke` declares `operation: connect_token_cookie_revoke`; these applicability predicates remain mutually exclusive even though both use that connection mode.

Without these fields where applicable, a route entry is incomplete for governance and CI enforcement.

### Privileged Role Assurance

Global-role possession is not sufficient to enter a privileged control window. `EnterPrivilegedControlWindow` uses the separate `control_ui_plus_recent_reauthentication_and_independent_totp` auth path and its machine-readable `privileged_control: establishes_window` bootstrap exemption, so the route that creates elevation does not require an elevation window that does not yet exist. `platformAdmin` authority always requires the machine-readable `privileged_control` elevation contract; `billingAdmin` requires it when exercising cross-tenant authority. Support-safe `support` access requires a live support global-role check but no `privileged_control` window and is limited to `cross_tenant_support_safe` routes. Account Service creates the bounded server-side role-scoped state only after recent ordinary reauthentication and independent TOTP. The state is bound to the account, current `control-ui` token `jti`, account generation, requested global role, issue time, and expiry, and is invalidated by token revocation, generation change, role loss, or expiry. It is not a JWT profile or gameplay authority.

Route authorization never becomes in-game elevation. If a global-role account passes the ordinary caller-bound join and admission flow, gameplay presence, command, and actor-capability resolution ignore its global roles. Any moderator, administrator, game-master, or equivalent gameplay authority requires an explicit tenant-scoped gameplay grant; no route classification creates a support impersonation or hidden-observer session.

### Operator Delegation

Account Service is the operator-delegation authority. Human issuance enters through the exact allowlisted workload mTLS identity plus the forwarded `control-ui` token, then validates that token's `jti`, account generation, current tenant or global role, tenant/scope, action family, canonical mutation digest, and any role-required `privileged_control` window. Tenant-scoped `tenantAdmin` and `moderator` actions require live membership and role but not a fabricated global elevation. A global `platformAdmin` tenant operation instead binds the current target-tenant generation without inventing membership. Unattended issuance validates exact allowlisted mTLS workload identity plus a current versioned automation policy and the same mutation digest. The resulting opaque reference is bound either to the human token/account/role evidence or to the workload/policy evidence; automation never impersonates a user.

Logging and Admin records the operator intent and actor kind, then forwards the same reference unchanged with the exact typed action-family request, action-family schema identifier/version, same `control_plane_request_id`, canonical `mutationDigest/v1` digest, and every scope, target, expected-version, mutation, and audit-reason field declared by that schema. Only transport credentials and the authorization reference are excluded from the digest. The owner call is `internal_workload`: exact mTLS caller identity plus Account redemption/validation of the exact reference against that action family, schema pair, scope, request identity, and digest, followed by owner recomputation of the digest from every declared input and owner-side domain facts, fencing, and idempotency checks. Logging and Admin role, actor, or automation-policy assertions alone are never authority, and owner-side admission RPCs do not accept an end-user JWT.

The target operator route inventory includes feature-flag toggle, gated moderation action, admission pointer and version-upgrade writes, tick pause/resume, and the adjacent Game Session session lifecycle mutation surfaces. These rows are target authorization contracts; current external availability remains gated by action schemas, shared `mutationDigest/v1` vectors, Account authorization-reference issuance/redemption, and any target-only `privileged_control` window. Missing quota-override and versioned/broader moderation-propagation contracts remain recorded in the machine-readable coverage-drift inventory; no route is invented for either. Current runtime enforcement instead consumes Logging & Admin's synchronous policy evaluation at `GAMEPLAY_ADMISSION` and `CHAT_SEND`.

## Seed Matrix (Current Required Entries)

For every first public-production text entry, the target command sequence is `LOGIN` -> conditional `JOIN` -> `PLAY`; returning members skip `JOIN`.

| Service | Route | Classification | Required roles/capability |
| --- | --- | --- | --- |
| Game Session Service | `LOGIN` / `LOGON` | `public` | Credential entrypoint only; no JWT required |
| Game Session Service | `WORLDS` anonymous browse | `public` | Public-production catalog only; no account-specific membership, grant, or character information |
| Game Session Service | `WORLDS` authenticated discovery | `pre_tenant_discovery` | No pre-existing tenant role is required. Tenant visibility is derived server-side from membership or public-production visibility plus entitlement state; global roles do not widen gameplay discovery |
| Game Session Service | `REALMS` | `public_production_onboarding` | Selects exactly one branch from the authoritative realm mode: public production permits pre-membership discovery; private/playtest requires caller-bound `membershipLifecycleState=ACTIVE`, current membership authority generation/version, and the current Account-owned grant for the exact target. It never falls back from private/playtest to public discovery or uses a grant to repair membership. Hidden/unauthorized targets are omitted; explicit branch failures use `NON_PUBLIC_ENROLLMENT_REQUIRED`, `REALM_ACCESS_DENIED`, `WORLD_ACCESS_DENIED`, `ADMISSION_POINTER_UNAVAILABLE`, `REALM_UNAVAILABLE`, `ENTITLEMENT_UNAVAILABLE`, or `AUTH_UNAVAILABLE` as applicable. |
| Game Session Service | `JOIN` | `public_production_onboarding` | Explicit caller-bound open-enrollment action for the current public production realm; Account commits durable membership plus audit/outbox idempotently |
| Game Session Service | `CHARS` | `public_production_onboarding` | Requires an existing caller-bound `ACTIVE` membership plus any non-public realm grant and current entitlements. It applies the current `admission_pointer` check to the selected catalog realm, but does not apply `PLAY`'s exact-binding admission lease, binding fence, or binding-ownership gate; character discovery and creation do not require an existing character. Missing or `INACTIVE` public-game membership returns `JOIN_REQUIRED` only when fresh authority confirms a publicly visible target with an allowed public-production join policy; policy denial remains `PUBLIC_PRODUCTION_ADMISSION_DENIED`, and unavailable or invalid authority uses the applicable unavailable/invalid-authority outcome. Missing or non-`ACTIVE` private/playtest membership returns `NON_PUBLIC_ENROLLMENT_REQUIRED`, and character discovery is not membership repair. |
| Game Session Service | `PLAY` | `gameplay_admission` | **Target:** requires an existing caller-bound membership plus any non-public realm grant, current entitlements, and the current admissible realm pointer; `PUBLIC_PRODUCTION_ADMISSION_DENIED` takes precedence when `public_production_admission` or selected-target admission fails, and only an admissible, publicly visible public-production target whose joining policy permits entry and whose membership is missing returns `JOIN_REQUIRED`; `PLAY` never creates membership. First-party `/ws/game/**` also enforces connect-context scope, and unavailable/ambiguous routing returns `ADMISSION_POINTER_UNAVAILABLE`; a deliberately closed realm returns `REALM_UNAVAILABLE`. |
| Spring Cloud Gateway | `/ws/game/**` (connect-token bootstrap) | `gameplay_admission` | This is one canonical WebSocket entry point with mutually exclusive variants. The current `first_party_web` variant accepts exactly the protected cookie for first-party protected-cookie clients, including native or other nonbrowser clients using a cookie jar. A future explicitly classified `non_first_party_public` variant may use `X-Firemud-Connect-Token` on this same path only after its dedicated matrix entry and issuance, replay, signed-context, response, and carrier proof exist; until then the header is rejected fail closed as `CONNECT_TOKEN_REJECTED` and cannot select or fall back to `first_party_web`. Gateway consumes exactly one supported carrier, strips it, atomically enforces single use using the dedicated replay record/fence, matches the signed connect scope to the request, and emits the signed connect context for Game Session. Non-`101` outcomes use the bounded `CONNECT_TOKEN_MISSING`, `CONNECT_TOKEN_EXPIRED`, `CONNECT_TOKEN_REPLAYED`, `CONNECT_SCOPE_MISMATCH`, `CONNECT_REPLAY_PROTECTION_UNAVAILABLE`, `CONNECT_TOKEN_REJECTED`, `POLICY_DENY`, `POLICY_PRESSURE`, `BACKEND_UNAVAILABLE`, `PROTOCOL_MISMATCH`, or `INTERNAL_ERROR` handshake classes. |
| Spring Cloud Gateway | `/ws/game/**` (trusted TCP Proxy bridge) | `gameplay_admission` | Applies only on the internal mTLS listener when the exact TCP Proxy workload identity is authenticated. Gateway canonicalizes trusted proxy metadata and emits `X-Firemud-Connection-Mode: trusted_tcp_proxy`; this path does not accept or consume a gameplay connect token, and public clients cannot select it. |
| Spring Cloud Gateway | `POST /ws/game/connect-token/revoke` | `public_production_onboarding` | **Target; not currently implemented.** Its applicability is `connection_mode: first_party_web` plus `operation: connect_token_cookie_revoke`, mutually exclusive with the websocket-upgrade route. First-party browser logout reads only the HttpOnly `Firemud-Connect-Token` cookie, requires the exact configured Origin and anti-CSRF proof, records a bounded deny marker when a token is present, clears the cookie, and is idempotently successful when it is absent. It never accepts a query, body, or caller-readable token. |
| Account Service | `GET /auth/bootstrap/worlds` | `pre_tenant_discovery` | Accepts only the caller-bound `player-bootstrap` profile; derives visible worlds before a tenant is selected and applies the membership/public-production visibility contract plus live runtime entitlement filtering |
| Account Service | `GET /auth/bootstrap/worlds/{worldSlug}/realms` | `public_production_onboarding` | Accepts only the caller-bound `player-bootstrap` profile; applies live realm visibility and runtime entitlement checks, including public-production visibility or an applicable Account-owned realm-access grant |
| Account Service | `GET /auth/bootstrap/worlds/{worldSlug}/realms/{realmSlug}/characters` | `public_production_onboarding` | Accepts only the caller-bound `player-bootstrap` profile; its target binding and `connect_scope_match` check require the opaque server-issued `connectScopeId` to match the selected route target, alongside live membership, applicable realm-grant, entitlement, and admission-pointer checks |
| Account Service | `POST /auth/bootstrap/worlds/{worldSlug}/realms/{realmSlug}/characters` | `public_production_onboarding` | Bootstrap-authenticated Account facade derives `accountId` from the authenticated caller subject, and its target binding plus `connect_scope_match` check require a matching opaque server-issued discovery `connectScopeId`; existing caller-bound membership, applicable realm visibility/grant, runtime entitlement, and admission-pointer checks precede delegation to internal Entity Management `CreateCharacter` |
| Account Service | `POST /auth/bootstrap/join` | `public_production_onboarding` | **Target; not currently implemented.** Accepts only the caller-bound `player-bootstrap` profile, derives `accountId` from the authenticated subject, and its target binding plus `connect_scope_match` check require the opaque server-issued discovery `connectScopeId` to match the selected public-production target before creating membership atomically |
| Logging & Admin | `POST /feature-flags/toggle`, `POST /tick-remediation/pause`, `POST /tick-remediation/resume` | `tenant_regular` operator ingress | **Target; not currently routable.** Exact `control-ui` profile and current operator role. Tenant-role branches require live membership and membership-generation freshness; privileged global-role branches require live `current_global_role` and `role_appropriate_assurance`, while the global `platformAdmin` branch uses target-tenant generation without target membership. Each mutation also requires its action schema, shared `mutationDigest/v1` golden vectors, and Account authorization-reference issuance/redemption before enablement. |
| Logging & Admin | `POST /moderation/actions` | `tenant_regular` operator ingress | **Target; unavailable/gated.** Exact `control-ui` profile. Roles are `moderator`, `tenantAdmin`, and `platformAdmin`; tenant-role branches require live membership and membership-generation freshness, while the global `platformAdmin` branch requires a valid TOTP-backed `privileged_control` window and uses target-tenant generation without target membership or gameplay admission. The route cannot be current operator support until its action-family schema, shared `mutationDigest/v1` vectors, and Account authorization-reference issuance/redemption flow exist. Target mutation contract: `durable_intent_then_moderation_record_idempotency`. Game Session and Social & Groups separately consume the live `EvaluateModerationPolicy` read at their enforcement boundaries. Versioned propagation remains coverage drift. |
| Logging & Admin | `POST /reports` | `player_bootstrap_tenant` | **Target-only; not currently routable.** Accepts caller-bound `player-bootstrap`, derives `tenantId` and `reporterAccountId` from the authenticated caller and live membership, applies `moderation_report_policy`, and writes a caller-bound durable report record. |
| Logging & Admin | `POST /admission-pointers`, `POST /admission-pointers/cutover`, `POST /admission-pointers/version-upgrades` | `tenant_regular` operator ingress | **Target; not currently routable.** This is a target inventory row only: the public OpenAPI surface no longer exposes the admission-pointer open/close/cutover mutations, and the remaining controller/owner paths are implementation drift. The `GET` read/audit/proof-read routes remain live; the `POST` preparation operation is separately gated. Exact `control-ui` profile and current operator role are required for any future ingress. Tenant-role branches require live membership and membership-generation freshness; privileged global-role branches require live `current_global_role` and `role_appropriate_assurance`, while the global `platformAdmin` branch uses target-tenant generation without target membership. Action schemas, shared `mutationDigest/v1` vectors, and Account authorization-reference issuance/redemption are required before external support; the global branch additionally requires target-only `privileged_control`. |
| Game Session Service | `ToggleFeatureFlag`, `PauseTicksForScope`, `ResumeTicksForScope`, `SetAdmissionPointer`, `PrepareVersionUpgrade`, `ExecutePreparedVersionCutover` | `internal_workload` mirrored owner RPCs | **Target; not currently routable.** Exact Logging & Admin mTLS workload identity plus an Account-redeemed bounded operator authorization reference; no end-user JWT. These owner RPCs cannot be enabled until the corresponding action schema, shared `mutationDigest/v1` golden vectors, and Account authorization-reference issuance/redemption flow exist. |
| Game Session Service | `StartSession` / `RestartSession` / `StopSession` | `internal_workload` operator RPCs | **Status: `target_not_currently_routable` as external operator support.** Exact Logging & Admin mTLS workload identity plus an Account-redeemed bounded operator authorization reference; no end-user JWT. The canonical external ingress is Logging & Admin; direct owner edge exposure is denied during migration. |
| Game Session Service | `RefreshRoles` | `internal_workload` | **Status: `target_not_currently_routable`.** The gRPC owner RPC remains target-only while the supported HTTP owner route is the current surface. When enabled, it requires exact Logging & Admin mTLS workload identity, current session and current Account role state (`current_account_roles`), and owner-local mutation-digest/idempotency fields; it does not carry an end-user token, delegated subject, or Account-issued operator authorization reference. |
| Game Session Service | `POST /sessions` | `tenant_regular` | **Target; not currently supported operator ingress.** A current owner-local OpenAPI scaffold exists, but `route_status` remains `target_not_currently_routable` until the operator mutation support gate is satisfied. Exact `control-ui` profile, `tenantAdmin` or `platformAdmin`, and `privileged_control_when_global_role` assurance. The tenant-role branch requires live membership and membership-generation checks; the global `platformAdmin` branch requires exact target-tenant generation binding without target membership. Runtime entitlement checks remain route-specific; target canonical external ingress is Logging & Admin with Account authorization-reference forwarding. |
| Game Session Service | `POST /sessions/{sessionId}/stop` | `tenant_regular` | **Target; not currently supported operator ingress.** A current owner-local OpenAPI scaffold exists, but `route_status` remains `target_not_currently_routable` until the operator mutation support gate is satisfied. Exact `control-ui` profile, `tenantAdmin` or `platformAdmin`, and `privileged_control_when_global_role` assurance. The tenant-role branch requires live membership and membership-generation checks; the global `platformAdmin` branch requires exact target-tenant generation binding without target membership. Runtime ownership checks remain route-specific; target canonical external ingress is Logging & Admin with Account authorization-reference forwarding. |
| Game Session Service | `POST /sessions/{sessionId}/restart` | `tenant_regular` | **Target; not currently supported operator ingress.** A current owner-local OpenAPI scaffold exists, but `route_status` remains `target_not_currently_routable` until the operator mutation support gate is satisfied. Exact `control-ui` profile, `tenantAdmin` or `platformAdmin`, and `privileged_control_when_global_role` assurance. The tenant-role branch requires live membership and membership-generation checks; the global `platformAdmin` branch requires exact target-tenant generation binding without target membership. Runtime entitlement checks remain route-specific; target canonical external ingress is Logging & Admin with Account authorization-reference forwarding. |
| Game Session Service | `POST /sessions/{sessionId}/refresh-roles` | `internal_workload` | **Status: `current_openapi_operator_surface`; owner-local workload route, not operator-reference-redeeming.** Exact Logging & Admin mTLS workload identity, current session and current Account role state, and owner-local mutation-digest/idempotency fields are required. It does not receive delegated subject authority or an Account-issued operator authorization reference, and it remains outside the operator mutation support gate. |
| Account Service | `AuthLogin` | `public` | `control-ui` auth entrypoint |
| Account Service | `PlayerBootstrapLogin` | `public` | First-party gameplay bootstrap entrypoint; issues `player-bootstrap` token profile only |
| Account Service | `EnterPrivilegedControlWindow` | `account_scoped` | **Target; not currently implemented.** Exact `control-ui` profile plus recent ordinary reauthentication and independent TOTP; this route creates the bounded role-scoped window and does not require an existing elevation window |
| Account Service | `IssueHumanOperatorAuthorizationReference` | `internal_workload` | **Target; not currently routable.** Logging & Admin only: exact allowlisted mTLS workload plus a forwarded `control-ui` token with its exact active Account issued-token registry record; Account rereads current account/token generation, operator roles, role-appropriate assurance, the applicable tenant or target-tenant generation, and required `privileged_control` predicate before issuing the bounded reference |
| Account Service | `IssueAutomationOperatorAuthorizationReference` | `internal_workload` | **Target; not currently routable.** Logging & Admin only: exact allowlisted mTLS workload plus current versioned automation policy; no end-user JWT or user impersonation. The current branch is tenant-scoped only, binds the exact current tenant generation, and rejects global-role or `privileged_control` action families |
| Account Service | `JoinPublicProductionMembership` | `internal_workload` | Canonical internal explicit-join mutation called only by the exact Game Session mTLS workload with typed caller-bound player execution context; it accepts no end-user token and commits membership plus durable operation, audit, and outbox evidence transactionally. `EnsurePublicProductionPlayerMembership` is a differently named current implementation seam and drift, not an alias or matrix operation. |
| Account Service | `DELETE /tenants/{tenantId}/memberships/me` | `caller_membership_scoped` | Caller-bound membership exit remains available while billing-blocked to any current member role, requires live subject-bound membership, and advances membership authority atomically |
| Account Service | `AuthLogout` / `AuthLogoutAll` | `account_scoped` | Accepts `control-ui` (`audience: control-ui`) or `player-bootstrap` (`audience: player-bootstrap`) with registry-backed authenticated account scope. Bounded retries may return no-op success only from a durable committed exact-token tombstone (`AuthLogout`) or durable proof that a prior logout-all superseded the token (`AuthLogoutAll`); registry absence alone is insufficient and the retry exception grants no reusable authorization context |
| Account Service | `GET /accounts/{accountId}/deletion` | `pending_deletion_scoped` | **Target; not currently implemented.** Account-issued pending-deletion credential bound to the account and deletion workflow; returns deletion status only |
| Account Service | `POST /accounts/{accountId}/deletion/cancel` | `pending_deletion_scoped` | **Target; not currently implemented.** Account-issued pending-deletion credential; cancellation revokes pending access before restoring normal authority |
| Account Service | `GET /accounts/{accountId}/deletion/export` | `pending_deletion_scoped` | **Target; not currently implemented.** Account-issued pending-deletion credential; returns only the asynchronous versioned cross-owner export manifest |
| Account Service | `POST /accounts/{accountId}/deletion/billing-settlement` | `pending_deletion_scoped` | **Target; not currently implemented.** Account-issued pending-deletion credential restricted to necessary billing settlement |
| Account Service | `GET/PUT /tenants/{tenantId}/profiles/{accountId}` | `tenant_regular` | Canonical tenant-qualified profile routes. Subject-bound to caller `accountId`; live membership authority supplies current membership lifecycle, membership authority generation, and independent `membershipVersion`; scoped tenant roles apply and `platformAdmin` is not an override. Legacy `/profiles/{accountId}` is implementation drift only |
| Account Service | `ExportAccount` / `DeleteAccount` (`/accounts/{accountId}/...`) | `account_scoped` | Subject-bound to caller `accountId`; `platformAdmin` override only. `DeleteAccount` also requires no nonterminal owned subscriptions. The self-service branch binds the target to the caller; the explicit `platformAdmin_override` branch may target another account only with current global role and privileged assurance. Delete additionally requires `billing_deletion_clearance`: no nonterminal owned subscriptions or billing-owner transfers and no unresolved provider, webhook, outbox, cancellation, customer, subscription, or reconciliation work. |
| Account Service | `LinkExternalAccount` (`POST /accounts/{accountId}/external`) | `account_scoped` | **Currently routable with known drift.** The current route remains a tenant-scoped caller-asserted scaffold without provider callback verification; the target binds the account subject globally, requires recent reauthentication and verified provider authorization, and permits no tenant-role or `platformAdmin` override. |
| Account Service | `IssueConnectToken` | `player_bootstrap_tenant` | **Target:** caller-bound `player-bootstrap` auth is registry-validated first. Issuance requires an existing caller-bound `membershipLifecycleState=ACTIVE` membership and, from one Account snapshot, exactly one selected-tenant `authorityTuple.tenantAuthorityGeneration` entry, exactly one selected-tenant `authorityTuple.membershipAuthorityGeneration` entry, and exactly one same-key `membershipVersion` entry; `privateRealmGrantVersions` is `[]` for public production or exactly the selected grant entry for private/playtest. The canonical `connect-token-issuance-evidence/v1` boundary binds `connectScopeId`, `requestId`, `requestDigest`, `selectedRouteMembershipEvidence`, the complete bounded `authorityTuple`, separate `membershipVersion`, `jti`, `replayAdmissionFence`, `runtimeEntitlementAuthorityReference`, and `admissionPointerAuthorityReference`; a successful dedicated record is `COMMITTED` before the token/cookie is exposed. Exact retries return the stored success or deterministic failure and never mint a second token. The gameplay-connect artifact is not written to the ordinary JWT registry and omits only its registry-only `tokenGeneration` and `issuanceFence` fields; the supplied player-bootstrap still requires its ordinary registry record. **Current drift:** the bootstrap path still uses the legacy `session:auth:account:<accountId>:<tokenHash>` session-key shape rather than the target `session:auth:token:<tokenHash>` record, and the target registry/evidence proof is not yet complete; the membership-generation reread remains unimplemented. Existing caller-bound membership is required for every realm regardless of visibility or grant; for a publicly visible public-production target whose joining policy permits entry, missing membership returns `JOIN_REQUIRED` with no membership, issuance record, or token. Private/playtest targets require both membership and grant; the grant never substitutes for membership. Global roles alone never grant gameplay admission or connect-token issuance. |
| Account Service | `Authenticate` | `internal_workload` | Exact Game Session mTLS identity, no pre-existing issued token, and trusted server-derived credential source context |
| Account Service | `RefreshGameplayServiceToken` | `internal_workload` | Exact Game Session mTLS identity plus current `game-session-account-delegation` authority with audience `account-service` |
| Account Service | `GetTenantMembershipForRuntime` / `GetRealmAccessGrant` / `ListRealmAccessGrantsForAccount` | `internal_workload` | Exact Game Session mTLS identity plus validated typed player context; no circular end-user token prerequisite |
| Account Service | `GetTenantEntitlementsForRuntime` | `internal_workload` | Exactly one complete caller-policy alternative: Game Session mTLS plus current private delegation and typed player context for player admission, or World Management mTLS plus tenant/operation-bound instance-lifecycle context without a player delegation. Constraints within the selected alternative are conjunctive; no `control-ui` or Logging and Admin caller; not edge exposed |
| Account Service | `CommitTenantCapacityAdmission` | `internal_workload` | **Target; not currently routable.** Only Game Session or World Management may call the exact Account mTLS boundary. The request binds `tenantId`, stable `requestId`/`admissionId`, the expected complete applicable `authorityTuple`, separate expected `entitlementVersion`, exact billing stream/checkpoint evidence, and a bounded `capacityDelta` with explicit wire presence so present zero differs from absent; both encodings require golden vectors. It also carries versioned `mutationDigest` over that payload; an exact identity-and-payload retry replays, while any changed evidence, delta, wire presence, or digest is an idempotency conflict before usage mutation. Account performs the final fenced reservation commit and returns the committed evidence or a canonical stale/quota/unavailable outcome. |
| Account Service | `GetTenantEntitlementsTenant` | `billing_safe_tenant` | `tenantAdmin` (tenant-scoped); live `current_operator_roles` and caller-bound membership checks |
| Account Service | `GetTenantEntitlementsCrossTenantSupportSafe` | `cross_tenant_support_safe` | `support`/`platformAdmin` |
| Account Service | `GetSubscriptionTenantHighLevel` | `billing_safe_tenant` | `tenantAdmin` (tenant-scoped); live `current_operator_roles` and caller-bound membership checks |
| Account Service | `GET /tenant-admin/tenants/{tenantId}/export` (`ExportTenantData`) | `billing_safe_tenant` | **Target; current implementation has known account-target drift and the tenant-wide route is not currently routable.** Live `current_operator_roles` and `tenantAdmin` membership are bound to the `{tenantId}` route segment; target-tenant generation is intentionally not required by this billing-safe classification. The request selects no `accountId` subject and returns tenant-wide tenant-owned export data only. |
| Account Service | `GetSubscriptionCrossTenantSupportSafe` | `cross_tenant_support_safe` | `support`/`platformAdmin` |
| Account Service | `ListSubscriptionsTenantHighLevel` | `billing_safe_tenant` | `tenantAdmin` (tenant-scoped); live `current_operator_roles` and caller-bound membership checks |
| Account Service | `ListSubscriptionsCrossTenantSupportSafe` | `cross_tenant_support_safe` | `support`/`platformAdmin` |
| Account Service | `ListSubscriptionsCrossTenantBillingSafeReports` | `cross_tenant_billing_safe` | `billingAdmin`/`platformAdmin` |
| Account Service | `GetCallerTenantMembershipTenant` | `billing_safe_tenant` | `tenantAdmin` (subject bound to caller); live `current_operator_roles`, caller-bound membership, and membership authority generation apply |
| Account Service | `GetTenantMembershipForAccountCrossTenant` | `cross_tenant_billing_safe` | `billingAdmin`/`platformAdmin` |
| Account Service | `BillingArtifactsTenant` | `billing_safe_tenant` | Target-only, not currently routable; live `current_operator_roles` must prove `tenantAdmin` alongside caller-bound membership. Tenant billing operations require billing-owner authorization, explicit per-subscription instrument binding, replacement-before-detach, and audited ownership transfer; unavailable authority returns `AUTH_UNAVAILABLE` |
| Account Service | `BillingArtifactsCrossTenant` | `cross_tenant_billing_safe` | Target-only, not currently routable; `billingAdmin`/`platformAdmin`; unavailable authority returns `AUTH_UNAVAILABLE` |
| Account Service | `PaymentInstrumentWalletAccount` | `account_scoped` | **Target-only; not currently routable.** Restricted to the current billing owner after recent reauthentication. Requires wallet-to-account/provider customer mapping, explicit per-subscription binding, replacement-before-detach, and audited billing-ownership transfer. |
| Account Service | `PaymentInstrumentWalletCrossTenant` | `cross_tenant_billing_safe` | **Target-only; not currently routable.** Restricted to `billingAdmin`/`platformAdmin` with `privileged_control` assurance. The target request binds `target_account_id`, `affected_subscription_ids`, `new_billing_owner_account_id`, replacement instrument bindings, expected subscription row versions, effective boundary, reason, and `control_plane_request_id`; live checks require issuer/account generations, current global role, wallet-to-provider-customer mapping, explicit per-subscription bindings, replacement-before-detach safety, and a clear audited ownership-transfer state. Shared-instrument acknowledgement or tenant membership is not authority, and this row does not claim a live route. |
| Account Service | `POST /auth/pending-deletion/recovery/challenge` | `public` | Target-only dedicated recovery challenge; it discloses no account state and grants no normal authority |
| Account Service | `POST /auth/pending-deletion/recovery` | `public` | Target-only recovery issuance; consumes the dedicated challenge, requires live `pending_deletion_state`, and issues only `pending-deletion-access` |

### Target Public Production Onboarding Example

1. A brand-new authenticated account completes `LOGIN` and issues `WORLDS`.
2. `WORLDS` may list the world's default public production realm even though no tenant membership exists yet.
3. `REALMS <world>` returns that default public production realm plus any separately granted additional realms.
4. The player explicitly uses `JOIN <world>` or `Join & Play`; Account atomically creates membership and durable audit/outbox.
5. `CHARS` requires the current membership generation, realm visibility/grant, entitlement, and admission pointer before listing caller-valid characters. It does not create a gameplay binding or perform `PLAY`'s final binding CAS.
6. Character creation, connect-token issuance, and `PLAY` require that membership and never create it implicitly; each consumes the same current catalog/pointer truth, while `PLAY` additionally performs its full gameplay-binding checks.
7. Global roles alone never bypass this flow or grant gameplay admission/connect-token issuance without the same live checks.

The matrix should be expanded as service API surfaces evolve. Service docs may include local excerpts, but this file is the canonical policy for declared entries; current omissions remain an explicit inventory drift/gap until source-stable coverage and validation complete the registry.
