# Realm Routing and Playable State

## Target-State Authority

This is a non-normative implementation tracker for realm routing and playable state. Canonical target-state realm visibility, admission pointers, playable-state projection, runtime target identity, and freshness contracts remain authoritative in [design/architecture](../../architecture/README.md) and the linked design sources below; this tracker owns current implementation status, proof evidence, gaps, and handoffs, not normative design.

## Current Status

The working realm-catalog projection, admission-pointer reads, bootstrap/connect-scope flow, session-freshness normalization, runtime projections, and currently live playable-state boundaries are implemented. This is not a claim that routing is fully implemented. The direct-text [REALMS-to-JOIN scope handoff](../../architecture/system-architecture-authentication.md#direct-text-realms-to-join-scope-normative) remains target-only and unimplemented; Account issuance/validation and Game Session transport-local scope retention, retry, terminal-outcome, and expiry behavior still need implementation and proof. The complete routing boundary remains partial: persisted routes cannot represent explicit `CLOSED`, catalog and route revisions are combined, the key is not tenant-qualified, version checking is not an atomic database CAS, prepared-cutover execution is recorded after the pointer transaction, and active bindings are invalidated from current pointer state rather than ending through the target bounded drain. Public/grant policy is also partial because explicit grant expiry, monotonic mutation outcomes/tombstones, and tenant-admin management are absent. Playtest initialization/reset, all-owner manifests, lifecycle generations, and close/expiry orchestration are not implemented or end-to-end proved; their target owner contracts are linked below.

Smoke/reset consequence: the current read-only `LOGIN -> PLAY -> LOOK` proof does not establish a writable playable-state boundary. The run-owned Compose mutation/teardown boundary is implemented, while persistent/shared mutation remains unavailable pending its restricted-synthetic identity/namespace/fence verifier and proof. Shared or player-facing whole-stack wipe/reset/restore is prohibited; run-owned teardown is fixture disposal and does not authorize Coordination Redis reset. Existing routing and smoke evidence does not prove persistent/shared mutation.

## Packet 4 Status and Proof Gaps

- `AA-3.3` / `AR-3.1`: [ADR 0122](../../architecture/decisions/adr-0122-stable-playable-state-namespaces-for-runtime-replacement.md) and [ADR 0123](../../architecture/decisions/adr-0123-database-authoritative-temporal-coordinated-world-lifecycle.md) remain the owners for namespace/replacement and lifecycle coordination. Current routing/playable-state implementation and focused proof remain partial; namespace transition, cutover, drain, cleanup, and termination proof are not established.
- `AR-3.4` / `AA-3.2`: [ADR 0137](../../architecture/decisions/adr-0137-isolated-playtest-state-modes-and-reset.md), [ADR 0138](../../architecture/decisions/adr-0138-expiring-playtest-grants-with-bounded-active-revocation.md), [ADR 0139](../../architecture/decisions/adr-0139-tenant-owned-runtime-lifecycle-with-audited-break-glass.md), [Versioning & Runtime Configuration](../../architecture/system-architecture-versioning-runtime.md#fork-snapshot-boundary-for-playtest-realms), [Account API Contracts](../../architecture/microservices/account-service/api-contracts.md#account-owned-playtest-grant-contract), and [Session Behavior](../../architecture/system-architecture-session-behavior.md#playtest-grant-and-lifecycle-session-consequences) remain the canonical owners. This tracker claims no playtest/lifecycle implementation or proof; cross-owner integration remains unverified.

These links add decision provenance only; they do not upgrade routing or playable-state capability status.

## Capability Status

| Capability | Implementation | Verification | Design | Implementation anchors | Proof anchors | Secondary handoffs | Gap or decision |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `AA-3.1` World and realm discovery catalog | `partial` | `drift-found` | [Multi-tenancy](../../architecture/system-architecture-multi-tenancy.md); [Authentication and authorization](../../architecture/system-architecture-authentication.md); [Versioning and runtime](../../architecture/system-architecture-versioning-runtime.md) | [GameplayWorldCatalog.java](../../../services/game-session-service/src/main/java/net/firedevops/firemud/gamesession/command/text/GameplayWorldCatalog.java); [Database pointer authority](../../../services/game-session-service/src/main/java/net/firedevops/firemud/gamesession/service/impl/DatabaseGameplayAdmissionPointerAuthorityService.java); [AuthController.java](../../../services/account-service/src/main/java/net/firedevops/firemud/accountservice/controller/AuthController.java); [AccountServiceImpl.java](../../../services/account-service/src/main/java/net/firedevops/firemud/accountservice/service/impl/AccountServiceImpl.java); [pointer schema](../../../services/game-session-service/src/main/resources/db/migration/V1__baseline.sql); [game_session_service.proto](../../../protos/game-session/v1/game_session_service.proto) | [GameplayWorldCatalogTest.java](../../../services/game-session-service/src/test/java/unit/net/firedevops/firemud/gamesession/command/text/GameplayWorldCatalogTest.java); [AccountServiceImplTest.java](../../../services/account-service/src/test/java/unit/net/firedevops/firemud/accountservice/service/impl/AccountServiceImplTest.java); [GameplayAdmissionPointerBootstrapInitializerTest.java](../../../services/game-session-service/src/test/java/unit/net/firedevops/firemud/gamesession/data/GameplayAdmissionPointerBootstrapInitializerTest.java) | [Player Access](./player-access-and-session.md); [Player Experience](./player-experience-commands-and-communication.md) | Filtered WORLDS/REALMS/CHARS discovery is live, but the baseline unique index is `(world_slug, realm_slug)` rather than canonical `{tenantId, worldSlug, realmSlug}`; cross-tenant duplicate selectors are not schema-safe. |
| `AA-3.2` Public visibility, explicit grants, and playable-state policy | `partial` | `drift-found` | [Multi-tenancy](../../architecture/system-architecture-multi-tenancy.md); [Authentication and authorization](../../architecture/system-architecture-authentication.md); [Account API contracts](../../architecture/microservices/account-service/api-contracts.md); [ADR 0137](../../architecture/decisions/adr-0137-isolated-playtest-state-modes-and-reset.md); [ADR 0138](../../architecture/decisions/adr-0138-expiring-playtest-grants-with-bounded-active-revocation.md); [ADR 0139](../../architecture/decisions/adr-0139-tenant-owned-runtime-lifecycle-with-audited-break-glass.md) | [AccountServiceImpl.java](../../../services/account-service/src/main/java/net/firedevops/firemud/accountservice/service/impl/AccountServiceImpl.java); [InternalRuntimeController.java](../../../services/account-service/src/main/java/net/firedevops/firemud/accountservice/controller/InternalRuntimeController.java); [AccountRealmAccessGrant.java](../../../services/account-service/src/main/java/net/firedevops/firemud/accountservice/entity/AccountRealmAccessGrant.java); [V17__account_realm_access_grants.sql](../../../services/account-service/src/main/resources/db/migration/V17__account_realm_access_grants.sql); [ScopedCharacterResolver.java](../../../services/entity-management-service/src/main/java/net/firedevops/firemud/entitymanagement/service/ScopedCharacterResolver.java); [PlayableStateKeyResolver.java](../../../services/entity-management-service/src/main/java/net/firedevops/firemud/entitymanagement/service/PlayableStateKeyResolver.java); [account_service.proto](../../../protos/account/v1/account_service.proto) | [AccountServiceImplTest.java](../../../services/account-service/src/test/java/unit/net/firedevops/firemud/accountservice/service/impl/AccountServiceImplTest.java); [CharacterServiceImplListTest.java](../../../services/entity-management-service/src/test/java/unit/net/firedevops/firemud/entitymanagement/service/impl/CharacterServiceImplListTest.java); [PublicBootstrapRoutesProdProfileTest.java](../../../services/account-service/src/test/java/unit/net/firedevops/firemud/accountservice/controller/PublicBootstrapRoutesProdProfileTest.java) | [Player Access](./player-access-and-session.md); [Player Experience](./player-experience-commands-and-communication.md) | Public-production admission, grant reads, and shared/isolated state projection are live; the grant entity/schema has no expiry and only internal grant endpoints, so tenant-admin grant management is absent. |
| `AA-3.3` Runtime target identity, admission pointers, and routing freshness | `partial` | `drift-found` | [Multi-tenancy](../../architecture/system-architecture-multi-tenancy.md); [Versioning and runtime](../../architecture/system-architecture-versioning-runtime.md); [Redis architecture](../../architecture/system-architecture-redis.md) | [GameplayAdmissionPointerSnapshots.java](../../../services/game-session-service/src/main/java/net/firedevops/firemud/gamesession/service/GameplayAdmissionPointerSnapshots.java); [DatabaseGameplayAdmissionPointerAuthorityService.java](../../../services/game-session-service/src/main/java/net/firedevops/firemud/gamesession/service/impl/DatabaseGameplayAdmissionPointerAuthorityService.java); [GameSessionAdmissionPointerControlPlaneService.java](../../../services/game-session-service/src/main/java/net/firedevops/firemud/gamesession/service/impl/GameSessionAdmissionPointerControlPlaneService.java); [SessionRoutingNormalizationService.java](../../../services/game-session-service/src/main/java/net/firedevops/firemud/gamesession/service/SessionRoutingNormalizationService.java); [pointer schema](../../../services/game-session-service/src/main/resources/db/migration/V1__baseline.sql); [game_session_service.proto](../../../protos/game-session/v1/game_session_service.proto) | [GameplayAdmissionPointerSnapshotsTest.java](../../../services/game-session-service/src/test/java/unit/net/firedevops/firemud/gamesession/service/GameplayAdmissionPointerSnapshotsTest.java); [DatabaseGameplayAdmissionPointerAuthorityServiceTest.java](../../../services/game-session-service/src/test/java/unit/net/firedevops/firemud/gamesession/service/impl/DatabaseGameplayAdmissionPointerAuthorityServiceTest.java); [SessionRoutingNormalizationServiceTest.java](../../../services/game-session-service/src/test/java/unit/net/firedevops/firemud/gamesession/service/SessionRoutingNormalizationServiceTest.java); [GameSessionControlPlaneGrpcServiceTest.java](../../../services/game-session-service/src/test/java/unit/net/firedevops/firemud/gamesession/service/impl/GameSessionControlPlaneGrpcServiceTest.java) | [Player Access](./player-access-and-session.md); [Game Session Runtime](./game-session-runtime-and-tick-coordination.md) | Pointer reads and audit reads are live. Internal sequential expected-version and prepared-cutover seams exist but are nonconformant and do not establish the target atomic CAS/cutover. Explicit closure, atomic CAS/cutover bookkeeping, catalog revision separation, tenant-qualified persistence, and bounded active-session drain remain unimplemented. |

## Canonical Design Sources

- [Multi-tenancy](../../architecture/system-architecture-multi-tenancy.md) defines global account identity, tenant membership, visible realms, admission, and realm-scoped gameplay state.
- [Authentication and authorization](../../architecture/system-architecture-authentication.md) defines bootstrap, connect-token, gameplay admission, and reconnect authority.
- [Game Session Service](../../architecture/microservices/game-session-service/README.md) owns the current realm catalog, admission pointers, session normalization, and runtime routing read/control-plane contracts.
- [Account Service](../../architecture/microservices/account-service/README.md) owns account membership, grants, bootstrap discovery, and connect-token issuance over Game Session authority.
- [Entity Management Service](../../architecture/microservices/entity-management-service/README.md) owns scoped playable-state resolution for character and stateful gameplay records.

## Consolidated Implementation Record

### Authority and Pointer Contract

Players address a visible `{tenantId, worldSlug, realmSlug}` realm, never a raw `gameInstanceId`. Game Session persists `gameplay_admission_pointer` rows and append-only audit events. The current row always requires one positive runtime target and cannot yet express the target `CLOSED` state. It combines runtime target, visibility/public-production classification, display metadata, `stateScope`, character-selection requirements, and character-creation policy under one `pointerVersion`; the target separates catalog revision from runtime routing.

Selectors resolve to a tenant-local durable `realmId` before direct pointer authority is read. The target canonical singular key is `{tenantId, realmId}`; `GetAdmissionPointer(tenantId, realmId)` reads direct pointer authority using that key and does not reconstruct authority through browse projection. The current Game Session proto and `GameplayAdmissionPointerAuthorityService` still use `{tenantId, worldSlug, realmSlug}` slug lookup; realm-ID lookup and caller support remain implementation drift and are not claimed as live. Target Account bootstrap admission, public-production membership creation, prepared cutover validation, live admission, and operator audit reads use the same resolved realm identity. Membership join and connect-token issuance have separate idempotency contracts: each binds its own `requestId` to its operation, caller, verified discovery scope, and resolved target digest, and the two request IDs are not interchangeable. Current pointer writes use a tenant-qualified slug lookup and may compare `expectedPointerVersion` after that read, but the repository then updates by row ID without a version predicate. This is not an atomic CAS under concurrent writers. The baseline migration also defines the unique pointer index as `(world_slug, realm_slug)` without `tenant_id`, so persistence has not converged with the target `{tenantId, realmId}` key.

Startup seeds pointers only when the persisted store is empty, using dedicated bootstrap pointer-seed properties. After seeding, runtime reads and mutations use persisted pointer authority. `GameplayCatalogProperties` is not production routing authority: Game Session's production catalog projection is pointer-backed, Account Service no longer ships the old local catalog config, and property-to-catalog construction remains only in explicit test support.

Game Session exposes canonical world, realm, and pointer reads plus pointer list, audit, runtime-state, prepared-upgrade, and cutover control-plane surfaces. The public selector-shaped singular helper and preferred runtime-target lookup have been removed; reverse runtime-target consumers use `listByRuntimeTarget(...)` so multiplicity is observable. Logging & Admin is an operator ingress over these contracts, not a second routing owner. Its audit route is `GET /admission-pointers/{tenantId}/{worldSlug}/{realmSlug}/audit`; authenticated tenant access is checked before the call, response tenant mismatches fail closed, and operator pointer mutation derives `actorPrincipal` from the authenticated session rather than caller input.

### Complete Authority and Ambiguity Rules

The shared `GameplayAdmissionPointerSnapshots` rule defines a complete pointer as one with positive `tenantId`, positive `gameInstanceId`, positive `pointerVersion`, nonblank `worldSlug`, `realmSlug`, and `stateScope`. Singular runtime-target authority exists only when the raw current-pointer result contains exactly one row and that row is complete. Any additional row, incomplete row, missing runtime identity, or missing scope makes the singular projection unavailable; incomplete rows are not discarded before multiplicity is assessed. Raw pointer lists still expose such rows for operator inspection.

`GameplayWorldCatalog` applies the shared completeness rule and additionally requires `characterCreationPolicy`. It groups rows by `{tenantId, worldSlug, realmSlug}` and emits a visible catalog entry only for exactly one complete row in that tenant-qualified selector scope. Reverse runtime-target lookup emits one visible identity only when exactly one realm survives for the tenant/runtime target; duplicate selector rows within a tenant, duplicate runtime matches, and incomplete rows disappear from player-facing catalog projection rather than selecting an arbitrary row.

Admission-pointer `stateScope` is the canonical realm policy. Downstream `playableStateScope` is its resolved cross-service representation: `SHARED` maps to `PLAYABLE_STATE_SCOPE_SHARED` and `ISOLATED` maps to `PLAYABLE_STATE_SCOPE_ISOLATED`; it is not a second admission authority. If no single complete pointer can be proved, the singular downstream scope and routing bundle fail closed to unspecified/absent rather than reverse-inferring authority from `gameInstanceId`.

All routing-sensitive bundles preserve `tenantId`, `worldSlug`, `realmSlug`, resolved `gameInstanceId`, and `pointerVersion` together, with `characterSelection` and `playableStateScope` where applicable. Partial, blank, non-positive, stale, or selector-inconsistent bundles are not authoritative. At ingress, proxy and remote control-plane callers must provide `worldSlug`, `realmSlug`, and `pointerVersion` together or none; partial `ScheduleRemoteFollowup` input and partial remote coordinator/followup/result filters return `INVALID_ARGUMENT` instead of widening to an unscoped query. Persisted/read-model partial bundles collapse to absent rather than being projected as current identity.

### Admission, Membership, and Selection

Account-to-tenant membership is an explicit runtime substrate rather than an `accounts.tenant_id` shortcut. The public default production realm is the only v1 open-enrollment path. The obsolete implicit public-production membership-writer seam has been removed. Current connect-token issuance and text-client `PLAY` consume an Account membership response identified by `accountId` and `tenantId` with four admission-evidence fields, `membershipExists`, `gameplayAdmissionAllowed`, `membershipVersion`, and `evaluatedAt`; they admit only when the two admission fields are true, and an eligible public-production request with `membershipExists=false` may return non-actionable `JOIN_REQUIRED`. The live Account adapter has no `membershipLifecycleState`: `membershipExists=false` is the current missing-membership case, while `membershipExists=true` with `gameplayAdmissionAllowed=false` is existing non-admitting membership and cannot be classified as `INACTIVE`. For non-public realms, a missing or non-admitting response returns `WORLD_ACCESS_DENIED` before any realm-grant lookup; text-client `PLAY` then performs the Account-owned realm-grant check for non-public targets, and a realm grant never substitutes for membership. ADR 0025's target requires explicit `JOIN` / `Join & Play`, with membership plus durable audit/outbox committed in one SQL transaction, a real monotonic `membershipVersion`, and no membership writes from character creation, connect-token issuance, or `PLAY`. Explicit `JOIN` / `Join & Play` remains unimplemented, and the target membership-authority-generation reread and applicable realm-grant validation at connect-token issuance remain gaps. Public-production classification remains explicit pointer/catalog metadata, not `realmSlug == "production"`.

First-party bootstrap discovery exposes worlds, realms, and characters as the same catalog/admission projection used by `WORLDS`, `REALMS`, `CHARS`, and `PLAY`. Discovery returns opaque, short-lived `connectScopeId` plus `pointerVersion`, `evaluatedAt`, and `connectScopeExpiresAt`. Current text-client `PLAY` enforces the Account-owned realm grant for private/playtest targets; current `/auth/connect-token` issuance revalidates current pointer/visibility and membership but does not validate that applicable grant. Target issuance adds that fresh grant check. The target returning shortcut has the same grant gate, while the current shortcut and any resulting token are not authorization proof. The endpoint treats `requestId` as an idempotency key and replays the same token payload or deterministic failure for a live repeated attempt. Invalid scopes return `CONNECT_SCOPE_INVALID`; expired or cutover-mismatched scopes return `CONNECT_SCOPE_MISMATCH`, both directing the client to rerun discovery instead of using local target fallback.

The direct-text [REALMS-to-JOIN scope handoff](../../architecture/system-architecture-authentication.md#direct-text-realms-to-join-scope-normative) remains target/unimplemented because Account direct-text `JOIN` issuance/validation and Game Session transport-local scope retention through retry, terminal outcome, or expiry still lack implementation and focused proof. This is distinct from the browser/WebSocket bootstrap snapshot and remains a contract gap rather than current proof.

Gateway and Game Session preserve and validate `worldSlug`, `realmSlug`, `connectScopeId`, `connectRequestId`, `pointerVersion`, and resolved target in first-party connect context. The durable bootstrap shell stores the selector identity and is the reconnect/login/`PLAY` fallback when transient registry state is absent. Generic websocket bootstrap preserves an incoming complete bundle, repairs a runtime-only shell only from one complete current pointer, and collapses ambiguous or incomplete authority to no bundle. If a reused shell had a bundle and the repaired incoming shell does not, it is a real route change: the `BOOTSTRAP_ROUTE_CHANGED` path clears authenticated/gameplay state instead of preserving the older route.

### Session Freshness and Runtime Entry Points

`SessionAuthenticationService` is the canonical higher-level session reader. Tenant-aware `resolveUnverifiedSessionContext(...)` applies stale-pointer normalization; raw `findBySessionId(...)` is only an index for tenant discovery and cannot revive a missing `{tenantId, sessionId}` authority row. Session-id replay, logout, tick-state, failed-login cleanup, and websocket bootstrap reuse consume the canonical helpers. Credential `LOGIN` resolves its bootstrap runtime only from that normalized shell and returns `SESSION_NOT_FOUND` when no bootstrap target survives; it never treats the transport `sessionId` as a runtime id. Gameplay-identity and gameplay-name helpers are also centralized there for `PLAY` existing-binding reuse, durable replay fallback, communication availability, and recipient delivery.

Current normalization clears a stored gameplay binding when it no longer matches current pointer authority, keeps the account logged in, clears live gameplay presence, emits the stale-pointer/region-exit lifecycle effect, and makes later gameplay fail at `PLAY_REQUIRED`. This protects new `LOGIN`/`PLAY` and reconnect from stale routes but currently also invalidates an already connected source binding after cutover. The [Protocol Bridging](../../architecture/system-architecture-protocol-bridging.md) document is the canonical owner for entitlement-continuity and retained-edge policy. The current runtime has no recorded implementation or focused proof for that entitlement-continuity path.

Command staging chooses an admitted gameplay runtime first, then an explicit bootstrap runtime only when the shell retains a complete bootstrap routing bundle; otherwise it fails closed with `NOT_FOUND`. It never treats a numeric `sessionId` as a runtime target. Current runtime-target authority repairs stale command metadata only when one complete pointer proves the target. For an already selected gameplay scope, missing, ambiguous, or incomplete authority persists `playableStateScope=UNSPECIFIED` with no world/realm/version bundle. The current blank-scope login/bootstrap branch is a narrower exception: when runtime authority cannot be resolved, it preserves a normalized shell `{worldSlug, realmSlug, pointerVersion}` trio while persisting `playableStateScope=UNSPECIFIED`. This trio is a non-authoritative lobby selection hint, not an admitted routing bundle: it cannot establish or preserve a runtime route, authorize `PLAY`, or become downstream gameplay metadata. Complete pre-`PLAY` shell metadata is freshness-checked before persistence. Durable command execution and selected-work manifests normalize/collapse admitted routing metadata before replay or tick-batch sealing.

`QueryState(sessionId)` requires a normalized live session shell with positive tenant authority and never guesses a Redis key from a runtime id. Logout stops a runtime only when explicit scope or exactly one complete current isolated pointer positively proves that decision; missing, partial, or multiple pointers mean keep the runtime running. TCP Proxy disconnects suspend runtime state only with explicit `gameInstanceId`; advisory `sessionId` is never promoted, and missing runtime metadata remains best-effort session cleanup plus missing-context metering. Proxy producers and consumers preserve this envelope contract, and partial proxy routing headers are normalized to an unscoped bootstrap rather than forwarded as malformed authority. Authenticated TCP Proxy/Game Session callbacks and Game Session/Game Logic blocking clients use internal-service gRPC identity, with packaged-stack blocking stubs bound to the resolved customizer rather than a provider/no-op fallback.

### Durable Delegation and Control-Plane Projection

Game Session to Game Logic and Entity Management gameplay attestations carry `worldSlug`, `realmSlug`, `pointerVersion`, and `playableStateScope` as an all-or-none admitted claim; downstream guards reject partial claims. Gameplay-originated Automation ingress, timer and materialized schedule work, pin projections, script work/audit/handoff/dead-letter records, automation command handoff, and operator reads preserve the same bundle. Runtime-state projection also carries stored `runtimeRegionId` or `targetRegionId` hints where those consumers already own them, so region-aware plugin/policy preflight and handoff target reads do not collapse to game-instance-only lookup. Live and recent account/friend presence validates against singular current runtime authority; stale rows fall back to recent/offline projection rather than hiding a still-current session or inventing display metadata.

`GetGameInstanceRuntimeState` exposes `currentAdmissionPointers[]`. Its legacy singular `playableStateScope`, `worldSlug`, `realmSlug`, and `pointerVersion` fields are populated only for one complete current pointer; ambiguous or incomplete authority clears those fields while retaining the explicit list. Command-status and remote control-plane projections use the same rule and mark persisted routing stale when current singular authority cannot be proved. Logging & Admin exposes tenant-guarded `GET /admission-pointers/runtime-state/{tenantId}/{gameInstanceId}`, maps canonical runtime metadata, publication details, and current pointers, returns `404` for no runtime row, and rejects response-tenant mismatch as an internal contract failure.

Remote followup persistence and projection use one normalized `{playableStateScope, worldSlug, realmSlug, pointerVersion}` shape. Scope alone is not authoritative; partial stored rows, retry comparisons, coordinator/followup/result projections, and scheduling metadata collapse to no routing identity. Automation handoff success requires a non-error response with nonblank `coordinatorId` and `followupId`; otherwise the response is `REMOTE_RESPONSE_INVALID`, the work item becomes `DEAD_LETTERED`, and the audit outcome is `handoff_failed` rather than `HANDED_OFF`.

### Realm-Scoped Playable State

Tenant membership and character ownership do not imply a shared gameplay namespace. Each realm carries explicit `stateScope` and `characterCreationPolicy`; shared-state realms use a tenant-live `playableStateKey`, while isolated realms, including playtest forks, use an instance-local key. `CHARS` is a realm-local roster with explicit creation policy. The scope-aware roster contract is used by bootstrap character discovery, `REALMS`, `CHARS`, `PLAY`, and `TELL`; isolated realms cannot read or mutate the tenant's production roster through tenant-wide or bare-character shortcuts.

For persistent/shared mutating smoke, this namespace boundary would be the mutation boundary, but the required restricted-synthetic verifier and proof remain unavailable; follow the [canonical testing owner](../../architecture/system-architecture-testing.md#player-flow-smoke-and-reset-boundaries) for the boundary contract. Shared or player-facing whole-stack wipe/reset/restore is prohibited. The implemented run-owned Compose teardown is fixture disposal and does not authorize Coordination Redis reset.

Account identity and account-level cross-game character discovery remain distinct from realm-local gameplay `CHARS`. The live `INVENTORY` surfaces are character gameplay inventory under the resolved playable-state namespace, not an Account-owned cross-game inventory; any future account inventory needs an explicit ownership and transfer contract rather than reuse of realm-local rows.

Entity Management provides shared `ScopedCharacterResolver` and `PlayableStateKeyResolver` boundaries and echoes the resolved scope to callers. The resolved `{tenantId, gameInstanceId, playableStateScope}` governs character reads, inventory, equipment, containers, room-ground items, friend links, progression/activity, actor resources, and active conditions. REST and gRPC state APIs carry the resolved target rather than bare tenant-plus-character IDs; cross-playable-state friend joins are rejected; actor state persists against the derived namespace rather than raw runtime id.

Automation faction reputation uses the same resolved namespace and requires `{tenantId, gameInstanceId, playableStateScope, characterId}`. Gameplay attestations preserve and validate scope in Entity Management. Automation ingress makes scope first-class, and durable work-item, ingress-audit, handler-audit, handoff-event, dead-letter, schedule-instance, and pin-projection records preserve it through timer refresh, requeue, control-plane readback, and presence/friend-presence projection. Shared and isolated behavior therefore remains distinct across the currently live roster, item, progression, resource/condition, faction, scripting, and presence surfaces.

### Recorded Proof

The full `WORLDS` -> `LOGIN` -> `REALMS` -> conditional `JOIN` -> conditional `CHARS`/character creation -> `PLAY` bridge sequence is target-only and is not claimed by the recorded smoke evidence. The evidence records focused proof for sequential stale-version rejection, stale-pointer rejection at connect-token issuance, verified `LOGIN`, `PLAY`, reused relogin, websocket route/selector refresh, session normalization, command staging and replay, communication availability/delivery, presence, logout, tick-state, disconnect envelopes, remote routing metadata, complete-pointer catalog/control-plane projection, world-qualified duplicate-slug lookup, operator tenant guards, and shared-versus-isolated playable-state behavior. It also records touched-service `check -PfullCheck` runs, focused Game Session/Account/Entity Management/Automation/Logging & Admin/TCP Proxy/Game Logic tests, and fresh rebuilt `bash dev-tools/verify-fresh-bootstrap.sh` proof covering the then-current abbreviated WebSocket and Telnet `LOGIN` -> `PLAY` -> `LOOK` path plus an item/container/equipment mutation extension and authenticated disconnect cleanup. That historical run does not establish the current default baseline boundary or mutating cross-transport parity. It does not prove `WORLDS`, `REALMS`, or `JOIN`, concurrent database CAS, crash recovery between pointer commit and prepared-upgrade execution marking, explicit closure, separated catalog revisions, or bounded active-session drain. These are historical validation facts; the decision review did not rerun them.

## Active Gaps

- Later gameplay-state families, especially loadouts, abilities, authored effects, richer resource tables, and any new character-creation or social/runtime consumer, still need explicit adoption of `{tenantId, gameInstanceId, playableStateScope}` before they become live. They must not reopen tenant-wide or bare-character shortcuts.
- Entity Management owns actor rows and the Account relationship/projection to those rows remains an evolving Account/Entity seam. Actor rows carry account and tenant identity, but neither membership nor that relationship establishes one implicit gameplay namespace across realms.
- Explicit `JOIN`/`Join & Play` remains unimplemented. Current connect-token issuance and `PLAY` consume `membershipExists`, `gameplayAdmissionAllowed`, `membershipVersion`, and `evaluatedAt`, and never create membership; an eligible public-production response with `membershipExists=false` may return `JOIN_REQUIRED`, while missing or non-admitting non-public membership remains `WORLD_ACCESS_DENIED`. An existing non-admitting response cannot be classified as `INACTIVE` because the live adapter does not expose lifecycle state. The target membership-authority-generation reread at issuance remains a gap.
- Future routing-sensitive delegated consumers, replay-sensitive caches, and new automation follow-up families need explicit proof that they preserve the complete bundle and fail closed; closed `09.1` seams do not imply unreviewed consumers are safe.
- Blank-scope login/bootstrap command staging currently retains the normalized shell selection hint with `playableStateScope=UNSPECIFIED` when runtime-target authority is absent, ambiguous, or incomplete. The implementation still needs focused proof that this hint cannot be consumed as routing authority or downstream gameplay metadata, plus convergence if any consumer crosses that boundary.
- Region-partitioned runtime ownership and broader region fencing are tracked separately from admission routing. Current runtime projections carry region hints where available but do not implement the final execution-partition topology.
- Replacement-instance migration/remap mechanics and domain-specific carry-forward rules remain unimplemented, including economy, guild, and combat-specific policy.
- The current pointer model cannot represent explicit `CLOSED`, increments its one version for every upsert including display metadata, and invalidates active bindings against current pointer state rather than ending them through the target bounded drain.
- Persistent/shared mutating smoke remains unavailable at the routing/playable-state boundary because its restricted-synthetic identity/namespace/fence verifier and proof are not implemented. Shared or player-facing whole-stack wipe/reset/restore is prohibited. The run-owned Compose mutation/teardown boundary is implemented; its teardown is fixture disposal and does not authorize Coordination Redis reset. See the [canonical testing owner](../../architecture/system-architecture-testing.md#player-flow-smoke-and-reset-boundaries). Current smoke/bootstrap evidence does not upgrade persistent/shared mutation status.
- The target replacement cutover also requires one World-owned durable `cutoverHoldId`/`cutoverHoldFence` after target `ACTIVE` proof. The current implementation has no hold record or coordinated acquire/finalize/readback surface; it does not block source/target termination while the hold is unresolved, bind the hold into the pointer/audit/prepared-execution/source-cleanup/drain-fence transaction, or prove safe abort versus finalization. Expiry and contradictory evidence therefore have no implemented reconciliation proof.
- `expectedPointerVersion` is checked after a read, but the database update is not conditional on the expected version. Concurrent writers and duplicate committed versions are not prevented or proven.
- Pointer state and audit event commit together, but `ExecutePreparedVersionCutover` marks the prepared upgrade executed afterward. A crash in that interval lacks the target atomic retry/reconciliation behavior; the target drain work item, acknowledgement, and readback must each remain bound to the exact `sourceGameInstanceId` as well as `sourceDrainId`.
- The pointer migration's unique index is `(world_slug, realm_slug)` rather than the canonical tenant-qualified key, so cross-tenant duplicate world/realm selectors are not schema-safe even though repository reads include `tenant_id`.
- The canonical grant record permits expiry and calls for tenant-admin management; the current Account grant row and internal-only grant endpoints do not implement that follow-through.
- The current documentation-only slice passed `./gradlew linkCheck lintMarkdown`. Validation and runtime-proof selection follows the [validation and runtime-proof workflow](../../developer-workflows/validation-and-runtime-proof.md); runtime proof is not applicable to this documentation-only checkpoint.

## To Discuss

No competing implementation is recorded for visible realm addressing, world-qualified admission-pointer authority, or shared-versus-isolated playable state. Design discussion is required before changing public admission policy, adding another isolated-state mode, defining replacement-instance migration/carry-forward rules, or introducing a gameplay-state family with different namespace semantics. The current open question is how future families and migration rules should preserve the established namespace and freshness fences without inventing a second authority model.

## Service and Contract Map

| Owner | Current responsibility | Primary contract boundary |
| --- | --- | --- |
| Game Session | Persisted realm catalog and admission pointers; current audit/read-then-write version check/cutover; catalog projection; session normalization; runtime, tick, command, remote, and operator projections | World-qualified admission-pointer gRPC/control-plane APIs; persisted pointer authority; normalized session helpers |
| Account Service | Tenant membership/grants; bootstrap worlds/realms/characters; membership exit; membership-required connect-token issuance; target explicit public-production join | Bootstrap/auth REST and gRPC APIs; runtime membership/grant/entitlement reads; target explicit-join behavior |
| Gateway and TCP Proxy | Trusted first-party/Telnet routing propagation; explicit disconnect runtime metadata; authenticated callbacks | Connect context; all-or-none routing headers; `gameInstanceId` disconnect envelope |
| Entity Management | Scoped character, roster, item, friend, progression, resource, condition, and playable-state persistence | `{tenantId, gameInstanceId, playableStateScope}` APIs and attested gameplay calls |
| Automation Scripting | Scope- and routing-aware ingress, scheduling, durable work, handoff, dead-letter, and control-plane reads | Gameplay trigger/handoff contracts; normalized routing metadata |
| Social Groups | Live/recent friend-presence projection with realm and playable-state scope | Account-presence/friend-presence contracts |
| Game Logic | Preserve and validate complete routing/playable-state attestation for gameplay calls | Authenticated outbound gameplay gRPC |
| Logging & Admin | Tenant-guarded pointer audit, cutover, and runtime-state operator reads | REST over Game Session control-plane authority |
