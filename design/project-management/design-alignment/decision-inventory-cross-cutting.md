# Cross-Cutting Consequential Decision Inventory

Status: Complete and independently coverage/fidelity-audited. This artifact is non-normative.

## Implementation Status

`Complete` means the assigned corpus scan and independent coverage/fidelity audit are complete. Human-led review is complete in the source archive, but row text in this merged ledger continues to describe `develop`: outcomes not yet selectively imported remain review evidence rather than canonical target state. Implementation and proof remain separate.

This ledger preserves the completed read-only decision scan, including the pre-review statuses, alternatives, ADR recommendations, flags, and review questions that informed the human review archive. Those fields are historical input rather than the current application state; [Applied Review Provenance](./consequential-decision-inventory.md#applied-review-provenance) records merged outcomes. This ledger does not create, accept, supersede, or amend canonical design.

Validation scope for this inventory is recorded in the [specialized runtime inventory](./decision-inventory-specialized-runtime.md#implementation-status), whose documentation check covers the complete changed Markdown set and separately identifies runtime-proof and validator-tooling status.

The ledger contains 68 decision rows: 59 retained rows and 9 distinct evidence-backed additions. The counted source partition is exactly 22 paths. The [main consequential-decision inventory](./consequential-decision-inventory.md) remains the compact control crosswalk; reviewed dispositions and ADR references are synchronized across both inventories.

## Status And Review Notation

| Status | Meaning |
| --- | --- |
| `accepted-explicit` | Canonical design and/or an accepted ADR explicitly establishes the choice; this does not prove human consultation. |
| `accepted-implicit` | Canonical design consistently implies the choice, but adequate rationale or ADR evidence was not found. |
| `proposed/deferred` | Target design is stated but incomplete, gated, or deferred. |
| `conflicting` | Canonical sources imply incompatible current or target states. |
| `needs-human-review` | Product, security, operational, or architectural consequences require explicit discussion. |

`I/R` means importance/reversibility. `H/hard` is high importance and difficult to reverse. Source codes resolve to paths and exact visible headings in the source index. Secondary cells contain exact taxonomy IDs, not service ownership claims.

## Human Review Evidence

`accepted-explicit` records a canonical choice, not human approval. A human-review claim is authoritative only when the linked ADR or consequential-decision record names the decision owner, review date, status, disposition, and exact checked provenance entry in [Applied Review Provenance](./consequential-decision-inventory.md#applied-review-provenance). Every ADR below names the FireMUD human product and architecture owner, records `Human review status: Completed`, and names the matching checked decision key as its review source:

| Decision key | Completed ADR | Review date | Disposition |
| --- | --- | --- | --- |
| `AUTH-02` | [ADR 0021](../../architecture/decisions/adr-0021-staged-player-authentication-and-gameplay-binding.md) | 2026-07-18 | Revised |
| `AUTH-03` | [ADR 0022](../../architecture/decisions/adr-0022-account-authority-and-gameplay-session-ownership.md) | 2026-07-18 | Accepted |
| `AUTH-04` | [ADR 0023](../../architecture/decisions/adr-0023-central-route-authorization-governance.md) | 2026-07-18 | Revised |
| `AUTH-05` | [ADR 0024](../../architecture/decisions/adr-0024-trusted-gameplay-workload-delegation.md) | 2026-07-19 | Revised |
| `AUTH-06` | [ADR 0025](../../architecture/decisions/adr-0025-explicit-open-enrollment-membership.md) | 2026-07-19 | Revised |
| `AUTH-07` | [ADR 0026](../../architecture/decisions/adr-0026-global-roles-do-not-grant-gameplay-authority.md) | 2026-07-19 | Revised |
| `TENANT-01` | [ADR 0027](../../architecture/decisions/adr-0027-single-realm-admission-target.md) | 2026-07-19 | Revised |
| `ADMIT-01` | [ADR 0028](../../architecture/decisions/adr-0028-differentiated-entitlement-freshness.md) | 2026-07-19 | Revised |
| `EDGE-04` | [ADR 0029](../../architecture/decisions/adr-0029-single-use-gameplay-connect-token-carriage.md) | 2026-07-19 | Revised |
| `SESSION-07` | [ADR 0030](../../architecture/decisions/adr-0030-risk-based-active-session-revocation.md) | 2026-07-19 | Revised |
| `SESSION-09` | [ADR 0031](../../architecture/decisions/adr-0031-revocation-safe-session-token-rotation-and-logout.md) | 2026-07-19 | Revised |
| `SEC-01` | [ADR 0032](../../architecture/decisions/adr-0032-kubernetes-native-secret-delivery-without-mandatory-vault.md) | 2026-07-19 | Revised |
| `SEC-03` | [ADR 0033](../../architecture/decisions/adr-0033-public-player-facing-telnet-requires-tls.md) | 2026-07-19 | Revised |
| `SEC-05` | [ADR 0034](../../architecture/decisions/adr-0034-layered-abuse-controls-without-attacker-triggered-account-locks.md) | 2026-07-19 | Revised |
| `JWT-01` | [ADR 0035](../../architecture/decisions/adr-0035-single-record-issued-token-registry.md) | 2026-07-19 | Revised |

The later completed records currently relevant to claims in this ledger are [ADR 0041](../../architecture/decisions/adr-0041-shared-tenant-infrastructure-with-full-environment-isolation-gate.md) for `TENANT-02`, [ADR 0047](../../architecture/decisions/adr-0047-logging-admin-as-external-operator-write-ingress.md) for `SEC-04`, and [ADR 0048](../../architecture/decisions/adr-0048-durable-idempotent-operator-write-execution.md) for `ADMIN-01`; each records completion on 2026-07-19 with a revised disposition and a validator-checked matching decision row. Coupled decisions without equivalent evidence remain pending; completed ADR records must not be regressed to pending merely because the broader inventory remains open.

## Source Index

| Code | Source | Code | Source |
| --- | --- | --- | --- |
| `AR` | [architecture README](../../architecture/README.md) | `OV` | [system overview](../../architecture/system-architecture-overview.md) |
| `AU` | [authentication](../../architecture/system-architecture-authentication.md) | `MT` | [multi-tenancy](../../architecture/system-architecture-multi-tenancy.md) |
| `GW` | [gateway](../../architecture/system-architecture-gateway.md) | `GR` | [gRPC](../../architecture/system-architecture-grpc.md) |
| `ST` | [settings](../../architecture/system-architecture-settings-model.md) | `TX` | [transactions](../../architecture/system-architecture-transactions.md) |
| `TW` | [Temporal](../../architecture/system-architecture-temporal-workflows.md) | `RD` | [Redis](../../architecture/system-architecture-redis.md) |
| `SB` | [session behavior](../../architecture/system-architecture-session-behavior.md) | `RC` | [reconnection](../../architecture/system-architecture-reconnection.md) |
| `PC` | [player command model](../../architecture/system-architecture-player-command-model.md) | `IO` | [input/output/presentation](../../architecture/system-architecture-input-output-and-presentation.md) |
| `GC` | [game customization](../../architecture/system-architecture-game-customization.md) | `SC` | [scripting](../../architecture/system-architecture-scripting.md) |
| `TK` | [ticks](../../architecture/system-architecture-ticks.md) | `VR` | [versioning/runtime](../../architecture/system-architecture-versioning-runtime.md) |
| `SE` | [security](../../architecture/system-architecture-security.md) | `CI` | [CI/CD](../../architecture/system-architecture-cicd.md) |
| `BK` | [backup/recovery](../../architecture/system-architecture-backup-recovery.md) | `RM` | [service responsibility matrix](../../architecture/service-responsibility-matrix.md) |
| `WE` | [world-editing tools](../../architecture/microservices/game-design-service/world-editing-tools.md) (secondary evidence; outside the 22-source partition) | `GT` | [game templates](../../architecture/microservices/game-design-service/game-templates.md) (secondary evidence; outside the 22-source partition) |
| `GD` | [Game Design Service README](../../architecture/microservices/game-design-service/README.md) (secondary evidence; outside the 22-source partition) | | |

The first 22 source-index entries are the counted cross-cutting partition. `WE`, `GT`, and `GD` are secondary evidence only and are excluded from the partition and decision-count claims.

## Decision Ledger

### Identity, Admission, And Tenancy

| Key | Primary capability | Secondary capabilities | Question -> current choice | Status | I/R | Sources / explicit ADR | Strongest alternative | ADR recommendation |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `AUTH-01` | `AA-3.1` | `AA-2.1`, `AA-3.3`, `SF-1.2` | What identifies gameplay and tenant selection? The target gameplay binding identity is `{tenantId, playableStateNamespaceId, playableStateScope, characterId}`; `gameInstanceId` is active-runtime fence/value evidence. Stable slugs and numbered selectors are lobby-only. | `accepted-explicit` | H/hard | `AU § Contract Decisions (Normative)`; `MT § Identity & Tenant Model`; [ADR-0005](../../architecture/decisions/adr-0005-tenant-identifiers-in-gameplay-protocol.md); [ADR 0122](../../architecture/decisions/adr-0122-stable-playable-state-namespaces-for-runtime-replacement.md) (superseding/expanding authority); [Session Behavior](../../architecture/system-architecture-session-behavior.md#session-and-identity-management) | Expose raw tenant IDs, use mutable names, or accept slugs in internal contracts. | ADR 0122 supersedes and expands the earlier instance-scoped target identity; the current per-instance implementation remains a documented gap. |
| `AUTH-02` | `AA-2.1` | `PO-2.2`, `PO-2.1`, `SF-1.3`, `EA-3.1` | What is the player authentication sequence? Public `WORLDS` remains a browse-only pre-authentication mode. The target authenticated WebSocket flow is player bootstrap -> membership-aware discovery -> conditional explicit `JOIN` for first public-production entry -> character selection or creation -> single-use connect token -> WebSocket -> bare `LOGIN` -> `PLAY`. Browsers carry the token in the protected cookie; explicitly classified non-browser WebSocket clients use the dedicated handshake header. Returning members skip the public-production `JOIN` step only after the target current `ACTIVE` membership and exact reread evidence pass. Grant-backed admission requires existing durable `membershipLifecycleState=ACTIVE` membership and separately validates the applicable realm grant; a grant never creates or substitutes for membership. A nonmember must use the Account-owned membership lifecycle, such as explicit `JOIN`/`Join & Play` for eligible public production, before admission. Telnet and other non-WebSocket text clients retain credential-bearing protocol `LOGIN`. Ordinary gameplay supports `PASSWORD` or verified-email `EMAIL_OTP` without mandatory TOTP. **Current state:** connect-token issuance and text `PLAY` require existing `ACTIVE` membership and return non-actionable `JOIN_REQUIRED` for eligible missing public-production membership; explicit `JOIN` remains unimplemented, so that result is not an actionable recovery flow. | `accepted-explicit` | H/hard | [ADR 0021](../../architecture/decisions/adr-0021-staged-player-authentication-and-gameplay-binding.md); [ADR 0029](../../architecture/decisions/adr-0029-single-use-gameplay-connect-token-carriage.md); `AU § Login and Session Flow`; `GW § Tenant-Aware Edge Connect Token (Gameplay Handshake)`; `RC § When Reauthentication Is Required` | Complete WebSocket login at the handshake, replay credentials in WebSocket `LOGIN`, or require TOTP/WebAuthn for every gameplay connection. | Revised and accepted by human decision; ADR 0021 records the sequence and factors, while ADR 0029 subsequently standardizes connect-token carriage for every public non-proxy WebSocket. |
| `AUTH-03` | `AA-1.3` | `AA-1.2`, `AA-1.5`, `AA-2.3`, `SF-1.3`, `SF-2.2` | Who owns identity, revocation, entitlements, and gameplay bindings? Account owns durable account, token, revocation, membership, entitlement, and the single issued-token registry authority; JWT-presenting registry-backed control-plane, bootstrap, and admission operations perform exactly one registry lookup, while Game Session owns gameplay bindings and derives typed execution context and Gateway has only bounded edge checks. The dedicated `gameplay-connect` artifact remains outside the issued-token registry and uses its own single-use replay fence. | `accepted-explicit` | H/hard | [ADR 0022](../../architecture/decisions/adr-0022-account-authority-and-gameplay-session-ownership.md); [ADR 0035](../../architecture/decisions/adr-0035-single-record-issued-token-registry.md); `AU § Responsibility Split`; `OV § Authentication Modes and Boundaries`; `RM § Notes on Movement and Moderation Contracts` | Introduce a dedicated IAM service, make Gateway the general identity authority, or use TTL-only tokens. | Affirmed by human decision; ADR 0022 records one writer per authority and separates accepted design from revocation/session implementation gaps. |
| `AUTH-04` | `SF-1.3` | `AA-1.2`, `PO-1.1`, `PO-2.1`, `PO-4.4` | How are protected routes authorized? One machine-readable route policy governs strict token profiles, explicit tenant/cross-tenant scopes, generated completeness checks, and runtime denial of unclassified routes; services retain domain checks. | `accepted-explicit` | H/hard | [ADR 0023](../../architecture/decisions/adr-0023-central-route-authorization-governance.md); `AU § Auth Middleware Algorithm (Normative)`; `AU § Global Role to Route-Class Matrix (Normative)`; `OV § Admin Entry Points and Traffic Surfaces` | Service-local role checks, Gateway-only authorization, or a general-purpose external policy engine. | Revised and accepted by human decision; policy/proof upkeep is an accepted security tradeoff and current enforcement gaps are explicit. |
| `AUTH-05` | `SF-1.3` | `SF-1.2`, `SF-1.1`, `GR-1.1`, `PO-3.2` | How is internal player delegation trusted? Concrete mTLS workload identity and exact method caller allowlists authorize the trusted gameplay service; unsigned typed `PlayerExecutionContext` carries validated scope, while mutations retain operation-specific idempotency. | `accepted-explicit` | H/hard | [ADR 0024](../../architecture/decisions/adr-0024-trusted-gameplay-workload-delegation.md); `AU § Gameplay Player Execution Context Contract (Normative)`; `SE § Gameplay Workload Trust`; `GR § TLS Requirements` | Per-RPC signed/replay-guarded attestations, signed attestations without replay storage, or generic backend JWTs carrying player identity. | Revised and accepted by human decision after two-sided adversarial review; the current first-party trust domain explicitly accepts allowlisted-intermediary impersonation risk to avoid universal hot-path attestation cost. |
| `AUTH-06` | `AA-1.2` | `AA-1.1`, `AA-3.2`, `AA-2.1`, `EA-3.1` | What does first public-production join mean? **Target:** one explicit `JOIN`/`Join & Play` action creates the durable Account-owned player membership used for the player's game library and future return; token issuance and `PLAY` never create it implicitly. **Current state:** connect-token issuance and text `PLAY` require existing `membershipLifecycleState=ACTIVE` membership and return a non-actionable `JOIN_REQUIRED` result for eligible missing or `INACTIVE` public-production membership. The obsolete implicit membership-writer seam has been removed, explicit `JOIN` is not implemented, and the connect-token membership-authority-generation reread remains a gap. Private/playtest admission currently retains `WORLD_ACCESS_DENIED` for missing/non-`ACTIVE` membership; target behavior uses `NON_PUBLIC_ENROLLMENT_REQUIRED` and requires existing `ACTIVE` membership plus the applicable grant. | `accepted-explicit` | H/hard | [ADR 0025](../../architecture/decisions/adr-0025-explicit-open-enrollment-membership.md); `AU § Login and Session Flow`; `MT § Account-to-Game Relationships` | Implicit membership during connect/`PLAY`, invitation/approval for every join, or ephemeral visitor gameplay without membership. | Revised and accepted by human decision; durable membership is a product feature, with explicit intent, transactional audit/outbox, leave lifecycle, and abuse controls. |
| `AUTH-07` | `AA-1.2` | `AA-2.1`, `PO-1.1`, `PO-1.2`, `EA-3.3` | Can global roles grant gameplay authority? No: global roles cannot admit or elevate gameplay, staff join as ordinary players unless given explicit tenant-scoped gameplay capabilities, and the target has no impersonation or hidden observation. | `accepted-explicit` | H/hard | [ADR 0026](../../architecture/decisions/adr-0026-global-roles-do-not-grant-gameplay-authority.md); `AU § Role Model`; `AU § Global Role to Route-Class Matrix (Normative)`; `OV § Ban and Moderation Taxonomy (Canonical)` | Global-role gameplay bypass, audited time-bounded impersonation, or read-only live observation. | Revised and accepted by human decision; current global `platformAdmin` to in-game `ADMIN` classification is implementation drift, while support remains on purpose-built control-plane and diagnostic surfaces. |
| `TENANT-01` | `AA-3.3` | `AA-3.1`, `AA-3.2`, `AR-3.2`, `GR-2.1`, `GR-1.1` | How is a realm admitted? A Game Session-owned route is `OPEN` on exactly one instance or explicitly `CLOSED`; atomic pointer versions govern new/renewed bindings, while active source sessions end through bounded drain rather than per-action revalidation. | `accepted-explicit` | H/hard | [ADR 0027](../../architecture/decisions/adr-0027-single-realm-admission-target.md); `MT § Realm Catalog and Admission-Pointer Contract`; `VR § Realm Routing Contract For Player-Addressable Realms`; `OV § Design-Time vs Runtime World Data` | An instance-placement pool behind one realm, mutable latest-version lookup, or client-selected instance. | Revised and accepted by human decision; one shared world scales inside one instance, while explicit realms represent separate worlds and implementation must converge on tenant-qualified atomic CAS and cutover bookkeeping. |
| `TENANT-02` | `AA-1.5` | `PO-3.1`, `PO-3.2`, `SF-2.2`, `GR-1.3` | Shared services, PostgreSQL, Coordination Redis, Cache/Rate-Limit Redis, and cluster capacity are the only supported normal multi-tenant topology; logical tenant scoping, authorization, quotas, and service ownership provide isolation. The environment-wide incident and recovery blast radius is accepted. A demonstrated future hard-isolation requirement uses a separately reviewed complete environment, not a hybrid selectively dedicated tenant data plane. | `accepted-explicit` | H/med | [ADR 0041](../../architecture/decisions/adr-0041-shared-tenant-infrastructure-with-full-environment-isolation-gate.md); `MT § Data Separation per Service`; `MT § Tenant Configuration & Scaling`; `OV § Multi-Tenancy Enforcement` | Dedicated tenant clusters, databases, Redis partitions, or a hybrid shared/dedicated routing tier. | Human review completed 2026-07-19; `revised`. |
| `TENANT-03` | `AR-3.4` | `AA-3.2`, `GR-2.1`, `GR-3.1`, `GR-3.2`, `AA-1.2`, `AR-3.3` | What does an isolated playtest fork copy and can it merge back? It copies local progression/inventory/roster, isolates runtime state, and has no automatic merge-back. | `needs-human-review` | H/hard | `MT § Realm State Model`; `VR § Fork-Snapshot Boundary For Playtest Realms` | Shared test state with reset, or explicit export/merge workflow. | Yes; player expectations and data-loss semantics. |
| `ADMIT-01` | `AA-1.5` | `AA-3.2`, `AA-2.1`, `SF-1.3`, `PO-4.2` | How fresh must entitlements be? New commitments require Account authority no older than 15 seconds; the same resumable session or non-expanding recovery may use safe positive last-known-good state for at most five minutes. | `accepted-explicit` | H/med | [ADR 0028](../../architecture/decisions/adr-0028-differentiated-entitlement-freshness.md); `AU § Tenant Authorization Contract`; `MT § Quota Enforcement Responsibilities` | Universal 15-second fail-closed, a permissive long-lived cache, or replicated runtime entitlement authority. | Revised and accepted by human decision; hard denial and sequence uncertainty never use grace, and the current response, freshness, event, cache, quota, and lifecycle behavior remain implementation drift. |

### Edge, Sessions, And Delivery

| Key | Primary capability | Secondary capabilities | Question -> current choice | Status | I/R | Sources / explicit ADR | Strongest alternative | ADR recommendation |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `EDGE-01` | `PO-2.1` | `AA-3.3`, `GR-1.1`, `PO-2.4`, `AA-2.2` | Does the edge own gameplay shard routing? No; `/ws/game/**` is stable and shard/lease routing is internal to Game Session. | `accepted-explicit` | H/hard | `GW § Gameplay Sharding (Routing Boundary)`; `OV § Architecture Decisions (Canonical)`; [ADR-0007](../../architecture/decisions/adr-0007-edge-sharding-and-close-taxonomy.md) | Client-carried routing keys with explicit edge shard selection and reroute close semantics. | Existing ADR-0007 settles this. |
| `EDGE-02` | `PO-3.1` | `GR-1.3`, `AA-3.3`, `PO-2.1`, `SF-2.2`, `PO-3.4` | How far does gameplay span clusters? One Kubernetes cluster per deployment; scale is in-cluster lease ownership. | `accepted-explicit` | H/hard | `OV § Architecture Decisions (Canonical)`; [ADR-0008](../../architecture/decisions/adr-0008-multi-cluster-gameplay-sharding-scope.md) | Multi-cluster gameplay with cross-cluster routing, trust, data, and failover. | Existing ADR-0008 defines an adoption gate. |
| `EDGE-03` | `SF-1.3` | `PO-2.1`, `PO-2.2`, `PO-3.2` | How does Gateway trust TCP Proxy headers? Production canonical URI SAN is the SPIFFE service identity; literal and percent-encoded ASCII unreserved path bytes are accepted with hex-case-insensitive spelling, while raw or decoded dot segments are rejected. Headers are trusted only inside the authenticated bridge; DNS SAN is not a fallback in player-facing environments, and certificate-instance approval supplements rather than replaces URI-SAN allowlisting. | `accepted-explicit` | H/hard | `GW § TCP Proxy → Gateway Authentication`; `SE § Cross-Service Trust`; [ADR-0010](../../architecture/decisions/adr-0010-tcp-proxy-identity-canonicalization.md); [ADR-0038](../../architecture/decisions/adr-0038-explicit-jwt-profiles-and-mtls-workload-identity.md) | DNS-only trust, public header trust, or certificate fingerprint pinning. | Existing ADR-0010 and ADR-0038 settle the production trust order and URI-SAN normalization. |
| `EDGE-04` | `PO-2.1` | `SF-1.3`, `AA-2.1`, `PO-2.2`, `PO-2.4` | What is the gameplay connect-token carrier and replay contract? Gateway accepts exactly one non-empty, single-valued dedicated header or cookie carrier, rejects query and ambiguous transport, enforces a 30-second hard maximum, and atomically consumes the token in shared replay state through its full acceptance window; player-facing uncertainty fails closed and only the positively authenticated TCP Proxy path bypasses the token. | `accepted-explicit` | H/hard | [ADR 0029](../../architecture/decisions/adr-0029-single-use-gameplay-connect-token-carriage.md); `GW § Tenant-Aware Edge Connect Token (Gameplay Handshake)`; `GW § Gameplay WebSocket Route` | Accept query parameters, choose carrier precedence, use stateless or per-pod replay checks, or allow uncertain replay outcomes. | Revised and accepted by human decision; exact lifetime, ambiguity, consume/retry, raw-token stripping, and proxy-exception boundaries are recorded, with remaining implementation and proof gaps explicit. |
| `EDGE-05` | `PO-2.4` | `PO-2.2`, `GR-1.1`, `AA-2.2`, `PO-4.2` | Gateway owns one bounded external close taxonomy. Terminal `logout`, displaced-controller `session_replaced`, and planned `service_restart` are distinct top-level outcomes; optional subreasons never determine lifecycle. Idle, policy, internal, and backend-unavailable outcomes retain their bounded meanings, hard no-frame loss uses the documented observation-specific retry fallback, and no client-visible `reroute` exists. | `accepted-explicit` | H/med | [ADR 0131](../../architecture/decisions/adr-0131-lifecycle-distinct-gameplay-close-taxonomy.md); `GW § WebSocket Liveness and Idle Timeouts`; `GW § Canonical Close Translation Matrix`; `GW § Backend-Unavailable Grace Window`; `GW § Gateway Restart Semantics` | Collapse lifecycle outcomes into `logout`, require optional subreasons for correctness, let downstream services choose codes, or expose `reroute`. | Human review completed 2026-07-20; `revised`. Current code and proof implement the displaced `logout/gateway_restart` mapping; taxonomy convergence and existing elapsed-cutoff, hysteresis, ping/pong, and stalled-input obligations remain gaps. |
| `EDGE-06` | `PO-2.1` | `PO-3.1`, `SF-2.2`, `PO-1.4`, `AA-3.3` | When may dynamic Gateway route overrides be used? Only explicitly classified dev/test runtimes may apply bounded ephemeral overrides; player-facing routing has one version-controlled declarative authority and fails startup if mutation is enabled. Production runtime mutation requires a separate future decision. | `accepted-explicit` | H/hard | [ADR 0018](../../architecture/decisions/adr-0018-declarative-production-gateway-routes.md); `GW § Dynamic Route Override Lifecycle`; `GW § Management Plane Security` | Keep production mutation deferred behind persistence/convergence/audit gates, build a durable route control plane now, or remove dev/test mutation too. | Revised by human decision: declarative production authority with isolated bounded dev/test overrides; persistence and convergence do not automatically promote the API. |
| `SESSION-01` | `GR-1.1` | `GR-1.3`, `AA-2.1`, `SF-1.2`, `SF-2.3`, `PO-2.2` | Who owns a live gameplay connection versus region execution? Session front-end owns socket/I/O; lease owner mutates region state; forwarding is fenced by epoch and sequence. | `accepted-explicit` | H/hard | `OV § Scaling Model`; `GW § Gameplay Sharding (Routing Boundary)`; [ADR-0011](../../architecture/decisions/adr-0011-gameplay-session-front-end-and-region-execution.md) | Co-locate socket and execution through affinity/reconnect, or use a dedicated session router. | Existing ADR-0011 settles the split. |
| `SESSION-02` | `AA-2.3` | `AA-2.1`, `AA-2.2`, `EA-3.1`, `SF-2.2`, `AR-3.3` | Exactly one authoritative controller exists per `{tenantId, playableStateNamespaceId, characterId}`. Authorized `PLAY` performs an atomic monotonic takeover; old input is fenced while previously admitted durable work retains its identity. Runtime replacement cannot create a second controller, while isolated playtests remain independent. | `accepted-explicit` | H/med | [ADR 0132](../../architecture/decisions/adr-0132-namespace-scoped-single-character-controller.md); `SB § Multi-Client Behavior and Session Takeover`; `RC § Multi-Client and Session Takeover`; `AU § Session Lifecycle and Rebinding` | Instance-scoped uniqueness, no runtime overlap, concurrent multi-writer sessions, or mandatory takeover confirmation. | Human review completed 2026-07-20; `revised`. Namespace-aware indexes, atomic ABA-safe transfer, old-input fencing, and lifecycle proof remain implementation gaps. |
| `SESSION-03` | `AA-2.2` | `AA-2.1`, `EA-1.3`, `PO-2.2`, `GR-1.4`, `SF-2.3` | Client-visible edge loss requires a fresh transport, token where applicable, `LOGIN`, and `PLAY`; successful bounded internal rebind does not. No client input, frame, byte, unsent output, or MCP state is replayed. Already admitted durable work continues under its existing identity, and authorized reconnect receives bounded semantic context followed by fresh `LOOK` and exactly one reconnect prompt only when both effective `firemud.presentation.prompt.enabled` and `firemud.presentation.prompt.emit-after-reconnect-restore` are enabled; if either is disabled, it emits zero reconnect prompts. | `accepted-explicit` | H/med | [ADR 0133](../../architecture/decisions/adr-0133-fresh-edge-reconnect-without-client-input-replay.md); `RC § Client Reconnection Behaviour`; `RC § Canonical durable resume-context policy`; `IO § Canonical resume-context model` | Exact acknowledged transport/command resumption, expose every internal worker loss, or fresh entry with no context. | Human review completed 2026-07-20; `revised`. Full elapsed rebind proof, resume-window enforcement, presence convergence, stalled-input handling, and explicit-logout replay cleanup remain gaps. |
| `SESSION-04` | `GR-1.4` | `AA-2.2`, `PO-4.2`, `PO-2.4`, `SF-2.3` | Ordinary non-edge failures use bounded invisible recovery when the edge socket, healthy replacement capacity, and shared authority remain available. Recovery targets 10 seconds and stops after 30 seconds with `1013/backend_unavailable`; edge transport loss, terminal policy, unsafe ambiguity, and unavailable authority remain explicit boundaries. | `accepted-explicit` | H/hard | `OV § Reconnection Strategy`; `RC § Bounded Non-Edge Restart Recovery`; `RC § Resume vs Reload Scenarios`; [ADR 0013](../../architecture/decisions/adr-0013-bounded-invisible-non-edge-restart-recovery.md) | Treat attached Game Session loss as an immediate visible reconnect boundary, or retain an unbounded aspiration without a testable guarantee. | ADR 0013 records the revised human decision. Complete authenticated real-Game-Session replacement, lifecycle, presence, authority, buffer, and elapsed-time proof remains implementation debt. |
| `SESSION-05` | `PO-2.4` | `PO-2.1`, `PO-2.2`, `AA-2.2`, `GR-1.1`, `PO-4.1` | How are edge failures classified? Standard close taxonomy only; sustained backend loss is `backend_unavailable`; no `reroute` category. | `accepted-explicit` | H/med | `GW § Gameplay Sharding (Routing Boundary)`; `GW § Backend-Unavailable Grace Window`; `RC § Backend-Unavailable Scenarios`; [ADR-0007](../../architecture/decisions/adr-0007-edge-sharding-and-close-taxonomy.md), superseding [ADR-0004](../../architecture/decisions/adr-0004-gameplay-reroute-vs-backend-unavailable.md) and [ADR-0006](../../architecture/decisions/adr-0006-gameplay-shard-routing-key-transport.md) | Dedicated fast-retry `reroute` close reason. | Existing ADR-0007 settles it; historical ADRs must remain visibly non-current. |
| `SESSION-06` | `SF-1.1` | `SF-2.3`, `GR-1.2`, `AA-2.2`, `PO-2.4` | What delivery guarantee is exposed? Per-connection commands are FIFO/at-most-once; internal events are at-least-once with durable dedupe; clients use fire-and-forget commands. | `accepted-explicit` | H/hard | [ADR 0062](../../architecture/decisions/adr-0062-layered-gameplay-command-delivery-semantics.md); `GR § Event and Streaming Semantics`; `GR § Error Handling`; `RC § Client Reconnection Behaviour` | Client idempotency keys and exactly-once command completion. | Human review revised and accepted the layered edge/trusted-acceptance/internal-delivery contract; implementation and focused proof remain. |
| `SESSION-07` | `AA-1.4` | `AA-1.2`, `AA-1.5`, `AA-2.3`, `GR-1.1`, `PO-1.2` | What happens after role, membership, billing, or security changes? Harmless roles refresh; authority/security loss revokes active gameplay; `past_due` remains playable; `grace` preserves current/resumable bindings but blocks new commitments; `suspended`/`canceled` closes gameplay and drains instances. Missed-event exposure is bounded to 60 seconds without per-command authority reads. | `accepted-explicit` | H/hard | [ADR 0030](../../architecture/decisions/adr-0030-risk-based-active-session-revocation.md); `SB § Mid-Session Role Updates`; `SB § Billing State and Revocation Rules`; `AU § Tenant Authorization Contract` | Check only at reconnect/expiry, kick on every role/billing change, allow sessions through every billing state, or read authority generations on every command. | Revised and accepted by human decision; state-specific admission, private-grant revocation, durable delivery, active indexes, and bounded reconciliation are explicit while implementation remains incomplete. |
| `SESSION-09` | `AA-2.3` | `AA-2.2`, `AA-1.3`, `SF-1.3`, `GR-1.1` | What are session-token refresh and logout-all semantics? Game Session rotates its private `game-session-account-delegation` JWT for Account before expiry using the still-valid token-lineage generation as refresh authority, atomically binds the replacement, and cannot cross an applicable Account-owned authority-generation advance; per-token logout affects one device, while account-authority-generation logout-all terminates every account token and gameplay binding. | `accepted-explicit` | H/hard | [ADR 0031](../../architecture/decisions/adr-0031-revocation-safe-session-token-rotation-and-logout.md); `SB § Active Session Token Refresh (Required)`; `SB § Control-Plane Logout`; `SB § Control-Plane Session UX Expectations` | Refresh only at expiry, use workload identity alone to mint replacements, delete every token for logout-all, or make per-token logout invalidate all devices. | Revised and accepted by human decision; refresh generation, expiry scheduling, atomic overlap, logout scopes, gameplay termination, and distinct audit semantics are explicit while implementation remains incomplete. |

### Gameplay Runtime, Persistence, And Contracts

| Key | Primary capability | Secondary capabilities | Question -> current choice | Status | I/R | Sources / explicit ADR | Strongest alternative | ADR recommendation |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `TICK-01` | `GR-1.2` | `GR-1.3`, `GR-1.1`, `GR-4.1`, `AS-1.4` | What is the gameplay clock and fairness unit? Independently advancing regional hybrid ticks use separate deterministic actor-action and passive/inbound-effect lanes. The actor-action lane selects at most one root intentional actor action originated by an eligible entity per tick; passive/inbound effects are separately bounded and do not consume the target entity's actor-action slot. | `accepted-explicit` | H/hard | [ADR 0051](../../architecture/decisions/adr-0051-separate-actor-action-and-effect-lanes.md); `TK § Hybrid Tick Model`; `TK § Per-Command Execution Phases`; `OV § Game Loop / Tick Model` | One universal entity work slot, a real-time event loop, global ticks, or unrestricted per-entity concurrency. | Human review revised the universal work slot into actor-action and effect lanes; implementation and focused proof remain. |
| `TICK-02` | `GR-1.3` | `GR-1.2`, `GR-1.4`, `SF-2.2`, `SF-2.3`, `PO-4.2` | How is tick authority fenced? An opaque Redis liveness token proves ephemeral lease possession; PostgreSQL independently installs a fresh never-reused durable `executorFence` by expected-epoch/prior-state CAS and persists only a non-secret token correlation; the same Redis token is revalidated before staging; Redis mutations compare token/epoch and durable writes compare fence/epoch. Renewal preserves the fence, reset advances epoch before Redis clear, and either authority outage stops new progress. | `accepted-explicit` | H/hard | [ADR 0052](../../architecture/decisions/adr-0052-redis-liveness-lease-with-durable-executor-fence.md); `TK § Region Authority and Tick Executor`; `TK § Tick Commit Definition (Heartbeat Watermark)`; `RD § Redis Coordination Invariants` | PostgreSQL-only expiring leases, Redis-only leases, a consensus service, or global coordinator. | Human review revised and accepted the dual-store install-and-revalidate protocol; true region partitioning and failure proof remain. |
| `TICK-03` | `SF-2.4` | `GR-1.2`, `SF-2.3`, `GR-4.1`, `GR-2.2`, `GR-3.2` | Are gameplay commands globally atomic? Local transactions plus stable `EffectId`-guarded at-least-once effects remain the default, but every command declares required/optional effects, permitted terminal combinations, and whether `PARTIAL` is intentional before execution; `SUCCESS` requires every required effect to be durably applied or have durable, evidence-qualified stored replay/no-op proof; unresolved or merely replayable required work remains `PENDING`/reconciliation-required. Invariant-sensitive commands use one authoritative local transaction or durable reservation/escrow; recovery records `APPLIED`/`ABANDONED` only where the existing durable evidence policy permits, never by bulk terminalization. | `accepted-explicit` | H/hard | [ADR 0053](../../architecture/decisions/adr-0053-command-atomicity-by-invariant-class.md); `TX § In-Game Command Transactions (No Sagas Needed)`; `TX § When Gameplay Commands Must Not Depend on Global Atomicity` | Distributed transaction, synchronous two-phase commit, or compensating rollback. | Human review revised and accepted invariant-class atomicity; command-family classification and proof remain. |
| `TICK-04` | `SF-2.3` | `GR-2.2`, `GR-3.2`, `GR-4.1`, `GR-1.4`, `SF-1.2` | How are effects deduplicated and spatial ownership divided? World owns topology/location/occupancy/ambient state; Entity owns inventory/equipment/containment/ground; participant guard uniqueness is the root `EffectId` plus typed operation plus target aggregate. The immutable `requestDigest` is persisted and validated separately as guard metadata—changed metadata fails closed—but it is never part of uniqueness. Under ADR 0059, Game Logic propagates the Game Session-allocated causal floor and composes returned same-scope/epoch served-through proofs with opaque component versions. | `accepted-explicit` | H/hard | [ADR 0054](../../architecture/decisions/adr-0054-split-spatial-authority-with-causal-read-composition.md); [ADR 0059](../../architecture/decisions/adr-0059-causal-floor-cross-service-presentation-reads.md); [ADR 0060](../../architecture/decisions/adr-0060-world-owned-ambient-facts-and-logic-owned-consequences.md); [identifier glossary](../../architecture/system-architecture-identifier-glossary.md) § `Cross-Service Causal-Read Fence Identity`; `TX § Tick Effects Are At-Least-Once: Idempotency Is Mandatory`; `TX § Spatial Effects: Location vs Containment (World ↔ Entity)`; `OV § Canonical Room Runtime Contract`; `TK § Tick Effect Identity and Idempotency Contract` | Shared room transaction or authoritative monolithic gameplay state. | Human review revised and accepted split spatial authority under ADR 0054 and causal-floor composition under ADR 0059; operation-bound guards, mutation preconditions, and proof remain. |
| `TICK-05` | `SF-2.4` | `SF-2.3`, `GR-1.2`, `AS-1.5`, `PO-1.1` | Where do side effects and workflows run? Gameplay uses tick reconciliation; simple asynchronous delivery uses an outbox; bounded caller/worker-owned orchestration may use a local saga; restart-independent continuation, durable waits/signals, and operator-managed in-flight lifecycle use Temporal. | `accepted-explicit` | H/med | `TX § Tick-Adjacent Workflows (Outbox Boundary)`; `TX § Saga vs Temporal Boundary`; `TW § Canonical Usage Boundary` | Put every durable operation in Temporal or rebuild durable continuation from local sagas and outbox workers. | Human review accepted the three-way substrate boundary with a mandatory adopter test; implementation/proof remain adopter-specific and no new ADR is required while gameplay remains outside Temporal. |
| `TICK-06` | `GR-2.1` | `GR-1.3`, `GR-1.4`, `SF-2.3`, `AA-3.3`, `PO-4.2` | How do cross-region effects and topology changes work? Durable asynchronous follow-ups use static live topology; maintenance split/merge freezes producers, drains and reconciles, bumps epochs, rebuilds coordination, and reopens only after explicit terminalization and health validation. | `accepted-explicit` | H/hard | [ADR 0055](../../architecture/decisions/adr-0055-durable-cross-region-effects-with-static-live-topology.md); `TK § Tick Regions and Parallel Execution`; `TK § Topology Changes (Split/Merge) Protocol (Required Invariants)`; `TX § Reconciliation Owner of Record (Spatial/Ambient Effects)` | Global locks, synchronous cross-region ticks, or automatic live topology migration. | Human review revised and accepted the static-topology/maintenance-cutover boundary; remote follow-up and migration proof remain. |
| `TICK-07` | `AS-1.4` | `GR-1.2`, `GR-2.3`, `AS-1.5`, `SF-1.4` | How are timers recovered? Each timer declares its clock and recovery class; durable correctness work preserves identity, recurring schedules choose `SKIP_MISSED` or one bounded `COALESCE_ONE`, and advisory work may drop under its declared budget. | `accepted-explicit` | H/med | [ADR 0072](../../architecture/decisions/adr-0072-class-specific-timer-durability-and-recovery.md); `TK § Timers and Time Scaling`; `TK § Scheduler Recovery Semantics`; `SC § Validation & Admission Pipeline` | Replay every missed timer or treat all timers as durable commands. | Human review revised and accepted class-specific timer durability/recovery; scheduler implementation and proof remain partial. |
| `HOTPATH-01` | `GR-1.2` | `GR-2.2`, `GR-3.2`, `SF-2.3`, `PO-4.2` | How is synchronous gameplay fan-out bounded? Each operation has one fan-out owner and at most two authoritative downstream participants across the transitive critical path; movement and LOOK use the ordinary rule, with measured stage budgets and explicit fail-closed outcomes. | `accepted-explicit` | H/hard | [ADR 0056](../../architecture/decisions/adr-0056-one-hot-path-fan-out-owner.md); `OV § Gameplay Hot Path Policy (Canonical)`; `OV § Command Fan-Out, Orchestration, and Scaling`; `RM § Notes on Movement and Moderation Contracts` | Allow recursive fan-out, add participants without an architecture decision, or return partial success after a budget/fence failure. | Human review revised and accepted the one-owner/two-participant rule; command-family measurements and focused proof remain. |
| `RECON-01` | `GR-1.4` | `SF-2.3`, `GR-2.3`, `GR-3.2`, `PO-4.2` | Who owns spatial/ambient reconciliation? Game Session owns one durable row per logical effect, the frozen participant set, owner outcomes, retry state, operator disposition, and cross-owner result; isolated workers preserve normal tick capacity while World and Entity retain write authority. | `accepted-explicit` | H/hard | [ADR 0057](../../architecture/decisions/adr-0057-game-session-owned-reconciliation-with-isolated-workers.md); `TX § Reconciliation Owner of Record (Spatial/Ambient Effects)`; `TX § Spatial Effects: Location vs Containment (World ↔ Entity)` | Let each domain service schedule independent retries, compensate with new effect identities, or rely on Redis/in-memory staging for convergence. | Human review revised and accepted the Game Session reconciliation owner and isolated-worker boundary; backlog, retention, and crash/outage proof remain. |
| `REDIS-01` | `SF-2.2` | `SF-2.1`, `SF-2.3`, `PO-3.4`, `GR-1.4`, `PO-4.2` | Is Redis durable authority? No; PostgreSQL and owning stores remain durable authority, and Redis loss uses class-specific rebuild, replay, pause, or terminalization outcomes. The operational write-window metric is evidence-calibrated rather than a tick-derived product RPO. | `accepted-explicit` | H/hard | [ADR 0058](../../architecture/decisions/adr-0058-class-specific-redis-loss-outcomes.md); `RD § Log of Record vs Coordination Buffer`; `RD § Redis Availability, Consistency, and Safety Guarantees`; `BK § Redis Persistence` | Redis as durable gameplay authority, replicated event log, or synchronous DB writes for every transition. | Human review revised and accepted class-specific Redis-loss outcomes; measured loss-window evidence and recovery proof remain. |
| `REDIS-02` | `SF-2.2` | `GR-1.3`, `AA-2.2`, `AS-1.5`, `PO-3.2`, `PO-4.4` | Who may write Redis and how is it sharded? ADR-0009 owner boundaries, hash-tagged region/session keyspaces, no cross-keyspace scripts, separate Coordination and Cache Redis except ephemeral environments. | `accepted-explicit` | H/hard | `RD § Key Naming and Shard Discipline`; `RD § Session and Region-Binding Contract`; [ADR-0009](../../architecture/decisions/adr-0009-coordination-redis-ownership-boundary.md) | Shared unrestricted Redis helpers or cross-region Lua transactions. | Existing ADR-0009 settles ownership; separate-instance policy still merits infrastructure review. |
| `REDIS-03` | `SF-2.2` | `SF-1.1`, `PO-4.1`, `AS-1.5`, `GR-1.4` | Is there an external event bus? No general broker initially. Durable flows use transactional outboxes, durable per-consumer state, idempotent workers, and reconstruction APIs; Redis carries only disposable wakes, pointers, and observability or coordination hints. Broker adoption requires measured gates and a separate decision. | `accepted-explicit` | H/med | [ADR 0083](../../architecture/decisions/adr-0083-no-general-event-broker-until-measured-adoption-gates.md); `RD § Redis as a Volatile State Layer`; `OV § Asynchronous and Event Flows`; `GR § Event and Streaming Semantics` | Adopt Kafka/NATS/Pulsar now, or use Redis as the only durable integration-event transport. | Human review revised and accepted the no-general-broker boundary with durable outbox and consumer-state obligations; repository-wide implementation and proof remain incomplete. |
| `GRPC-01` | `SF-1.1` | `SF-1.5`, `GR-1.1`, `PO-4.1`, `PO-4.2` | How are failures represented? Expected domain decisions after successful evaluation use transport `OK` plus a typed result union. Request/auth/precondition/exhaustion and infrastructure failures that prevent a domain result use canonical non-OK status with structured details; ambiguous mutations retain stable identity and status reconciliation. | `accepted-explicit` | H/hard | [ADR 0092](../../architecture/decisions/adr-0092-grpc-status-and-typed-domain-outcome-boundary.md); `GR § Error Handling`; `AR § Runtime Architecture` | Put every failure in transport-OK `ErrorDetail`, or put every non-success domain outcome in gRPC status. | `revised` on 2026-07-20: classify status by whether the RPC produced its declared domain result, retain typed business outcomes, and preserve standard request/infrastructure status semantics. |
| `GRPC-02` | `SF-1.1` | `SF-2.3`, `PO-4.3`, `PO-3.1`, `AS-1.5` | How do contracts evolve? Versioned packages/paths, Buf breaking checks, additive fields, reserved removals, and at-least-once streams with stable dedupe keys. | `accepted-explicit` | H/hard | `GR § Versioning Strategy`; `GR § Schema Evolution Rules`; `GR § Event and Streaming Semantics` | Breaking replacement or exactly-once streams. | No ADR for normal schema hygiene; ADR required for breaking or exactly-once changes. |

### Commands, Presentation, Authoring, And Automation

| Key | Primary capability | Secondary capabilities | Question -> current choice | Status | I/R | Sources / explicit ADR | Strongest alternative | ADR recommendation |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `CMD-01` | `EA-1.1` | `AR-1.1`, `GR-4.1`, `EA-3.4`, `SF-1.1` | What is a player command? Transport-independent normalized declarations with identity, aliases, stage, capability, ownership, history, and typed effects. | `accepted-explicit` | M/med | `PC § Command Model`; `PC § Player Stages`; `IO § Input Model` | Per-transport command classes or free-form handler registration. | No ADR; command-model document is the appropriate contract. |
| `CMD-02` | `GR-4.1` | `AR-1.1`, `AR-1.5`, `AR-3.3`, `SF-1.1`, `AS-1.2` | Authored commands use immutable typed declarations; Game Logic resolves bounded target plans and typed outcomes, Entity owns actor/effect mutation, and Game Session admits commands into ticks. A future grammar may compose registered primitives but cannot introduce arbitrary hot-path script execution or ownerless facts/mutations. When an authored path is backed by a linked plugin, its bundle remains behind the immutable signed/attested admission boundary in `CONTENT-03`; this command decision does not broaden that boundary or authorize arbitrary hot-path plugin execution. | `accepted-explicit` | H/hard | [ADR 0112](../../architecture/decisions/adr-0112-typed-bounded-gameplay-effect-extension.md); `PC § Typed Execution Effects`; `PC § Versioned Declaration Lifecycle`; `CONTENT-03` (plugin admission and signing boundary) | Arbitrary plugin/script execution, per-action signed internal authorization, or platform-written commands only. | Human review completed 2026-07-20; `accepted`; broader implementation/proof remains incomplete. |
| `CMD-03` | `EA-1.2` | `EA-1.1`, `EA-3.1`, `PO-2.2`, `SF-1.1`, `SF-1.2` | Domains return typed semantic outcomes and Game Session applies presentation policy through a compact versioned player-output envelope with a mandatory text projection. Supported first-party schemas require explicit versioning; a general presentation tree and class-per-feature expansion are not the current target. | `accepted-explicit` | H/med | [ADR 0135](../../architecture/decisions/adr-0135-compact-versioned-player-output-and-late-rendering.md); `IO § Output Model`; `IO § Late Rendering`; `IO § Client Surface Expectations` | Domain-rendered text, client-specific domain responses, or a general presentation-document platform. | Human review completed 2026-07-20; `revised`. The late-rendering seam is substantial, but the browser consumer and supported schema lifecycle are incomplete. Same-fence LOOK remains required; the current static scope-token comparison is not temporal snapshot proof. |
| `CMD-04` | `EA-1.3` | `AA-2.2`, `EA-1.2`, `SF-2.1`, `SF-2.3` | Reconnect receives namespace-scoped bounded durable semantic recent context, not delivery acknowledgement, exact transport replay, unsent output, command history, or an archive. Current authority gates context restoration; context precedes fresh `LOOK` and emits exactly one reconnect prompt only when both effective `firemud.presentation.prompt.enabled` and `firemud.presentation.prompt.emit-after-reconnect-restore` are enabled; if either is disabled, it emits zero reconnect prompts. The hard byte ceiling has no oversized-entry exception. | `accepted-explicit` | H/med | [ADR 0134](../../architecture/decisions/adr-0134-bounded-durable-semantic-reconnect-context.md); `IO § Canonical resume-context model`; `IO § Resume-context bounds`; `RC § Canonical durable resume-context policy` | Exact delivery-acknowledged replay, no retained context, complete/player-selected archive, or runtime-instance-scoped context. | Human review completed 2026-07-20; `revised`. Durable persistence is substantial, but namespace migration and explicit-logout/authority cleanup remain implementation gaps; strict oversized-entry omission already has partial proof. |
| `CMD-05` | `EA-1.2` | `EA-3.1`, `AR-1.1`, `EA-3.4` | Localization is a future product capability with only a minimal source-locale and stored-variant boundary now. Fallback is exact tag, explicit base-language variant, then source; live gameplay translation and default player-speech translation are excluded. A concrete future use case begins with research into then-current local and remote tooling rather than a preselected provider. | `accepted-explicit` | M/med | [ADR 0136](../../architecture/decisions/adr-0136-future-compatible-localization-boundary.md); `IO § Localization and translation`; `IO § Late Rendering` | Select a localization platform now, use live runtime translation, arbitrary regional fallback, or remove locale metadata until later. | Human review completed 2026-07-20; `revised`. Existing keyed output and room variants are partial technical groundwork, not a current localization product; deterministic explicit-base fallback and future creator workflow/tooling remain gaps. |
| `CMD-06` | `EA-1.1` | `AR-2.2`, `EA-1.3`, `AA-2.1` | Are capabilities and command history separate? Yes; capability gates affect admission/help, reserved names cannot shadow, and history is distinct from transcript. | `accepted-implicit` | M/easy | `PC § Reserved Names And Extensions`; `PC § Command History`; `ST § Canonical Domains` | UI-only feature flags or one combined history/transcript model. | No ADR unless capability defaults become monetized or tenant policy. |
| `CONTENT-01` | `AR-1.5` | `AR-1.4`, `AR-3.2`, `PO-3.3`, `GR-2.1`, `AA-3.3` | Who publishes content and assets? Game Design owns version metadata, publish coordination, and release attestation while domain services retain their versioned-template authority. Runtime uses the pinned release's immutable domain data and object/CDN artifacts, with actual-content digests for every required participant and exported artifact; only explicitly optional presentation assets may use declared defaults. | `accepted-explicit` | H/hard | [ADR 0093](../../architecture/decisions/adr-0093-game-design-coordinated-digest-attested-content-publication.md); `GC § Theme and Branding`; `VR § Game Version Publishing`; `VR § Cross-Asset Version Cohesion` | Runtime queries Game Design or builds assets dynamically. | Human review revised and accepted digest-attested coordinated publication without making Game Design a gameplay availability dependency; implementation and proof remain incomplete. |
| `CONTENT-02` | `AR-3.3` | `AR-1.5`, `AR-3.2`, `AA-3.3`, `GR-1.1`, `PO-3.1` | Can runtime select latest or mix versions? No. Each instance pins one legal tuple of base version, release bundle/manifest, script patch, and enabled plugin versions after compatibility and readiness proof. Friendly channels resolve only for a new launch or rollout, after which the concrete tuple is immutable and rollback selects a recorded tuple. | `accepted-explicit` | H/hard | [ADR 0094](../../architecture/decisions/adr-0094-explicit-cohesive-runtime-release-tuples.md); `VR § Version Lifecycle`; `VR § Script-Only Patch Versions`; `VR § Version Activation & Rollback` | Mutable latest pointers or independently versioned assets. | Human review revised and accepted explicit cohesive tuples with one-time channel resolution; readiness, compatibility, idempotency, and rollout proof remain incomplete. |
| `CONTENT-03` | `AS-1.2` | `AS-1.6`, `AS-1.5`, `AR-1.3`, `GR-4.1`, `SF-1.3` | One DSL/runtime serves embedded game scripts and independently activated linked plugins. Embedded scripts remain game-owned versioned content under publication, readiness, pinning, and rollback; independently activated linked-plugin bundles require an immutable digest, complete validation, explicit scoped approval, platform acceptance attestation, capability grants, and revocation. Signed plugin bundles additionally require signer identity and signature verification; operator-permitted unsigned plugin activation is target-only and allowed only through the ADR 0111 attested policy path, and hosted policy may prohibit it. **Current signed-only status:** the live path accepts allowlisted Ed25519-signed bundles and applies the signed-intake v1 digest/signature format, with current publication/activation preflight and policy seams; it persists/exposes only one allowlisted signer, does not yet prove complete manifest/content/binding/component/capability validation or all-signer revocation, and lacks complete bundle-to-compiled-runtime and focused proof. Unsigned activation is disabled. The common scoped-approval, platform-acceptance, exact-capability, revocation, validation, and proof controls also apply to signed activation; the accepted signed-activation contract remains defined by ADR 0111, while its implementation and proof remain incomplete until they are complete. This preserves the signed-intake boundary recorded by `CMD-02` and `MS-AS-PLUGIN-TRUST`. Base-game DML publishes through Draft, while typed owners retain hot-path invariants. | `accepted-explicit` | H/hard | [ADR 0111](../../architecture/decisions/adr-0111-unified-dsl-with-distinct-embedded-script-and-plugin-lifecycles.md) § Implementation Status; [Modding Framework](../../architecture/microservices/game-design-service/modding-framework.md) § Canonical Plugin Bundle Contract; `SC § Sandboxing & Security`; `GC § Scripting Hooks` | Separate DSLs, warning-only unsigned activation, arbitrary layered plugin DML, or running routine foundational mechanics through the durable DSL path. | Human review completed 2026-07-20; `revised`; current signed-path enforcement and proof remain partial. |
| `AUTO-01` | `AS-1.1` | `AS-1.5`, `SF-1.2`, `SF-2.3`, `GR-1.2` | What makes a script event idempotent? The endpoint-specific Trigger Identity matrix includes admitted runtime scope, handler/event/version identity, dry-run namespace, and scheduler, readiness, or plugin dimensions where applicable. | `accepted-explicit` | H/hard | `SC § Validation & Admission Pipeline`; [ADR-0001](../../architecture/decisions/adr-0001-scripting-event-ingress-idempotency-identity.md); [scripting normative tables](../../architecture/system-architecture-scripting-normative-contract-tables.md) § `Table 1: Trigger Identity (Required Fields)` | Event ID alone, one universal tuple with sentinel values, or an infrastructure-generated opaque key. | Existing ADR-0001 and the normative table settle the baseline. |
| `AUTO-02` | `AS-1.5` | `GR-1.2`, `SF-2.3`, `PO-4.2`, `SF-1.1` | When may automation report success? Only after resulting commands are accepted into the tick system; admitted work cannot rely solely on Redis staging. | `accepted-explicit` | H/hard | `SC § Validation & Admission Pipeline`; [ADR-0002](../../architecture/decisions/adr-0002-automation-handoff-reliability-and-success-semantics.md) | Define success as DSL evaluation and treat downstream delivery as best effort. | Existing ADR-0002 settles stage-aware success. |
| `AUTO-03` | `AS-1.6` | `AS-1.1`, `AS-1.4`, `AS-1.5`, `GR-1.2`, `PO-2.4` | What happens during script reload? Application backpressure and audit; bounded same-ID retry for selected external events; no general timer backfill. | `accepted-explicit` | H/med | `SC § Implementation Status`; [ADR-0003](../../architecture/decisions/adr-0003-reload-backpressure-and-retry-contract.md) | Queue all events durably or drop every event uniformly. | Existing ADR-0003 settles it. |
| `CONTENT-04` | `AR-2.2` | `AR-1.1`, `AR-2.3`, `GR-4.3`, `GR-4.1` | Starter profiles and ordered packs materialize ordinary game-owned DML/scripts into Draft and never become hidden runtime inheritance. Exact baselines, source identities/mappings, deletion/detachment lineage, and commit history support conservative creator-initiated upgrades: unchanged objects may advance automatically, while local changes, broken references, and semantic identity changes require explicit resolution. | `accepted-explicit` | M/med | [ADR 0124](../../architecture/decisions/adr-0124-materialized-starter-profiles-with-conservative-draft-upgrades.md); `OV § Game-Authored Defaults and Starter Experience Profiles`; `GT § Starter Experience Profiles` | Live inheritance, permanent overlays, copying without upgrade lineage, or generic automatic merge. | Human review completed 2026-07-20; `revised`; profile materialization and upgrade implementation/proof remain incomplete. |
| `CONTENT-05` | `AR-1.2` | `AR-1.1`, `AR-1.5`, `AR-1.3`, `SF-1.2`, `PO-3.3` | Whole-game import/export, round-trip JSON, filesystem projects, Git synchronization, and a promised portable snapshot are outside the current target. Typed Game Design APIs remain authoritative while stable identities and normalized references preserve future options; server-side copy, recovery export, profiles, plugins, and asset delivery retain purpose-specific contracts. | `accepted-explicit` | M/med | [ADR 0125](../../architecture/decisions/adr-0125-defer-whole-game-portability-and-external-authoring-formats.md); `GC § World Configuration`; `WE § Capabilities`; `GD § Canonical Authoring Boundary` | Commit now to import-as-new-Draft portability or a Git/filesystem authoring authority. | Human review completed 2026-07-20; `deferred`; revisit only for a concrete migration, portability, offline-tooling, or whole-game-distribution requirement. |

### Settings, Security, Delivery, And Recovery

| Key | Primary capability | Secondary capabilities | Question -> current choice | Status | I/R | Sources / explicit ADR | Strongest alternative | ADR recommendation |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `SET-01` | `AR-2.1` | `AR-2.3`, `AR-2.2`, `SF-2.1`, `GR-1.1`, `EA-1.2` | What is settings precedence? Accepted target value precedence is defaults -> preset -> bootstrap -> supported runtime default -> tenant -> game instance, with per-key source eligibility; schema/platform bounds and operator caps apply separately as constraints. Current implementation remains service defaults plus merged persisted overrides. | `accepted-explicit` | H/med | `ST § Ownership and Precedence`; `ST § Implemented Status`; `ST § Current Practical Rule`; [ADR-0012](../../architecture/decisions/adr-0012-settings-value-precedence-and-constraints.md) | Make the current three-layer implementation canonical, with fixed hard bounds and no presets, runtime defaults, or configurable caps. | ADR-0012 records the revised human decision; remaining differences are implementation and proof gaps. |
| `SET-02` | `AR-2.3` | `AR-2.1`, `SF-2.1`, `SF-2.2`, `GR-1.1` | Accepted target: revisioned pull snapshots and local caches, not a general distributed config platform. Per-key freshness bounds permit bounded last-known-good use; expiry falls back only for declared harmless settings and fails closed for restrictive policy. Urgent revocation uses another authoritative fence. Current implementation remains a short-TTL/force-refresh/per-scope-eviction reader and does not yet prove monotonic revisions, consumer compare-and-set, freshness-class stale behavior, or the restrictive-setting fence. | `accepted-explicit` | M/med | [ADR 0113](../../architecture/decisions/adr-0113-bounded-pull-settings-distribution-with-freshness-classes.md); `ST § Effective Config Resolution`; `ST § Distribution and Freshness` | General push configuration, synchronous reads on every use, indefinite stale permissive values, or one failure rule for every setting. | Human review completed 2026-07-20; `revised`; revision/freshness implementation remains incomplete. |
| `SEC-01` | `SF-1.3` | `PO-3.2`, `PO-2.1`, `PO-1.1`, `SF-1.5` | Where are secrets and certificates managed? FireMUD has one Kubernetes Secret/mounted-file workload contract, no bundled or mandatory Vault, Account-only JWT private keys, public JWKS configuration, per-workload cert-manager identities, hardened cluster controls, and out-of-cluster recovery custody. External provisioning is transparent, while shared multi-DC authority requires a future decision. | `accepted-explicit` | H/hard | [ADR 0032](../../architecture/decisions/adr-0032-kubernetes-native-secret-delivery-without-mandatory-vault.md); `SE § Token Issuance & Secret Storage`; `SE § TLS Termination & Internal Encryption`; `GR § TLS Requirements` | Require Vault everywhere, make Kubernetes the mandatory upstream origin as well as delivery, or integrate provider APIs into applications. | Revised and accepted by human decision; one provider-neutral consumption contract avoids dual product modes while explicit hardening, least privilege, identity, recovery, and future multi-DC triggers remain enforceable. |
| `SEC-02` | `SF-1.3` | `PO-3.2`, `PO-4.2`, `AA-1.3`, `PO-1.3` | How do Account JWT keys rotate and when is the boundary player-facing ready? Planned rotation prepublishes, proves validator visibility, promotes, overlaps through retiring-token expiry, and prunes; compromise/restore quarantines and hard-cuts the environment-wide issuer. Current shared HMAC is implementation debt and fails the readiness gate. | `accepted-explicit` | H/hard | `SE § JWT Key & JWKS Rotation Workflow`; [ADR-0014](../../architecture/decisions/adr-0014-phased-jwt-signing-key-rotation-and-readiness.md) | Use a maintenance-window hard cutover for every rotation, or adopt a non-exportable external signer while preserving the lifecycle protocol. | ADR-0014 records the revised human decision; issuance, validation, automation, convergence, and gate enforcement remain incomplete. |
| `SEC-03` | `PO-2.2` | `SF-1.3`, `AA-1.3`, `PO-2.1`, `AA-2.1`, `EA-3.1` | Is plaintext Telnet accepted in production and is TOTP enforced? Public player-facing Telnet requires TLS; raw Telnet is local/test/private-only, and no TOTP transport gate is added. | `accepted-explicit` | H/hard | [ADR 0033](../../architecture/decisions/adr-0033-public-player-facing-telnet-requires-tls.md); `SE § Plaintext Telnet policy`; `AU § Implemented Status` | Preserve public plaintext with warnings/hardening, require TOTP, or add a secure-side-channel single-use ticket. | Revised and accepted by human decision; TLS protects the actual channel without imposing a factor that leaves the session exposed. |
| `SEC-04` | `PO-1.1` | `PO-1.2`, `PO-1.3`, `PO-1.4`, `PO-2.1`, `GR-1.1`, `AR-3.3` | Where do administrative and session-lifecycle writes enter? The target external operator boundary sends moderation, runtime flags, quota overrides, admission control, tick/coordination remediation, and session-lifecycle operations through Gateway to Logging/Admin; domain services retain mutation authority. Current Game Session `/sessions*` routes remain owner-local/internal and are not evidence that target Logging/Admin forwarding is live. The moderation policy-input/audit mutation is currently unavailable/gated pending its action-family schema, shared cross-language `mutationDigest/v1` golden vectors, Account-owned authorization-reference issuance, and Logging & Admin receiving-boundary validation/redemption; `EvaluateModerationPolicy` remains a separate live read consumed at the owner enforcement boundaries, and no domain-owner redemption or enforcement is part of that persistence path. Quota overrides, broader remediation, and other owner mutations without a complete route/proof remain gated or target-only. | `accepted-explicit` | H/med | `OV § Admin Entry Points and Traffic Surfaces`; `OV § Core Operator Action Backing Contracts (Canonical)`; `RM § Notes on Movement and Moderation Contracts`; [ADR-0047](../../architecture/decisions/adr-0047-logging-admin-as-external-operator-write-ingress.md) (human review completed 2026-07-19; revised); [ADR-0048](../../architecture/decisions/adr-0048-durable-idempotent-operator-write-execution.md) (human review completed 2026-07-19; revised) | Direct domain admin endpoints or a separate operator write plane. | ADR-0047 and ADR-0048 are accepted, revised, and human-reviewed; action-family implementation and proof remain partial where the route or owner contract is gated. |
| `ADMIN-01` | `PO-1.1` | `PO-1.3`, `PO-1.4`, `SF-2.3`, `GR-1.1`, `PO-4.1` | What backs operator writes and forwarding? Logging & Admin durably records one logical mutation under one `controlPlaneRequestId`; only ADR 0048-permitted transport retries reuse the exact authority-bound tuple, and the domain owner deduplicates them while atomically persisting the mutation and idempotent result. Lost responses and final-audit failures use read-only reconciliation under that identifier rather than redelivering an ambiguous mutation. Observability is not a write dependency, and region-scoped Game Session mutations execute at the current lease owner under the current gameplay fence. | `accepted-explicit` | H/hard | [ADR 0048](../../architecture/decisions/adr-0048-durable-idempotent-operator-write-execution.md); `OV § Core Operator Action Backing Contracts (Canonical)`; `OV § Admin Entry Points and Traffic Surfaces`; `RM § Notes on Movement and Moderation Contracts` | Let operators write domain stores directly, use best-effort forwarding or distributed transactions, let dashboards determine write success, or let a session front-end mutate region-owned coordination without lease-owner forwarding. | Human review revised and accepted durable intent, owner-local idempotency, explicit uncertain outcomes, and fenced lease-owner forwarding; implementation and focused proof remain. |
| `MOD-01` | `PO-1.2` | `EA-2.4`, `AA-1.3`, `PO-1.3`, `PO-4.1` | How is moderation policy propagated and enforced? Logging & Admin is the sole policy-definition, evaluation, and audit authority; the policy-input/audit mutation is currently unavailable/gated pending its action-family schema, shared cross-language `mutationDigest/v1` vectors, Account-owned authorization-reference issuance, and Logging & Admin receiving-boundary validation/redemption. It is not a domain-owner enforcement mutation or a domain-owner redemption path. Until versioned propagation is implemented, the live `EvaluateModerationPolicy` read is the only enforcement input. In the target state, consumers use only a validated versioned snapshot derived from that authority: `{tenantId, policyScope, policyVersion, issuedAt, eventId}` must match, invalidation is monotonic, and push invalidation is paired with pull-on-miss refresh. A snapshot/live version divergence invalidates the local snapshot and requires authoritative refresh; if no fresh evidence is available within the shared bounded staleness window, `gameplay_ban` and `chat_ban` fail closed, while `chat_mute` may reuse the last valid snapshot only within that window. | `accepted-explicit` | H/hard | `OV § Moderation Policy Distribution and Enforcement Contract`; `OV § Ban and Moderation Taxonomy (Canonical)`; `RM § Notes on Movement and Moderation Contracts`; [ADR-0048](../../architecture/decisions/adr-0048-durable-idempotent-operator-write-execution.md) | Centralize all enforcement in Logging/Admin, permit unbounded stale caches, or let each service maintain an independent policy authority. | The canonical overview settles authority, current-versus-target precedence, freshness, invalidation, and divergence handling; create or amend an ADR only if that contract changes. |
| `SEC-05` | `SF-1.3` | `AA-1.3`, `AA-2.1`, `PO-2.4`, `PO-1.2` | Who owns brute-force and abuse controls? Edge services own transport pressure, Account exclusively owns credential abuse without failed-attempt hard locks, and Game Session owns local fast-path post-auth command budgets with optional coarse shared backstops. | `accepted-explicit` | H/hard | [ADR 0034](../../architecture/decisions/adr-0034-layered-abuse-controls-without-attacker-triggered-account-locks.md); `SE § Brute-Force Defense and Abuse Handling`; `GW § Rate Limiting & Abuse Protection` | Let the edge lock accounts, centralize all limits in Account, durably lock after a failure threshold, or retain a distributed Redis operation for every gameplay command. | Revised and accepted by human decision; trusted source propagation, non-enumerating graduated throttles, fail-closed new login, and local command buckets preserve security without an attacker-triggered denial or gameplay datastore dependency. |
| `OPS-01` | `PO-3.1` | `AR-1.5`, `PO-3.3`, `PO-4.4`, `SF-1.1` | What artifact is promoted? Exact reviewed image digest and release attestation are promoted without rebuild; tags are convenience references only. | `accepted-explicit` | H/med | `CI § Building and Pushing Images`; `CI § Promotion & Rollback Model`; `VR § Game Version Publishing` | Rebuild separately per environment or promote mutable tags. | No ADR while CI/CD remains authoritative. |
| `OPS-02` | `PO-3.1` | `AR-3.3`, `PO-4.2`, `PO-4.4`, `PO-1.4` | How are production deployments rolled out and rolled back? RollingUpdate plus explicit known-good digest reapplication; no default canary or auto-rollback. | `needs-human-review` | H/med | `CI § Rollback Strategy` | Canary/progressive delivery with automated rollback. | Yes; blast radius and operator burden lack risk rationale. |
| `OPS-03` | `PO-3.4` | `SF-2.1`, `SF-2.2`, `SF-2.3`, `GR-1.4`, `PO-4.4` | What recovery model is accepted? **Current implementation** runs an online PostgreSQL `pg_dump` every 15 minutes and uses Redis AOF for runtime crash recovery only; it does not yet record complete artifact lineage or prove restore readiness. **Accepted target** under ADR 0015 is an online transactionally consistent environment-wide PostgreSQL snapshot with Redis excluded from restore; the player-facing rewind boundary is specified by `OPS-04`. Cadence/RPO and operational acceptance remain open in this row. | `needs-human-review` | H/hard | `BK § PostgreSQL Logical Backups`; `BK § Redis Persistence`; `BK § Restore Mode Selection`; [ADR 0015](../../architecture/decisions/adr-0015-online-backup-and-environment-wide-cold-start-recovery.md) | PITR/physical backups, coordinated Redis restore, or tenant-selective replay. | Yes; RPO, player loss, and recovery behavior need explicit acceptance beyond the accepted restore boundary. |
| `OPS-04` | `PO-3.4` | `PO-3.1`, `GR-1.3`, `PO-4.4`, `PO-1.3`, `SF-2.1` | What is the player-facing backup and restore boundary? **Current implementation and proof** are not player-facing restore-ready: quarantine, empty Coordination Redis proof, environment-wide convergence, session/epoch invalidation, and controller-gated reopen remain incomplete. **Accepted target** is routine online environment-wide PostgreSQL backup plus player-facing `cold_start_restore`, empty Coordination Redis, safe participant dispositions, quarantine, and controlled reopen; scoped surviving-Redis restore is deferred. The **durable recovery controller is the runtime authority** for `ready_to_reopen -> releasing -> finalized`; checked-in recovery and traffic-open records are immutable post-finalization projections, not runtime authority. | `accepted-explicit` | H/hard | `BK § Online Snapshot Contract`; `BK § Restore Mode Selection`; [ADR 0015](../../architecture/decisions/adr-0015-online-backup-and-environment-wide-cold-start-recovery.md) | Quiesce every writer or pause gameplay before each dump, adopt PITR, or retain surviving Redis for scoped restore. | ADR 0015 records the revised human decision; implementation/proof debt remains in artifact lineage, quarantine, convergence, reconciliation, controller gating, and release validation. |
| `OPS-05` | `PO-3.1` | `PO-3.2`, `PO-4.4`, `PO-1.3`, `SF-1.3` | Who may promote and apply environments? PRs validate; `develop` is staging candidate; `main` release tags target production; production apply is operator-controlled. | `accepted-explicit` | H/med | `CI § Workflow Structure`; `CI § Deploying to Kubernetes`; `CI § Deployment Credentials & Environments` | Fully automated production deployment or GitOps-only reconciliation. | No ADR unless deployment authority changes. |
| `OPS-06` | `PO-4.4` | `PO-3.1`, `PO-3.4`, `AA-2.1`, `EA-3.1` | What is preview proof and lifecycle? PR previews are disposable/resettable; first hosted proof is Telnet `LOGIN -> PLAY -> LOOK`, not browser-first. | `accepted-implicit` | M/med | `CI § PR Preview Environments` | Persistent previews and browser-first acceptance tests. | No ADR; document as acceptance policy if preview becomes a product promise. |

## Main Inventory Crosswalk

The main inventory remains the compact control crosswalk. These aliases preserve its nine decision keys while allowing this file to use the finer-grained taxonomy. The 9 additions below have no main-inventory alias because they remain source-ledger-only decisions.

| Main inventory key | Cross-cutting key | Exact primary capability here |
| --- | --- | --- |
| `AS-INGRESS-IDEMPOTENCY` | `AUTO-01` | `AS-1.1` |
| `AS-HANDOFF-SUCCESS` | `AUTO-02` | `AS-1.5` |
| `AS-RELOAD-BACKPRESSURE` | `AUTO-03` | `AS-1.6` |
| `AA-WORLD-SELECTOR-IDENTITY` | `AUTH-01` | `AA-3.1` |
| `PO-EDGE-SHARDING` | `EDGE-01` | `PO-2.1` |
| `GR-GAMEPLAY-CLUSTER-SCOPE` | `EDGE-02` | `PO-3.1` |
| `SF-COORDINATION-REDIS-OWNERSHIP` | `REDIS-02` | `SF-2.2` |
| `SF-TCP-PROXY-IDENTITY` | `EDGE-03` | `SF-1.3` |
| `GR-SESSION-FRONTEND-EXECUTION` | `SESSION-01` | `GR-1.1` |
| No existing main key | `EDGE-04` | `PO-2.1` |
| No existing main key | `EDGE-05` | `PO-2.4` |
| No existing main key | `EDGE-06` | `PO-2.1` |
| No existing main key | `SESSION-09` | `AA-2.3` |
| No existing main key | `HOTPATH-01` | `GR-1.2` |
| No existing main key | `RECON-01` | `GR-1.4` |
| No existing main key | `ADMIN-01` | `PO-1.1` |
| No existing main key | `MOD-01` | `PO-1.2` |
| No existing main key | `SEC-05` | `SF-1.3` |

## Flags

### Contradictions And Target/Current Tensions

| Flag | Evidence | Consequence |
| --- | --- | --- |
| `SET-01` implementation gap | ADR-0012 separates accepted value precedence from hard-bound/operator-cap constraints; `ST § Current Practical Rule` leaves presets, runtime defaults, caps, provenance, and centralized resolution future work. | Runtime and design tools can resolve different effective settings until the accepted model and its proof are complete. |
| `SESSION-04` implementation and proof gap | ADR 0013 defines qualifying failures, the meaning of invisibility, 10-second ordinary recovery, a 30-second hard cutoff, explicit stalled-input behavior, authority continuation, and fallback. | Stub upstream rebind and downstream-worker restart proof do not yet establish authenticated real-Game-Session replacement continuity. |
| `SEC-02` implementation and proof gap | ADR-0014 defines Account-only asymmetric authority, phased planned rotation, compromise hard cutover, validator convergence, and an explicit player-facing gate; live code and manifests remain shared-HMAC, distribute signing material, and do not implement the gate. | Mounted JWT/JWKS paths and raw JWKS serving cannot establish player-facing readiness until issuance, validation, rotation drills, and convergence proof implement the accepted boundary. |
| `OPS-04` implementation and proof gap | **Current implementation/proof** does not yet provide player-facing quarantine, empty-Redis proof, environment-wide convergence, or controller-gated reopen. **Accepted target** is ADR 0015's online environment-wide backup and cold-start-only player-facing restore; the durable recovery controller is the runtime authority, while checked-in evidence is only a finalized projection. | Artifact lineage, enforced quarantine, empty-Redis proof, participant/external reconciliation, compatibility classification, durable-controller gating, and controlled-reopen validation are not implemented or proved. |
| Historical ADRs | ADR-0004 and ADR-0006 retain obsolete `reroute` material but explicitly declare supersession/withdrawal by ADR-0007. | Search-based implementers can select obsolete behavior unless historical status is prominent. |

### Assertions With Weak Or Missing Rationale

| Decision keys | Assertion requiring rationale | Missing question |
| --- | --- | --- |
| `SESSION-02`, `SESSION-03`, `SESSION-04`, `SESSION-06` | Single-session takeover, no transport replay, bounded transcript, fire-and-forget commands, and bounded invisible non-edge recovery. | What loss, multi-device, and availability guarantees are actually promised? |
| `TICK-01`, `TICK-03`, `TICK-07` | Separate actor-action and passive/inbound-effect lanes, no global ACID, bounded timer catch-up, and timer skipping. | Which gameplay fairness and recovery requirements make these semantics correct? |
| `TENANT-02`, `TENANT-03` | Shared infrastructure and no playtest merge-back. | What isolation, cost, creator workflow, and player-data tradeoffs were accepted? |
| `CMD-04`, `CMD-05`, `CONTENT-04` | Durable transcript bounds, no live translation, and editable starter DML. | What UX, localization, and creator-product requirements set the defaults? |
| `OPS-02`, `OPS-03` | Current implementation has no canary/auto-rollback and performs a 15-minute online dump; ADR 0015 accepts the online environment-wide snapshot and non-restored Redis/cold-start recovery boundary as the target, while complete proof and the remaining RPO/operational choice are still open. | What deployment, RPO, and operational burdens remain for human acceptance? |

### Choices Likely Requiring Product Consultation

No source proves who made these choices; the flag means that product/security/operations approval was not visible in the reviewed corpus.

| Priority | Decision keys | Consultation topic |
| --- | --- | --- |
| `P1` | `SESSION-02`, `SESSION-03`, `SESSION-04`, `TENANT-03` | Takeover, reconnect loss, restart visibility, playtest isolation, and merge-back expectations. |
| `P1` | `OPS-02`, `OPS-03` | Deployment blast radius, RPO, and production readiness. |
| `P2` | `CMD-05`, `CONTENT-04`, `TICK-01`, `TICK-07` | Localization, starter profiles, action fairness, and missed-timer gameplay semantics. |

## Coverage By Exact Capability

| Capability group | Exact primary capability coverage | Exact IDs represented only as secondary handoffs or not primary in this scan |
| --- | --- | --- |
| Accounts and Access | `AA-1.2` (`AUTH-06`, `AUTH-07`); `AA-1.3` (`AUTH-03`); `AA-1.4` (`SESSION-07`); `AA-1.5` (`TENANT-02`, `ADMIT-01`); `AA-2.1` (`AUTH-02`); `AA-2.2` (`SESSION-03`); `AA-2.3` (`SESSION-02`, `SESSION-09`); `AA-3.1` (`AUTH-01`); `AA-3.3` (`TENANT-01`) | Secondary-only: `AA-1.1` (`AUTH-06`), `AA-3.2` (`AUTH-06`, `TENANT-01`, `TENANT-03`, `ADMIT-01`); no primary-only IDs. |
| Experience and Applications | `EA-1.1` (`CMD-01`, `CMD-06`); `EA-1.2` (`CMD-03`, `CMD-05`); `EA-1.3` (`CMD-04`) | Secondary-only: `EA-2.4` (`MOD-01`), `EA-3.1` (`AUTH-02`, `AUTH-06`, `SESSION-02`, `CMD-03`, `CMD-05`, `SEC-03`, `OPS-06`), `EA-3.3` (`AUTH-07`), `EA-3.4` (`CMD-01`, `CMD-05`); no primary row for `EA-2.1`, `EA-2.2`, `EA-2.3`, or `EA-3.2`. |
| Gameplay Runtime | `GR-1.1` (`SESSION-01`); `GR-1.2` (`TICK-01`, `HOTPATH-01`); `GR-1.3` (`TICK-02`); `GR-1.4` (`SESSION-04`, `RECON-01`); `GR-2.1` (`TICK-06`); `GR-4.1` (`CMD-02`) | Secondary-only: `GR-2.2` (`TICK-03`, `TICK-04`, `HOTPATH-01`), `GR-2.3` (`TICK-07`, `RECON-01`), `GR-3.1` (`TENANT-03`), `GR-3.2` (`TENANT-03`, `TICK-03`, `TICK-04`, `HOTPATH-01`, `RECON-01`), `GR-4.3` (`CONTENT-04`); no primary row for `GR-3.3`, `GR-4.2`, or `GR-4.4`. |
| Authoring and Release | `AR-1.2` (`CONTENT-05`); `AR-1.5` (`CONTENT-01`); `AR-2.1` (`SET-01`); `AR-2.2` (`CONTENT-04`); `AR-2.3` (`SET-02`); `AR-3.3` (`CONTENT-02`); `AR-3.4` (`TENANT-03`) | Secondary-only: `AR-1.1` (`CMD-01`, `CMD-02`, `CMD-05`, `CONTENT-04`, `CONTENT-05`), `AR-1.3` (`CONTENT-03`, `CONTENT-05`), `AR-1.4` (`CONTENT-01`), `AR-3.2` (`TENANT-01`, `CONTENT-01`, `CONTENT-02`); no primary row for `AR-3.1`. |
| Automation and Scripting | `AS-1.1` (`AUTO-01`); `AS-1.2` (`CONTENT-03`); `AS-1.4` (`TICK-07`); `AS-1.5` (`AUTO-02`); `AS-1.6` (`AUTO-03`) | None; no primary row for `AS-1.3`. |
| Shared Runtime Foundations | `SF-1.1` (`SESSION-06`, `GRPC-01`, `GRPC-02`); `SF-1.3` (`AUTH-04`, `AUTH-05`, `EDGE-03`, `SEC-01`, `SEC-02`, `SEC-05`); `SF-2.2` (`REDIS-01`, `REDIS-02`, `REDIS-03`); `SF-2.3` (`TICK-04`); `SF-2.4` (`TICK-03`, `TICK-05`) | Secondary-only: `SF-1.2` (`AUTH-01`, `AUTH-05`, `SESSION-01`, `TICK-04`, `AUTO-01`, `CONTENT-05`), `SF-1.4` (`TICK-07`), `SF-1.5` (`GRPC-01`, `SEC-01`), `SF-2.1` (`REDIS-01`, `CMD-04`, `SET-01`, `SET-02`, `OPS-03`, `OPS-04`); no primary-only IDs. |
| Platform and Operations | `PO-1.1` (`SEC-04`, `ADMIN-01`); `PO-1.2` (`MOD-01`); `PO-2.1` (`EDGE-01`, `EDGE-04`, `EDGE-06`); `PO-2.2` (`SEC-03`); `PO-2.4` (`EDGE-05`, `SESSION-05`); `PO-3.1` (`EDGE-02`, `OPS-01`, `OPS-02`, `OPS-05`); `PO-3.4` (`OPS-03`, `OPS-04`); `PO-4.4` (`OPS-06`) | Secondary-only: `PO-1.3` (`SEC-02`, `SEC-04`, `ADMIN-01`, `MOD-01`, `OPS-04`, `OPS-05`), `PO-1.4` (`EDGE-06`, `SEC-04`, `ADMIN-01`, `OPS-02`), `PO-3.2` (`AUTH-05`, `TENANT-02`, `EDGE-03`, `REDIS-02`, `SEC-01`, `SEC-02`, `OPS-05`), `PO-3.3` (`CONTENT-01`, `CONTENT-05`, `OPS-01`), `PO-4.1` (`SESSION-05`, `REDIS-03`, `GRPC-01`, `ADMIN-01`, `MOD-01`), `PO-4.2` (`ADMIT-01`, `EDGE-05`, `SESSION-04`, `TICK-02`, `TICK-06`, `HOTPATH-01`, `RECON-01`, `REDIS-01`, `GRPC-01`, `AUTO-02`, `SEC-02`, `OPS-02`), `PO-4.3` (`GRPC-02`); no primary row for `PO-2.3`. |

## Historical Adversarial Review Questions

These questions preserve the priority model used by the completed human review. They are not an active autonomous review queue.

| Priority | Exact capability focus | Decision keys | Adversarial question |
| --- | --- | --- | --- |
| `P0` | `AR-2.1`, `SF-1.3`, `PO-3.4`, `PO-4.4` | `SET-01`, `SEC-02`, `OPS-03`, `OPS-04` | Are the accepted settings, key-rotation, and ADR 0015 online-snapshot/cold-start target executable and proved, while OPS-03's snapshot cadence, RPO, and operational-acceptance gates remain explicitly human-reviewed? |
| `P0` | `AA-1.2`, `AA-1.5`, `AA-2.1`, `AA-2.3`, `PO-2.1`, `PO-2.4`, `SF-1.3` | `AUTH-02`, `AUTH-06`, `AUTH-07`, `ADMIT-01`, `EDGE-04`, `SESSION-07`, `SESSION-09`, `SEC-05` | Can stale tokens, ambiguous/query-carried connect tokens, missing membership, billing changes, global roles, brute-force paths, or refresh/logout failures admit the wrong tenant/player or revoke too late? |
| `P0` | `AA-2.2`, `GR-1.1`, `GR-1.3`, `PO-2.4`, `SF-2.2`, `SF-2.3` | `EDGE-05`, `SESSION-01..06`, `TICK-02`, `REDIS-01..02` | Do lease loss, Redis reset, takeover, reconnect, Gateway close/restart translation, and duplicate delivery preserve the no-duplicate/no-resurrection invariants? |
| `P0` | `GR-1.2`, `GR-1.4`, `GR-2.1`, `GR-2.2`, `PO-1.1`, `PO-1.2`, `SF-2.3` | `ADMIN-01`, `HOTPATH-01`, `MOD-01`, `RECON-01`, `TICK-03..07` | Do bounded hot-path orchestration, movement fail-closed behavior, durable operator forwarding, versioned moderation enforcement, and spatial backlog ownership converge without partial writes or stale policy? |
| `P1` | `GR-1.2`, `GR-1.4`, `GR-2.1`, `SF-2.3`, `AS-1.4` | `HOTPATH-01`, `RECON-01`, `TICK-01`, `TICK-03..07`, `AUTO-01..03` | Are fairness, timer loss, cross-region follow-ups, outbox recovery, and at-least-once dedupe correct under crashes and topology changes? |
| `P1` | `AR-1.2`, `AR-1.5`, `AR-3.3`, `AS-1.2`, `GR-4.1` | `CONTENT-01..05`, `CMD-02` | Can publication, version pinning, asset attestation, import/export, and authored effects avoid mixed or unsafe runtime state? |
| `P1` | `EA-1.2`, `EA-1.3`, `AA-2.2`, `PO-2.2` | `CMD-03..05`, `SESSION-03` | Do transcript bounds, prompts, localization fallback, and Telnet/WebSocket recovery match the player-visible promise? |
| `P2` | `PO-1.1`, `PO-1.2`, `PO-2.1`, `PO-2.4`, `PO-3.1`, `PO-3.2`, `PO-4.4`, `SF-1.3` | `ADMIN-01`, `EDGE-01..06`, `MOD-01`, `OPS-01..06`, `SEC-05` | Do edge trust, carrier/replay/route controls, liveness/close taxonomy, operator backing, moderation propagation, brute-force controls, single-cluster assumptions, digest promotion, rollback, preview proof, and operator-only deployment remain verifiable at scale? |
