# Game Design jOOQ Migration Vertical Slice

## Goal and Status

Goal: migrate `game-design-service` onto `jOOQ + Flyway` so publication, release-attestation, asset-manifest, and launch-descriptor persistence uses the same explicit SQL model as the rest of the converged stack. Status: planned.

## Scope

- replace JPA repository/entity persistence in the Game Design publication and control-plane seams with `jOOQ`;
- keep the current Flyway baseline and current publication/runtime contracts intact;
- remove JPA/Hibernate runtime/config dependencies from the service.

## Checklist

- [ ] Inventory Game Design persistence seams and choose the service-local `jOOQ` ownership boundaries.
- [ ] Replace JPA persistence with explicit SQL queries and mappings.
- [ ] Remove JPA/Hibernate runtime/config dependencies.
- [ ] Re-prove publication/control-plane behavior on the new SQL path.
