# Effective Settings Resolution and Invalidation Vertical Slice Task List

## Goal and Status

Goal: implement one bounded effective-settings resolution path so runtime services consume the same merged settings result instead of each service inventing its own merge logic. Status: planned.

This slice follows `02.9.2`. It should stay focused on read resolution, precedence, and bounded cache/invalidation behavior for the surfaced pre-`06` domains.

## Scope

- Effective-settings merge rules for the current settings domains
- Resolution precedence:
  - hardcoded safe defaults
  - selected preset baseline
  - operator bootstrap overrides
  - operator runtime overrides
  - operator caps
  - tenant/game overrides
- Bounded cache/invalidation behavior
- Canonical runtime read surfaces for Game Session and Game Logic

## Key Tasks

- [ ] Implement one shared effective-settings resolver/read model for the current surfaced domains.
- [ ] Apply canonical precedence consistently across services.
- [ ] Replace existing ad hoc service-local merge paths where the shared resolver now exists.
- [ ] Define bounded caching and invalidation behavior for runtime consumers.
- [ ] Keep the runtime contract simple and authoritative rather than turning it into a general config-distribution platform.
- [ ] Update operator/debug effective-settings surfaces to reflect the shared resolver.

## Tests

- [ ] Add resolution tests proving precedence across defaults, preset, overrides, caps, and tenant/game settings.
- [ ] Add focused cache/invalidation tests for the first shared read model.
- [ ] Add cross-service tests proving Game Session and Game Logic resolve the same effective values for the same scope.

## Notes

- This slice should close the biggest remaining “settings are real but not unified” gap.
- Runtime consistency matters more than broad dynamic hot-reload sophistication here.
