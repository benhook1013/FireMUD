# Account Service jOOQ Migration Vertical Slice

## Goal and Status

Goal: migrate `account-service` onto `jOOQ + Flyway` so even the more CRUD-shaped account/profile/membership service follows the same SQL persistence model as the rest of the converged repo. Status: planned.

## Scope

- replace JPA repository/entity seams in account, profile, token, membership, and billing-adjacent persistence with `jOOQ`;
- preserve the current Flyway baseline and live account contracts;
- remove JPA/Hibernate runtime/config dependencies from the service.

## Checklist

- [ ] Inventory current JPA account persistence seams.
- [ ] Replace them with `jOOQ` queries and mappings.
- [ ] Remove JPA/Hibernate runtime/config dependencies.
- [ ] Re-prove account service behavior and downstream client expectations on the new SQL path.
