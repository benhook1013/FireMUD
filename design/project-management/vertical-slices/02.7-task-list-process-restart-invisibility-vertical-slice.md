# Process Restart Invisibility Vertical Slice Task List

## Goal and Status

Goal: prove that real Game Session and Game Logic process restarts can be hidden behind the already-live Gateway gameplay bridge so an established client sees at most a bounded stall instead of a forced reconnect. Status: planned follow-up after `02.6-task-list-live-backend-rebind-invisibility-vertical-slice.md`.

This slice intentionally starts after the Gateway-owned gameplay bridge and focused upstream rebind proof are already live. It narrows the remaining work to actual process-restart continuity instead of first introducing the bridge mechanism itself.

## Implementation Notes

- `02.6` already provides the Gateway-owned gameplay WebSocket bridge, stable edge transport session ids, and a focused abrupt-upstream-loss rebind proof.
- The remaining gap is proving the same mechanism against real Game Session and Game Logic restart conditions instead of a synthetic upstream drop.
- This slice should stay honest about the bounded loss envelope:
  - no command replay fantasy,
  - no transport-byte replay guarantee,
  - reconnect screen buffer plus fresh current-state redraw remain the player-visible continuity model.

## 1. Game Session Process Restart

- [ ] Add one focused proof that a real Game Session restart can occur while the downstream Gateway socket remains open and gameplay traffic resumes after a bounded stall.
- [ ] Confirm the stable edge transport session id is reused across the rebound upstream connection.
- [ ] Ensure the resumed path still uses current shared reconnect/admission state rather than stale process-local memory.

## 2. Game Logic Process Restart

- [ ] Add one focused proof that a real Game Logic restart can occur without forcing a visible client reconnect when command/tick ownership is reconstructed from shared state.
- [ ] Confirm the player-visible continuity model remains:
  - recent screen buffer context,
  - fresh `LOOK`,
  - fresh prompt,
  rather than a made-up replay stream.

## 3. Edge and Classification Discipline

- [ ] Keep edge-visible disconnect taxonomy only for cases where the edge route itself really dropped or upstream rebind exhausted its bounded stall window.
- [ ] Avoid turning this slice into generalized chaos testing; keep the proofs operationally meaningful and bounded.

## 4. Final QA Checklist

- [ ] Manually verify one real Game Session restart while a client stays connected at the edge.
- [ ] Manually verify one real Game Logic restart while a client stays connected at the edge.
- [ ] Confirm the player sees continued gameplay after a brief stall instead of a forced `LOGIN` / `PLAY` cycle.
