# Entity Management jOOQ Migration Vertical Slice

## Goal and Status

Goal: migrate `entity-management-service` onto `jOOQ + Flyway` so inventories, item instances, transfer audit, actor state, and related control-plane reads follow the same explicit SQL persistence model as the rest of the repo. Status: in progress; shared tooling plus the character/item/state/audit support seam are on `jOOQ`, while the denser item/container runtime repositories still remain.

## Scope

- replace JPA repository/entity seams in item, actor-state, and transfer-audit persistence with `jOOQ`;
- preserve the current Flyway baseline and live gameplay contracts;
- remove JPA/Hibernate runtime/config dependencies from the service.

## Checklist

- [x] Inventory current JPA persistence seams and group them into explicit SQL ownership areas.
- [ ] Replace those seams with `jOOQ` queries and mappers.
  Shared `jOOQ` build/codegen is now enabled for Entity Management, the squashed baseline was normalized to the current canonical schema so `jOOQ` can generate against it, and the first repository cluster is migrated: characters, items, NPCs, actor conditions/resources, equipment/body-layout definitions, character friends, visible-ref counters, and transfer audit.
- [ ] Remove JPA/Hibernate runtime/config dependencies.
- [ ] Re-prove gameplay-facing entity flows and module checks on the new SQL path.
