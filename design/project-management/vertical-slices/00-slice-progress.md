# Slice Progress Queue

This file is the short ordered view of what is still outstanding.

It intentionally omits completed slices and uses the individual slice docs as the canonical detail source.

## Suggested Order

### 1. Immediate implementation queue

These are narrow enough to implement now and unblock the current runtime/platform direction.

1. [02.19.3 Game Session jOOQ Migration](./02.19.3-task-list-game-session-jooq-migration-vertical-slice.md)
   Remaining: the first Game Session `jOOQ` cluster is now live on admission-pointer authority/audit, prepared version-upgrade rows, feature flags, and game manifests, and the next persistence follow-through is the heavier runtime/tick/remote/game-instance migration while the shared SQL foundation is still fresh and before new JPA-shaped persistence work accretes there.
2. [02.20.3 Publish and Script Patch Temporal Migration](./02.20.3-task-list-publish-and-script-patch-temporal-migration-vertical-slice.md)
   Remaining: the shared Temporal substrate and first world-lifecycle adopter are now live, and this slice now has a real `script-patch-readiness` workflow family plus operator-visible workflow metadata on patch-status reads; the remaining follow-through is publish / release-attestation migration plus any later rollout-specific workflow cleanup while the durable control-plane pattern is still fresh.
3. [09.1 Realm Catalog and Admission-Pointer Routing](./09.1-task-list-realm-catalog-and-admission-pointer-routing-vertical-slice.md)
   Remaining: keep broader reconnect/cutover consumers on the now-live persisted Game Session admission-pointer authority and preserve the full world/realm/resolved-target/pointer-freshness routing bundle in later routing-sensitive seams now that operator-facing prepare/read/cutover/audit tooling, retry-safe cutover execution, gateway-routed operator access, explicit public-production pointer metadata, active session/presence routing-bundle carry-through, first-party stale-pointer proof, fail-closed reused-session relogin cleanup for denied `LOGIN` reuse, active gameplay-session stale-pointer fencing back to a logged-in shell, websocket transport-bootstrap route refresh plus stale-shell cleanup on reused session ids, trusted TCP proxy partial-routing rejection at the gateway edge, Telnet proxy hidden-bootstrap routing-bundle normalization on the producer side, gameplay-originated scripting ingress/work-item/operator-read carry-through, the current scheduler-owned timer/work-item/audit routing carry-through, Automation->Game Session staged-command plus command-status carry-through, automation/remote-followup partial-routing rejection in command-and-script producers, remote-followup durable-row and control-plane read normalization for partial routing metadata, retry-safe remote scheduling equality plus partial remote list-filter/current-runtime readback collapse, automation scheduler/ingress/handoff/control-plane persistence and projection collapse of partial routing bundles, the local gameplay-command staging plus sealed selected-work manifest repair/collapse path for partial routing metadata, fail-closed Game Logic / Entity Management outbound attestation normalization, and isolated tcp-proxy gateway-restart cross-service proof are in place.
4. [02.18.8 Tick Batch and Effect Ledger Hardening](./02.18.8-task-list-tick-batch-and-effect-ledger-hardening-vertical-slice.md)
   Remaining: build on the now-live durable batch/effect substrate, explicit current-boundary `regionId` on ownership and `tick_batch`, gameplay-command selected-work manifest with current-boundary `enqueueSeq`, queue-claim kind/state/ordinal plus due-point metadata now mirrored onto durable command status rows, current owned runtime `gameInstanceId` / `regionId` / `regionEpoch` plus current owned routing bundle and explicit stale-routing signaling now exposed on gameplay command status, immediate retry queue-source truth on requeue before the next retry claim, source-aware requeue metrics, automation command/source identity plus admitted routing-bundle carry-through, sealed-manifest-authoritative replay recovery, stale-fence batch drain, origin-side executable `remote_followup` / `remote_command_coordinator` / `remote_followup_result` substrate with explicit coordinator `followupId` linking plus first `remote:*` hint, the new internal remote-followup scheduling gRPC surface over that same durable origin-side scheduler, direct request-supplied routing/provenance carry-through at remote scheduling time plus first-class request-supplied remote payload authority (`payloadKind`, requested command text, `requiresSoloTick`) and trigger-script-event authority (`eventType`, `eventSchemaVersion`, `scriptEventId`, `triggerMode`, `readSnapshotToken`, `eventPayloadJson`) with fail-closed validation that any optional payload blob agrees with those explicit fields before durable rows are written, target-side execution now using persisted remote-row `payloadKind`, requested-command, `requiresSoloTick`, trigger-script-event authority, durable `claimTargetAggregate`, and origin-source tuple truth as authority instead of reparsing JSON first for those fields, target-side payload fallback onto persisted remote-row provenance for the live automation-command, gameplay-command, and target-region script-event handlers, Automation emitted-command handoff now carrying explicit target runtime scope and scheduling durable `enqueue_automation_command` follow-ups when the target scope is remote instead of flattening that handoff to the local owned region, durable handoff/control-plane history now preserving target runtime scope plus remote coordinator/followup ids for those command legs and supporting direct filtering by remote scope, remote ids, and origin script/plugin/dispatch identity, target-side gameplay-command rows now persisting `remoteCoordinatorId` / `remoteFollowupId` with durable `remoteFollowupId` idempotence and status lookup from those ids, remote coordinator/followup/result reads plus origin command-status reads now exposing target-command id/status from durable followup linkage rather than payload-only inference and now also carrying remote origin/target game-instance and region scope, current owned origin/target runtime `gameInstanceId` / `regionId` / `regionEpoch`, current owned origin/target routing bundle plus explicit stale-routing signaling, deadline policy, linked target-command execution/gameplay outcome, coordinator-visible target-leg identity (`targetEntityId`, `claimTargetAggregate`, `effectKey`, followup failure code/message), first-class trigger-script-event identity from the coordinator/followup/target-command rows, and the durable remote-followup contract itself on gameplay command status, with standalone remote coordinator/followup/result list surfaces now all supporting bounded filtering over persisted origin/target game-instance ids, region ids, region epochs, current owned origin/target runtime `gameInstanceId` / `regionId` / `regionEpoch`, provenance fields, admitted routing-bundle fields, live payload/result classifiers, direct work-item/target/result identity fields, the linked target/result command leg itself (`targetCommandId` or `resultCommandId` plus execution outcome and gameplay result), result-side linked followup identity/payload/trigger-event truth (`targetEntityId`, `claimTargetAggregate`, `effectKey`, `failureCode`, `payloadKind`, `originSourceKind`, `eventType`, `scriptEventId`), the newly explicit remote queue-source contract (`queueSourceKind`, `queueSourceState`, `queueSourceOrdinal`, `queueSourceDueTickId`, `queueSourceDueAtMs`), and now the already-exposed remote policy/state fields themselves (late-result policy, mirrored coordinator outcome, followup claim/solo/source state, requested command text, claimed tick-batch id, and concrete result message) instead of forcing a second client-side pass over returned rows, while the widened remote coordinator/followup/result list reads now bulk-hydrate linked followup/coordinator/target-command truth instead of degenerating into per-row point lookups as that operator contract grows, and the stronger query path is now backed by matching coordinator/followup/result routing-provenance plus identity indexes instead of leaving that richer filter surface on broad scans, pending origin reconciliation now waiting for linked target-command terminal convergence, failing closed when the target command evidence is missing, mirroring concrete target-command failure codes back onto the origin command instead of flattening everything to generic remote-abandoned status, and now also preferring concrete durable remote-result failure code/message when the target command row itself is unavailable, mirroring that same concrete remote failure code/message onto the terminal follow-up row instead of flattening it there too, rejecting retry-time remote metadata rewrites instead of allowing due/deadline/payload/provenance drift under one durable coordinator/followup identity, persisting remote payload kind/requested command plus result command/error/message identity as first-class row authority instead of reparsing JSON first on every read, treating `resultPayloadJson` as optional enrichment on the target-side result contract instead of replay-grade authority, carrying that same command/provenance/routing/payload-summary plus durable origin-source truth into the sealed `REMOTE_FOLLOWUP_QUEUE` manifest instead of leaving remote replay/inspection dependent on opaque payload blobs, and now also carrying the persisted fair-selection aggregate there so target-side drain ownership and operator inspection are anchored on the same execution aggregate instead of ad hoc `targetEntityId` fallbacks, structured payload/result summaries on remote control-plane reads and gameplay command status, due/backlog observation now exposed on runtime ownership reads plus `remoteFollowupDrainLagMs` and on the documented per-scope `remote_followups_due_total` / `remote_followups_drain_lag_ms` gauges, coordinator/followup/result control-plane reads now driven by current followup/result state plus persisted remote-row routing/provenance and direct script-dispatch identity truth, committed-tick timeout reconciliation, origin-tick result-inbox reconciliation ahead of timeout evaluation with stale-origin-epoch guardrails plus fail-closed result-scope validation, immutable retry identity guards, immutable `resultId` replay guards plus deterministic target-side `remote-result:<followupId>` writes that no longer rewrite original inbox timing on identical replay, stable mirrored timeout failure taxonomy across ignored late-result bookkeeping, paused-origin result reconciliation that stays live without advancing timeout progression, linked gameplay-command convergence onto `PENDING_REMOTE` / `TIMEOUT` / terminal remote outcomes, target-side claim/release work now staged as `REMOTE_FOLLOWUP_DRAIN` batches with one-claim-per-aggregate fairness plus explicit persisted `claimOrdinal` ordering and explicit missing-coordinator abandonment, and the now-live automation-command, gameplay-command, and target-region script-event remote payload families before broader producers and the later full cross-region return-path/result contract land.
   Note: the gameplay proof family is also now re-hardened against one recurring full-check websocket false negative: the move-after-play integration proof waits for the concrete `north` enqueue instead of failing early on the expected login bootstrap enqueue that precedes it.
5. [02.18.9 Region Epoch, Fencing, and Runtime Ownership](./02.18.9-task-list-region-epoch-fencing-and-runtime-ownership-vertical-slice.md)
   Remaining: the durable owner row, explicit current-boundary `regionId`, pause/resume epoch bumps, stale-fence batch rejection, stale post-drain effect requeue, last-committed-batch pointer, bounded ownership-status query, Automation tick-progress publication from that stored scope, Tick-side preservation of stored `regionId` across ownership observation, stale-fence checks, tick advancement, and batch manifests, region-first Automation gameplay admission validation, and now region-first `GetGameInstanceRuntimeState` resolution plus matching Automation stale-scope checks are all live at the current game-instance boundary; the next gap is carrying that model into true region ownership and later downstream/domain-specific effect families.
6. [10.1 Script Event Ingress and Handler Resolution](./10.1-task-list-script-event-ingress-and-handler-resolution-vertical-slice.md)
   Remaining: build on the now-live event-scope admission, ingress audit, durable bindings, pending-work materialization, tenant-readiness `onLoad`, current Game Session `onCommand` including direct communication commands, direct gameplay read handlers (`INVENTORY` / `EQUIPMENT` / `CONTAINER`, `WHO`, `FRIENDS`), gameplay-scoped `HELP`, gameplay-scoped world browse (`WORLDS` / `REALMS` / `CHARS`), gameplay-scoped authored actions, successful direct `PLAY` / `LOGOUT`, plus Automation/remote durable execution, fresh-PLAY `onSpawn`, movement plus forced/disconnect-backed `onLeaveRegion` producers, gameplay plus current scheduler and staged-command/status routing-bundle carry-through, fail-closed runtime region-scope fencing on gameplay-originated ingress, explicit remote target-scope handoff for emitted commands, and first durable evaluator by broadening producer coverage and moving beyond the current command-template execution boundary.
7. [02.15.8 Environment Preflight and Secret-Binding Convergence](./02.15.8-task-list-environment-preflight-and-secret-binding-convergence-vertical-slice.md)
   Remaining: build on the now-live JWT secret-path startup, mounted JWKS serving, base Kubernetes JWT/JWKS mounts, preview-unique JWT/JWKS rendering, expected-binding manifest schema expansion, `expectedBindingsRef` report output, full required preflight policy-ID emission, executable production/hobby traffic-open backup gates, and report contract tests by tightening the first-pass expected-binding validation against real environment evidence as it becomes available.

### 2. Active architecture follow-through

These are already partly real in code and should continue after the immediate runtime fixes.

1. [08 Game Design Publishing and Runtime Activation](./08-task-list-game-design-publishing-and-runtime-activation-vertical-slice.md)
   Remaining: the family now has real publish-attestation, asset lifecycle, typed launch-preflight outcomes, approved remap-set control-plane substrate, cutover-compatibility preflight, persisted `PrepareVersionUpgrade` record, retry-safe prepared cutover execution, asset-proof validation, version-state, activation substrate, and stricter participant guardrails including current Entity `S1` / `S2` compatibility; `08.4` and `08.5` are now complete at their current canonical seams, while the rest of the family continues through broader digest coverage, creator/editor UX adoption, broader purge/deletion truth, exact target-template validation, and later runtime world-state families.
2. [09 Multi-Tenancy, Realm Routing, and Runtime Boundaries](./09-task-list-multi-tenancy-realm-routing-and-runtime-boundaries-vertical-slice.md)
   Remaining: the family now has canonical routing, explicit public-production pointer metadata, public-production membership creation, bootstrap/connect-token alignment, scope-aware roster policy, first scoped friend/progression/activity mutation guards, scoped scripting runtime-state propagation, scope-aware account/friend presence reads, operator-facing admission-pointer/cutover tooling, and gateway-routed operator access; `09.3` is complete at the current boundary, and the next honest work is broader admission-pointer consumers plus later loadout/ability/resource-table follow-through as those state families land.
3. [02.13.8 Built-In Command Registry and Dispatch Rollout](./02.13.8-task-list-built-in-command-registry-and-dispatch-rollout-vertical-slice.md)
   Remaining: keep future built-in and authored command growth on the provider-backed registry plus family-handler seam without regressing into central interpreter branching.
4. [02.13.7 Action Classification and Activity Semantics](./02.13.7-task-list-action-classification-and-activity-semantics-vertical-slice.md)
   Remaining: built-in and first-pass authored actions now carry bounded optional tags/facets too; the next follow-through is real scripting/policy consumers of those tags rather than metadata-only attachment.
5. [02.13.9 Authored Action Definition and Execution Model](./02.13.9-task-list-authored-action-definition-and-execution-model-vertical-slice.md)
   Remaining: build on the now-live typed config-backed authored-action record, shared `commandId` registry seam, first non-built-in provider, authored `HELP` discovery, and fail-fast unsupported-metadata validation by adding richer targeting/cost/cooldown/effect semantics.
6. [10.1 Script Event Ingress and Handler Resolution](./10.1-task-list-script-event-ingress-and-handler-resolution-vertical-slice.md)
   Remaining: build on the now-live event-scope `TriggerScriptEvent` admission, ingress audit, built-in registry enforcement/read APIs, snapshot-token validation, plugin runtime-state fencing for plugin-trigger ingress, durable script event bindings, first `script_work_items` outbox materialization, handler-scoped `script_event_audit` rows, real pending-work cancellation, current Game Session `onCommand` coverage for queued gameplay, direct communication, direct gameplay reads, fresh-PLAY `onSpawn`, and canonical gameplay disconnect `onLeaveRegion` producers, pending-work claiming, first durable evaluator/handoff path, and dry-run pre-resolution quota guard by broadening producer coverage and moving beyond the current command-template execution boundary.
7. [10.3 Script Execution Budgets, Quotas, and Isolation](./10.3-task-list-script-execution-budgets-quotas-and-isolation-vertical-slice.md)
   Remaining: terminal outbox retention cleanup, serialized work-item payload-size rejection, target-aware command-count output-budget enforcement, configurable pin/rollout projection freshness, plugin-policy freshness, admission-time live per-script quota charging, priority-tagged live tenant-budget reservation, live-budget bypass for dry-runs, first per-minute dry-run tenant/principal quotas, tenant-local plus cluster-wide dry-run execution capacity, and first durable work-item outcome metrics are now live from documented knobs; the next gap is richer source policy as more producers land.
8. [10.5 Scripting Operator Visibility and Runtime Convergence](./10.5-task-list-scripting-operator-visibility-and-runtime-convergence-vertical-slice.md)
   Remaining: durable work/audit/cancel/claim state, terminal cleanup, current script-patch readiness projections plus supersession semantics with concrete terminal `onLoad` reason preservation, bounded dead-letter listing plus replay including readiness-scoped `onLoad` replay behind canonical readiness projection checks, operator-canceled readiness now surfacing as `ROLLED_BACK`, patch-status listing preserving legacy/runtime-only rows alongside readiness-projected rows and now cross-linking current Game Design script-patch publication metadata while also preserving concrete legacy terminal cancel/failure reasons instead of flattening them to generic rollback/failure labels, Automation pin-convergence and its durable pin projection now carrying current owned runtime `regionId` / `regionEpoch` plus region-aware stale refresh through the last observed runtime scope, Game Session pin-convergence, pinned patch version/runtime-state rows now also exposing current owned runtime `regionId` / `regionEpoch`, gameplay command status plus remote coordinator/followup/result reads now also carrying plugin publication cross-links alongside script-patch publication and runtime provenance, schedule-instance rows, dead-letter rows, handoff history, and scheduler timer audit rows now all carrying current owned runtime scope plus current owned routing bundle and stale-scope/routing signaling where applicable, handoff history also including pre-enqueue stale-scope cancellation and later gameplay-command execution truth, and scheduler timer audit now also carrying both script-patch publication and plugin publication cross-links when plugin-owned runtime rows are involved, with plugin activation preflight, scheduler materialization, and scoped plugin-policy convergence now all failing closed on exact current runtime scope (`runtimeRegionId` plus `runtimeRegionEpoch`) instead of only region id, first scope-local drain reads, first Automation/Game Session pin-convergence reads with persisted pin request ids, first per-instance script-patch rollout reads with explicit freshness flags and now publication cross-links on current rollout status, rollout listings, and rollout event history, Automation-owned plugin runtime state transitions plus append-only runtime history with publication cross-links on current status, runtime event, and policy-convergence violation surfaces and now also the last observed Game Session runtime `regionId` / `regionEpoch` so later runtime-state reconciliation and policy-fail-closed reads no longer flatten back to `gameInstanceId` only, Game Design-backed plugin publication plus immutable runtime compatibility checks (`baseVersionId` and release-bundle-backed `abilitySchemaDigest`), signer/component-policy activation gates, scheduled policy reconciliation for already-enabled plugins, and plugin-version pending-work cancellation now exist; the next gaps are later freshness/read-model separation and richer activation/publication event handling.
9. [02.1.3 Session Activity and WHO Presence](./02.1.3-task-list-session-activity-and-who-presence-vertical-slice.md)
   Remaining: grow from the current bounded `WHO` plus explicit/auto-AFK and canonical lifecycle substrate into the fuller activity model when that work becomes active.
10. [02.1.4 Cross-Game Social Presence and Friend Activity](./02.1.4-task-list-cross-game-social-presence-and-friend-activity-vertical-slice.md)
   Remaining: grow the first account-scoped friend presence seam, now consumed by REST, gRPC, gameplay `FRIENDS`, first-party-web structured friend roster/detail/summary/policy plus structured mutation-result payloads with fail-closed gameplay grammar, the now-canonical gameplay plus REST/gRPC Social Groups visibility-policy read/write seam, and one shared Account-profile JSON helper for that visibility-policy substrate across live clients plus shared runtime proof, into later social consumers and later privacy refinement without reopening `WHO`.
11. [06 Task List Inventory, Containers, Equipment](./06-task-list-inventory-containers-equipment-vertical-slice.md)
   Remaining: holder-transfer safety, canonical transfer-audit persistence with attested session/effect correlation, Game Session -> Game Logic -> Entity Management item command routing, game-configured equipment slot/body-layout validation, and WebSocket plus Telnet cross-service room-ground/equipment happy/failure paths are now live; the main remaining work is the `06.3` follow-through and later authored stackability depth.
12. [06.3.1 Stable Item Instance Visible Ref Allocation](./06.3.1-task-list-item-instance-visible-ref-allocation-vertical-slice.md)
   Remaining: build on the now-live stable visible-ref substrate across inventory/equipment/container/room-ground and `HERE`-style targeting views for non-player items and entities; players remain the special case and are still identified by character name rather than a generated visible ref.
13. [06.3.2 Authored Stackability and Fungibility](./06.3.2-task-list-authored-stackability-and-fungibility-vertical-slice.md)
   Remaining: build on the now-live authored `stackVariantKey` plus runtime `stackFamilyKey` substrate with richer authored family sources; explicit stack-family selector UX for ambiguous holder-local families is now live.
14. [02.18.10 Effect Idempotency and Replay Guards](./02.18.10-task-list-effect-idempotency-and-replay-guards-vertical-slice.md)
   Remaining: the first ledger-side `effectKey`, deterministic `effectId`, movement-backed replay/no-op seam, Game Session communication/activity replay guard, Social Groups communication replay guard, Entity Management item mutation response replay guard, transfer-audit `effectId` plus session correlation, and first apply/replay metrics are now live; the next honest gap is later domain-specific effect guards as new owning mutation families land, not another forced proving-ground batch in the current built-in command set.

15. [02.18 Service Boundary and Audit Hardening](./02.18-task-list-service-boundary-and-audit-hardening-vertical-slice.md)
   Remaining: continue any later hardening only if real-load evidence justifies it; the original audit-family scheduler-pressure/operator-proof tail in `02.18.6` is now closed at the current boundary.

16. [02.18.1 Audit Log and Moderation Separation](./02.18.1-task-list-audit-log-and-moderation-separation-vertical-slice.md)
   Remaining: keep future callers on the dedicated log-event path; the current account/logging-admin separation is now in place and covered.

### 3. Design settled enough, but not started or only placeholder-level

These are not broad audit topics anymore; they are real future slices with known direction.

1. [02.19 jOOQ and Flyway Persistence Convergence](./02.19-task-list-jooq-and-flyway-persistence-convergence-vertical-slice.md)
   Remaining: the architecture decision and shared foundation are now in place, Automation Scripting is fully migrated at the service boundary, and Game Session has its first real control-plane `jOOQ` cluster; the next honest work is the rest of the service-by-service migration family, starting with the heavier Game Session runtime/tick/remote cluster.
2. [02.20 Temporal Control-Plane Workflow Convergence](./02.20-task-list-temporal-control-plane-workflow-convergence-vertical-slice.md)
   Remaining: the control-plane-only Temporal decision, shared foundation, and first world-lifecycle adopter are now live; the remaining work is the publish/script-patch workflow migration plus the later boundary cleanup.
3. [10 Scripting, Automation, and Runtime Orchestration](./10-task-list-scripting-automation-and-runtime-orchestration-vertical-slice.md)
   Remaining: the scripting domain now has a canonical family instead of fragmented indirect coverage; `10.1` through `10.5` are the bounded runtime/control-plane cuts, with explicit freshness now live across rollout, plugin-policy status, plugin-policy convergence, Automation pin-convergence, Game Session pin-convergence, drain-status, and scheduler-materialization reads, while plugin activation preflight, policy-convergence reads, and schedule materialization now also fail closed against stale same-instance plugin runtime rows by respecting the current observed runtime region, and design-time publication boundaries remain intentionally anchored in `08.4`.
4. [02.1.1 Email OTP and Text Auth Options](./02.1.1-task-list-email-otp-and-text-auth-options-vertical-slice.md)
5. [02.1.5 Admin and God Capability and Visibility](./02.1.5-task-list-admin-god-capability-and-visibility-vertical-slice.md)
6. [02.1.4.1 Account Versus Character Social Scope](./02.1.4.1-task-list-account-vs-character-social-scope-vertical-slice.md)
7. [02.1.5.1 Hidden Staff Modes and Capability Bundles](./02.1.5.1-task-list-hidden-staff-modes-and-capability-bundles-vertical-slice.md)
8. [02.13.10 Structured Transcript and Replay End State](./02.13.10-task-list-structured-transcript-and-replay-end-state-vertical-slice.md)
   Remaining: the hot reconnect buffer now stores structured `PlayerOutput` replay metadata alongside classic rendered protocol text for new entries and keeps text-only legacy entries readable; durable transcript history and retention classes remain future work.
9. [02.13.10.1 Structured Transcript Persistence and Replay Storage](./02.13.10.1-task-list-structured-transcript-persistence-and-replay-storage-vertical-slice.md)
   Remaining: build on the structured hot-buffer entry model by adding bounded durable transcript history when product retention policy becomes active.
10. [02.13.11 Shared Time, Duration, and Scheduler Semantics](./02.13.11-task-list-shared-time-duration-and-scheduler-semantics-vertical-slice.md)
   Remaining: the first proto contract guard now prevents ambiguous cross-service time fields such as bare `timeout`, `expires`, and `duration`; broader runtime adoption remains future work as timed gameplay systems land.
11. [07.4 Unified Actor Model](./07.4-task-list-unified-actor-model-vertical-slice.md)
12. [07 Entity Stats and Conditions](./07-task-list-entity-stats-and-conditions-vertical-slice.md)
   Remaining: Entity Management now has the first gameplay-attested actor-state read substrate for baseline resources plus persisted resource/condition rows; authored definitions, shared effect evaluation, mutation/expiry, equipment/action contributions, and damage/mitigation remain future work.
13. [07.1 Shared Effect Engine](./07.1-task-list-shared-effect-engine-vertical-slice.md)
   Remaining: the first typed in-process effect evaluation seam now exists for additive/multiplicative/clamp modifiers, granted flags/conditions, scopes, and provenance, and active condition payloads are wired through it for actor-state reads; authored definitions plus equipment/action-state producer wiring remain future work.
14. [07.2 Equipment and Action-State Contributions](./07.2-task-list-equipment-and-action-state-contributions-vertical-slice.md)
   Remaining: equipped item templates can now contribute effect payload modifiers through actor-state reads, and an internal condition/action-state apply/expire seam exists; player command wiring, scheduled expiry execution, and richer scoped combat-facing consumption remain future work.
15. [07.3 Damage and Mitigation Resolution](./07.3-task-list-damage-and-mitigation-resolution-vertical-slice.md)

### 4. Lower-priority platform/settings expansion

These are still valid, but they are behind the current gameplay/runtime slices.

1. [06.1 Inventory and Equipment Settings](./06.1-task-list-inventory-and-equipment-settings-vertical-slice.md)
2. [02.9.1 Settings Presets and Operator Baselines](./02.9.1-task-list-settings-presets-and-operator-baselines-vertical-slice.md)

### 5. Manual QA / operator-proof tails

These are not major design problems, but they are not fully closed.

1. [02.3 Reconnect and Session Recovery](./02.3-task-list-reconnect-and-session-recovery-vertical-slice.md)
2. [02.4 First-Party Reconnect Parity](./02.4-task-list-first-party-reconnect-parity-vertical-slice.md)
3. [02.5 Non-Edge Failover Invisibility](./02.5-task-list-non-edge-failover-invisibility-vertical-slice.md)
4. [02.8 Game Logic Restart Invisibility](./02.8-task-list-game-logic-restart-invisibility-vertical-slice.md)
5. [02.14.4 Metrics Cardinality and Label Policy Hardening](./02.14.4-task-list-metrics-cardinality-and-label-policy-hardening-vertical-slice.md)
   Remaining: only opportunistic audit tail work and later policy wording cleanup.
6. [02.14.5 Player-Experience Canary and Deadman Smoke](./02.14.5-task-list-player-experience-canary-and-deadman-smoke-vertical-slice.md)
   Remaining: runtime canary/blackbox/deadman producers and failure injection are paused pending endpoint, synthetic identity, external deadman authority, and non-production paging-route decisions.

## Operator-Proof / Audit Tails

These are still useful, but they are not the next best feature/code-shape slices.

1. `02.14.4` opportunistic metrics-cardinality tail only
2. `02.14.5` prod-like canary/deadman smoke proof
