# Social Groups jOOQ Migration Vertical Slice

## Goal and Status

Goal: migrate `social-groups-service` onto `jOOQ + Flyway` so friend, guild, and social-presence persistence no longer sits on a separate ORM stack after the rest of the SQL services have converged. Status: planned.

## Scope

- replace JPA repository/entity seams in social-group persistence with `jOOQ`;
- preserve the current Flyway baseline and gameplay/REST/gRPC contracts;
- remove JPA/Hibernate runtime/config dependencies from the service.

## Checklist

- [ ] Inventory current JPA social-group persistence seams.
- [ ] Replace them with `jOOQ` queries and mappings.
- [ ] Remove JPA/Hibernate runtime/config dependencies.
- [ ] Re-prove social REST/gRPC/gameplay consumers on the new SQL path.
