# Vertical Slice Ideas Index

This zero-indexed overview sits at the top of the design/project-management/vertical-slices folder. When a slice is selected for work, add a full task list file and update this index so the team can quickly see what's already live, what follow-up slices exist, and what's queued next.

0. **Login and Session Hardening** - Follow-up slice to reduce `dev-isolated` dependence, re-enable representative integration coverage, and make the existing login/session path more trustworthy without reopening the original login feature scope (planned in `02.1-task-list-login-session-hardening-vertical-slice.md`).
1. **Directional Movement (MOVE/GO)** - Use the current World snapshots and movement primitives to validate exits, update player location, and stream a fresh `LOOK` result so movement becomes part of the real gameplay loop (planned in `05-task-list-movement-vertical-slice.md`).
2. **Inventory, Containers, and Equipment** - Extend the playable loop with hidden inventory containers, room-attached ground containers, first-class equipment bindings, game-configurable slot/body-layout rules, richer inventory queries, and an auditable transfer trail (planned in `06-task-list-inventory-containers-equipment-vertical-slice.md`).
3. **Gameplay Admission UX Alignment** - Align the login/lobby/player-facing command flow with a simpler MUD-like experience: public `WORLDS` browsing, `LOGIN`, and `PLAY <world> [character]` as the normal happy path, with `SESSION` demoted to advanced-client metadata (planned in `02.2-task-list-gameplay-admission-ux-vertical-slice.md`).
4. **Reconnect and Session Recovery Semantics** - The baseline Telnet and generic WebSocket reconnect slice is now live enough to stand on its own: screen-buffer restore, fresh-entry fallback, runtime `PLAY` authority, and edge classification are implemented in `02.3-task-list-reconnect-and-session-recovery-vertical-slice.md`.
5. **First-Party Reconnect Parity** - First-party `/ws/game/**` reconnect is now baseline live with fresh connect-token handshake enforcement, signed connect-context validation, bare first-party `LOGIN`, and reconnect redraw parity in `02.4-task-list-first-party-reconnect-parity-vertical-slice.md`; manual QA remains.
6. **Non-Edge Failover Coordination Prerequisites** - The shared-state prerequisite is now baseline live: first-party connect-context tracking and disconnect dedupe are externalized out of local Game Session memory in `02.5-task-list-non-edge-failover-invisibility-vertical-slice.md`; manual QA remains.
7. **Live Backend Rebind Invisibility** - The Gateway-owned gameplay bridge, stable edge transport session ids, and focused abrupt-upstream-loss rebind proof are now baseline live in `02.6-task-list-live-backend-rebind-invisibility-vertical-slice.md`.
8. **Game Session Process Restart Invisibility** - The focused same-JVM Game Session-like process-bounce proof is now baseline live in `02.7-task-list-process-restart-invisibility-vertical-slice.md`; Game Logic continuity continues in `02.8-task-list-game-logic-restart-invisibility-vertical-slice.md`.
9. **Game Logic Process Restart Invisibility** - The focused post-restart command proof is now baseline live in `02.8-task-list-game-logic-restart-invisibility-vertical-slice.md`: move-derived room context survives a real Game Logic restart on the same connected client because the player-visible world/session state is not stored in Game Logic memory; manual QA remains.
10. **Platform Settings Model and Generated Config Docs** - This cross-cutting settings slice is now pre-`06` complete in `02.9-task-list-platform-settings-model-vertical-slice.md`: operator/bootstrap defaults, shared persisted tenant/game overrides, shared merged effective resolution, and generated settings schema/reference are all in place.
11. **Settings Presets and Operator Baselines** - Add named preset baselines above ordinary operator overrides so most deployments can start from strong defaults and only tweak the settings they really need (planned in `02.9.1-task-list-settings-presets-and-operator-baselines-vertical-slice.md`).
12. **Settings Authority and Persistence** - Add the first shared persisted settings authority and tenant/game override storage model for the surfaced pre-`06` domains (planned in `02.9.2-task-list-settings-authority-and-persistence-vertical-slice.md`).
13. **Effective Settings Resolution and Invalidation** - Add one shared effective-settings resolver with canonical precedence, bounded caching, and cross-service runtime consumption (planned in `02.9.3-task-list-effective-settings-resolution-and-invalidation-vertical-slice.md`).
14. **Generated Settings Schema and Reference** - Add generated schema/reference output from the canonical settings metadata rather than relying only on service-local configuration docs (planned in `02.9.4-task-list-generated-settings-schema-and-reference-vertical-slice.md`).
15. **Communication and Prompt Settings** - Surface shared communication defaults, prompt behavior, and prompt delivery policy through the new settings model instead of leaving them as code-only defaults (planned in `02.10-task-list-communication-and-prompt-settings-vertical-slice.md`).
16. **LOOK and Transcript Settings** - Surface room-view, reconnect screen-buffer, and transcript rendering policy through the settings model so `LOOK` and later `QUICKLOOK` evolve through explicit config (planned in `02.11-task-list-look-and-transcript-settings-vertical-slice.md`).
17. **Movement and Topology Settings** - This settings slice is now pre-`06` complete in `02.12-task-list-movement-and-topology-settings-vertical-slice.md`: movement redraw policy, normalized topology effective settings, and the typed topology home for future scope-sensitive communication are surfaced through config.
18. **Input, Output, and Presentation Model** - This cross-cutting architecture slice is now pre-`06` complete in `02.13-task-list-input-output-and-presentation-model-vertical-slice.md`: the structured command/output model, late rendering seam, prompt separation, and renderer policy hooks are all in place.
19. **Normalized Command Envelope Rollout** - The typed command-payload rollout is now pre-`06` complete in `02.13.1-task-list-normalized-command-envelope-rollout-vertical-slice.md`, with normalized verb metadata and typed payloads covering the active built-in command set.
20. **Player Output Envelope Rollout** - The `PlayerOutput` rollout is now pre-`06` complete in `02.13.2-task-list-player-output-envelope-rollout-vertical-slice.md`: gameplay responses, replay policy, and structured error paths no longer depend on raw strings as the canonical internal contract.
21. **Prompt Pipeline and Structured Status** - The prompt/status rollout is now pre-`06` complete in `02.13.3-task-list-prompt-pipeline-and-structured-status-vertical-slice.md`: coalescing, prompt throttling, reconnect prompt re-emit, and first structured prompt fields are implemented.
22. **Presentation Policy: BRIEF, Color, and Room Refresh** - This slice is now pre-`06` complete in `02.13.4-task-list-presentation-policy-brief-color-and-room-refresh-vertical-slice.md`: the first color modes, broader `BRIEF` suppression, and explicit room-refresh policy are live, while combat-sensitive brief-on-move remains later policy work.
23. **Localization Foundation** - The first runtime/content-model localization foundation is now pre-`06` complete in `02.13.5-task-list-localization-foundation-vertical-slice.md`, using built-in text templates, stored localized content variants, and future AI-assisted offline localization generation rather than live translation on the gameplay hot path.
24. **Runtime Identity and Structured Logging Consistency** - This baseline infrastructure slice is now pre-`06` complete in `02.14-task-list-runtime-identity-and-structured-logging-consistency-vertical-slice.md`: shared runtime identity, startup lifecycle logging, request-path baseline fields, gameplay-context enrichment, structured logging audit, and `/actuator/runtime` are implemented.
25. **Gameplay Context Logging Enrichment** - This follow-up is now pre-`06` complete in `02.14.1-task-list-gameplay-context-logging-enrichment-vertical-slice.md`: the highest-value Game Session and Game Logic paths attach `tenantId`, `gameInstanceId`, and `characterId`, while `regionId` remains deferred pending a canonical source.
26. **Structured Logging Consistency Audit** - This audit follow-up is now pre-`06` complete in `02.14.2-task-list-structured-logging-consistency-audit-vertical-slice.md`: the main HTTP/gRPC/WebSocket consistency gaps are closed and bounded exceptions are documented.
27. **Observability Label Exception Documentation** - This follow-up is done in `02.14.3-task-list-observability-label-exception-documentation-vertical-slice.md`: no `serviceInstanceId` metric-label exceptions are currently approved.
28. **Shared Communication Infrastructure** - Refactor the initial room-speech path into a reusable communication system that all later communication actions can use, while keeping `say` as the first fully live mode (planned in `04.1-task-list-shared-communication-infrastructure-vertical-slice.md`).
29. **Whisper Communication** - The first true target-directed in-room communication mode is now baseline live, with canonical sender/target prose and default metadata-only observer wording recorded in `04.2-task-list-whisper-vertical-slice.md`.
30. **Tell Communication** - The first direct non-room-scoped live gameplay tell is now baseline live, with canonical sender/target prose recorded in `04.3-task-list-tell-vertical-slice.md`.
31. **Communication Observers & Interceptors** - The baseline observer/interceptor slice is now live for structured metadata-only `whisper` observer resolution in `04.4-task-list-communication-observers-and-interceptors-vertical-slice.md`.
32. **Communication Recipient Delivery** - Target-side and metadata-only observer-side communication delivery are now baseline live for generic WebSocket and Telnet in `04.5-task-list-communication-recipient-delivery-vertical-slice.md`; first-party/MCP-aware delivery remains future work.
33. **Basic Multiplayer Load Proof** - Add the first bounded real multiplayer/load validation with roughly 10 concurrent players performing `LOGIN` -> `PLAY` -> `LOOK` against the real service stack, using generated test data where practical (planned in `05.1-task-list-basic-multiplayer-load-proof-vertical-slice.md`).
34. **Hosted Preview Manual Proof** - Turn PR preview into a real reviewer-usable hosted environment and make manual `LOGIN -> PLAY -> LOOK` over TCP/Telnet the first hosted proof milestone (planned in `02.15-task-list-hosted-preview-manual-proof-vertical-slice.md`).
35. **Preview Internal gRPC Transport Alignment** - Explicitly document the temporary preview/plaintext internal gRPC stance, keep mTLS as the canonical target state, and define the cleanup criteria for removing the exception (planned in `02.15.1-task-list-preview-internal-grpc-transport-alignment-vertical-slice.md`).
36. **Spring gRPC Server TLS Bundle Migration** - Replace stale server-side gRPC TLS configuration patterns with the Spring gRPC `1.0.x` SSL-bundle model so internal mTLS is configured correctly and consistently (planned in `02.15.2-task-list-spring-grpc-server-tls-bundle-migration-vertical-slice.md`).
37. **Preview Rollout Diagnostics and Fast Failure** - Improve preview visibility so blocked rollouts explain the first real dependency/config problem without waiting blind through Helm timeouts (planned in `02.15.3-task-list-preview-rollout-diagnostics-and-fast-failure-vertical-slice.md`).
38. **gRPC Transport Configuration Sanity Checks** - Add fast local/CI checks that reject legacy or misleading gRPC transport configuration patterns before smoke or preview discovers them (planned in `02.15.4-task-list-grpc-transport-configuration-sanity-checks-vertical-slice.md`).
39. **Dedicated First-Party Web App Service** - Introduce the dedicated long-term home for browser assets and first-party web bootstrap, starting with a terminal-style browser client if needed (planned in `02.16-task-list-dedicated-first-party-web-app-service-vertical-slice.md`).
40. **Local Reset and Bootstrap Proof Tooling** - Add one canonical local reset/rebuild-from-zero toolchain so developers can reproduce preview/smoke startup issues, prove fresh bootstrap, and safely test pre-launch migration cleanup (planned in `02.17-task-list-local-reset-and-bootstrap-proof-tooling-vertical-slice.md`).
41. **Service DB Reset and Flyway Hygiene** - Add service-scoped destructive reset tooling that understands owned Flyway history and service-local schema cleanup rather than wiping the whole local stack (planned in `02.17.1-task-list-service-db-reset-and-flyway-hygiene-vertical-slice.md`).
42. **Fresh Bootstrap and Restart Proof Tooling** - Add explicit local verification commands for “boot from zero” and “restart against existing state,” including a narrow gameplay happy-path proof (planned in `02.17.2-task-list-fresh-bootstrap-and-restart-proof-tooling-vertical-slice.md`).
43. **Flyway Migration Sanity Checks** - Add fast local/CI validation for obvious migration-shape errors such as duplicate Flyway versions before full smoke or preview startup discovers them (planned in `02.17.3-task-list-flyway-migration-sanity-checks-vertical-slice.md`).

---

## Beyond MVP - Future Enhancements

- **Advanced Reconnection Replay** - Extend the reconnect cache to replay buffered commands or cached room/global context so reconnecting clients are dropped back in without losing actions.
- **Dynamic Room Scripts/Triggers** - Let rooms surface scripted events or ambient messages after a `LOOK`, proving Game Logic can orchestrate scripting or event services alongside the base description.
- **Social Channels & NPC Responses** - Expand SAY to feed NPC dialogue or group channels, showcasing richer text patterns and multi-service orchestration (Social, Entity, Group services).
- **Shout and Communication Scope Settings** - Add `shout` as a future built-in only after the game-settings/configuration model can express topology-dependent propagation such as region-wide versus map-wide delivery when regions are absent or disabled.

Note: After choosing the next slice, add a corresponding numbered task list file, update this index, and reconcile any duplicated items in the existing per-service status docs and design docs.
