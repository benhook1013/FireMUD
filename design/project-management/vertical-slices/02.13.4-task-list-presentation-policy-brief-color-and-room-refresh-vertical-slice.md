# Presentation Policy: BRIEF, Color, and Room Refresh Vertical Slice Task List

## Goal and Status

Goal: make `BRIEF`, color capability, and room-refresh presentation policy real parts of the structured presentation model rather than scattered string-shaping rules. Status: in progress.

## Implementation Notes

- `game-session-service` now has the first real color-mode branches in `TextPlayerOutputRenderer` for built-in room-view labels, notices, and prompts.
- The renderer now also honors `BriefRenderPolicy.SUPPRESS_IN_BRIEF` for non-view outputs, making `BRIEF` more than a single `LOOK`-specific boolean proof.
- Movement-specific room-refresh policy and combat-sensitive brief-on-move behavior remain follow-up work once the movement path exposes those presentation hooks explicitly.

## Scope

- `BRIEF` and verbose room-view policy
- color mode behavior (`none`, `basic`, `rich`)
- movement-triggered room refresh policy
- combat-sensitive brief-style movement refresh default

## Key Tasks

- [~] Expand the current `BRIEF` handling from the initial proof into a deliberate presentation-policy layer.
- [~] Define how structured outputs carry the tags or hints needed for `BRIEF` suppression.
- [x] Implement the first real color-mode branches in the renderer without leaking ANSI/rich formatting into gameplay logic.
- [ ] Refine movement-triggered room refresh to use the same room-view model with different presentation policy.
- [ ] Add the default combat-sensitive rule that movement-triggered room refresh can automatically render in brief-style form while in combat.

## Tests

- [x] Add renderer tests for `BRIEF` versus verbose behavior on room views.
- [x] Add color-mode regression tests for the first bounded built-in outputs.
- [ ] Add tests proving combat-sensitive brief-on-move policy when that state becomes available.

## Notes

- `BRIEF` should remain primarily tag/policy driven, not dual-authored prose everywhere.

## Validation

- [x] `./gradlew check`
- [x] `./gradlew linkCheck lintMarkdown`
