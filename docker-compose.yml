version: '3.9'
services:
  account-service:
    build: ./account-service
    ports:
      - "8081:8080"
  gateway:
    build: ./spring-cloud-gateway
    ports:
      - "8080:8080"
  # TODO: Add other services here
