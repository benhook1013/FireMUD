package net.firedevops.firemud.gamesession.presentation;

/** Controls whether a rendered output may be retained for reconnect transcript replay. */
public enum ReplayPolicy {
  BUFFERABLE,
  NO_REPLAY
}
