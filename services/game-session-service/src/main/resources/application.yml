spring:
  profiles:
    active: dev
  application:
    name: game-session-service

management:
  endpoints:
    web:
      exposure:
        include: health
  endpoint:
    health:
      probes:
        enabled: true
