spring:
  profiles:
    active: dev
  application:
    name: social-groups-service

management:
  endpoints:
    web:
      exposure:
        include: health
  endpoint:
    health:
      probes:
        enabled: true
