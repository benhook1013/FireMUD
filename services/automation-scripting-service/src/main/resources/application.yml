spring:
  profiles:
    active: dev
  application:
    name: automation-scripting-service
  flyway:
    enabled: true

management:
  endpoints:
    web:
      exposure:
        include: health,prometheus
  endpoint:
    health:
      probes:
        enabled: true
