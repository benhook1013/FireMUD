spring:
  profiles:
    active: dev
  application:
    name: logging-admin-service
  flyway:
    enabled: true

management:
  endpoints:
    web:
      exposure:
        include: health
  endpoint:
    health:
      probes:
        enabled: true
