spring:
  profiles:
    active: dev
  application:
    name: game-design-service

management:
  endpoints:
    web:
      exposure:
        include: health
  endpoint:
    health:
      probes:
        enabled: true
