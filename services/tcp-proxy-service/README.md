# TCP Proxy Service

The complete design can be found in:
[TCP Proxy Service Design](../../design/architecture/microservices/tcp-proxy-service/README.md)

This README is only a stub for reference. Do not include design details here.

## Local Development

Run the proxy against the normal local Gateway -> Game Session gameplay path. The canonical Telnet proof is the source-built smoke stack under `dev-tools/`, not a proxy-only echo loop.
