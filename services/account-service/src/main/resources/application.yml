spring:
  profiles:
    active: dev
  application:
    name: account-service
  flyway:
    enabled: true
grpc:
  server:
    port: 6565

management:
  endpoints:
    web:
      exposure:
        include: health
  endpoint:
    health:
      probes:
        enabled: true
