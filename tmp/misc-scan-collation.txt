FireMUD misc follow-up tracker
Updated: 2026-07-08

Purpose
- Temporary tracking list extracted from the earlier misc scan collation.
- This file now tracks concrete follow-up slices rather than preserving the raw discussion summary.

Status key
- done
- next
- later

done - Production-shaped checked-in secrets/defaults
- Terraform production Postgres auth should come from Terraform inputs instead of checked-in values.
- Local Velero/MinIO path should stop shipping checked-in live credentials in `k8s/velero/minio.yaml` and docs.

next - Repeated demo/local credential contracts
- Hosted preview/dev-demo/smoke now routes through a shared demo-smoke defaults file instead of repeating inline login values.
- Local Compose and example Helm values still repeat weak/demo defaults across several files.
- Goal: reduce the remaining duplication and make the contract more canonical across local and hosted paths.

next - Automation determinism and environment contracts
- Harden CI/bootstrap downloads that currently use direct scripts or unverified binaries.
- Decide whether Java 21 build + Java 25 runtime is intentional and document or align it.
- Make CI Node/tool provisioning explicit where docs validation depends on it.

later - Operational script hardening
- `dev-tools/docs/generate-erd.sh`: remove mutable tag and hardcoded local DB assumptions.
- `dev-tools/restore-redis-aof.sh`: add stronger guardrails around destructive behavior.
- `dev-tools/verify-fresh-bootstrap.sh`: clarify when the heavy proof path is the correct tool.

later - Residual hygiene
- Crash-log and backup-directory cleanup guidance.
- Local cert material framing.
- Legacy/example chart clarity where it still teaches placeholder surfaces.
